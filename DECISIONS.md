# Decisions log

Interview script — every real choice, why it was made. See `CLAUDE.md` Rule 4.
Numbered per phase (`INFRA-1`, `DATA-1`, ...) so a phase can grow without renumbering
everything else. Result numbers live only in the baseline table below — decision rows
below don't repeat them.

## Current baseline (post-`RETR-7` re-index, 2026-09-04 — `RETR-39`)

Dev n=1235, test n=1546, `RETR-35` coverage-based labels, corpus re-indexed under
`RETR-7`/`RETR-8`. **These supersede every table below.**

| Arm (dev unless stated) | recall@10 | recall@50 | nDCG@10 |
|---|---|---|---|
| 1 — dense | 0.337 ± 0.013 | 0.473 | 0.226 |
| 2 — +BM25/RRF | 0.514 ± 0.014 | 0.708 | 0.345 |
| 3 — +reranker | 0.629 ± 0.013 | 0.708 | 0.488 |
| 3 + company filter + query strip | **0.760** | 0.802 | 0.627 |
| **3 + filter + strip, TEST** | **0.747** ± 0.012 | 0.785 | 0.617 |

Arm 4-B/C were deliberately not re-embedded and stay on pre-`RETR-7` headings, so they are no
longer text-comparable with variant A. Arm 1/Arm 2 moved corpus *and* labels at once — quote
their levels, never a delta (`data/retr7_ANALYSIS.md`).

## Superseded baseline (`GOLD-1` labeler, pre-re-index — kept because the rows below cite it)

Corpus: 799 filings, 99,654 chunks (T²-RAGBench FinQA+ConvFinQA, full CIK coverage).
Dev split: n=1235, seed 42. Gold-relevance labeling: `gold_inds`-based positional
matching, fallback to clustered word-alignment for the ~4.9% that didn't resolve
(`GOLD-1`) — **numbers below supersede every prior run of Arms 1-4**, rescored against
this labeler, same retrieved rankings otherwise unchanged (Arm 3/4 rescored offline from
already-saved reranked orderings; Arm 1/2 retrieval re-run, no ranking logic changed).

| Arm | recall@10 | recall@50 | nDCG@10 | MRR | latency |
|---|---|---|---|---|---|
| 1 — dense (BGE-M3) | 0.329 ± 0.012 | 0.466 ± 0.013 | 0.233 ± 0.009 | 0.247 ± 0.010 | — |
| 2 — +BM25/RRF | 0.495 ± 0.013 | 0.687 ± 0.011 | 0.344 ± 0.010 | 0.348 ± 0.011 | — |
| 3 — +bge-reranker-v2-m3 | 0.609 ± 0.012 | 0.685 ± 0.012 | 0.495 ± 0.011 | 0.521 ± 0.012 | p50 4.67s / p95 4.87s |

Arm 4 (same pipeline as Arm 3, gold tables only re-indexed per variant — `ARM4-3`):

| Variant | recall@10 | recall@50 | nDCG@10 | MRR | latency |
|---|---|---|---|---|---|
| A — whole table (baseline) | 0.609 ± 0.012 | 0.687 ± 0.011 | 0.496 ± 0.011 | 0.522 ± 0.012 | p50 4.32s / p95 4.50s |
| B — one row per chunk | 0.350 ± 0.012 | 0.498 ± 0.013 | 0.272 ± 0.010 | 0.305 ± 0.011 | p50 4.25s / p95 4.46s |
| C — raw table + LLM summary | 0.352 ± 0.010 | 0.497 ± 0.011 | 0.296 ± 0.009 | 0.400 ± 0.012 | p50 4.23s / p95 4.41s |

Company-filtered candidate generation (`RETR-5`, company resolved from question text only).
Same dev split, same labeler, same metrics — one change, a `stem`-prefix predicate on both
retrieval legs. Baseline rows repeated for comparison; `data/*_companyfilter.json` sidecars
hold the runs, the unfiltered results are untouched:

| Arm | recall@10 | recall@50 | nDCG@10 | MRR |
|---|---|---|---|---|
| 1 — dense | 0.329 → **0.384** ± 0.013 | 0.466 → **0.607** ± 0.012 | 0.233 → **0.271** ± 0.010 | 0.247 → **0.286** ± 0.011 |
| 2 — +BM25/RRF | 0.495 → **0.520** ± 0.013 | 0.687 → **0.776** ± 0.010 | 0.344 → **0.374** ± 0.010 | 0.348 → **0.386** ± 0.011 |
| 3 — +reranker, filter only | 0.609 → **0.641** | 0.685 → **0.776** | 0.496 → **0.519** | 0.522 → **0.546** |
| 3 — +reranker, query strip only | 0.609 → **0.619** | 0.685 → 0.687 | 0.496 → **0.502** | 0.522 → **0.530** |
| 3 — **+reranker, filter + query strip** | 0.609 → **0.736** ± 0.012 | 0.685 → **0.776** ± 0.010 | 0.496 → **0.635** ± 0.011 | 0.522 → **0.679** ± 0.012 |

Arm 3/4 rows above are unaffected by `RETR-24`: they came from the variant-aware
`day6_*` scripts. Consistency check after the fix — Arm 2's clean recall@50 (0.687)
now matches Arm 3's (0.685), as it must, since Arm 3 reranks Arm 2's own pool.

Pre-`GOLD-1` numbers (old shingle/numeric-overlap labeler) kept in `data/*.bak_old_labeler`
files alongside each rescored results JSON, not deleted, for before/after comparison.

## Infra

| # | decision | why |
|---|---|---|
| INFRA-1 | Store: Postgres + pgvector, not a dedicated vector DB (Pinecone/Weaviate/Qdrant) | Dedicated vector DBs win at hundreds-of-millions+ scale (sharding, ANN specialization). Our corpus is ~100k chunks — HNSW on one instance handles that. Also need hybrid search (dense + BM25 + relational metadata) in one SQL query, not 2-3 systems plus consistency glue |
| INFRA-2 | Docker image pinned to `pgvector/pgvector:0.8.6-pg18-trixie` | Checked current state (Aug 2026): Postgres 18 stable (supported to Nov 2030), 19 still beta, 0.8.6 is pgvector's actively-maintained release. Pinned the full tag (not just `pg18-trixie`) so pgvector can't silently drift |
| INFRA-3 | Compose volume mount: `pgdata:/var/lib/postgresql` (not `.../data`) | First attempt used the pre-pg18 path and crash-looped on startup. Postgres 18+ needs the volume one level up so it can manage major-version subdirectories for `pg_upgrade --link` |
| INFRA-4 | BM25 extension: `pg_search` (ParadeDB), not `tsvector`/`ts_rank`, `VectorChord-bm25`, or `pg_textsearch` | `ts_rank` isn't real BM25 (no document-length normalization or term saturation — degrades on long filings). `VectorChord-bm25` would mean re-platforming off our pinned image (`INFRA-2`); `pg_textsearch` ships no package yet. `pg_search` has a prebuilt `.deb` for our exact base image, depends on pgvector rather than competing with it, AGPL (free self-hosted). Index: `USING bm25 (id, text)`; query: `id @@@ paradedb.match(...)` (literal-text match, default OR-mode — matches real BM25 semantics of IDF-weighted overlap, not require-all-terms) |

## Dataset & eval harness

| # | decision | why |
|---|---|---|
| DATA-1 | Primary eval set: T²-RAGBench (`G4KMU/t2-ragbench`), not FinanceBench | FinanceBench: only 150 of 10,231 triples are public — too small to gate on (~4pt stderr at n=150). T²-RAGBench: 23,088 rows / 7,318 docs, fully open. FinanceBench kept as a secondary sanity check only |
| DATA-2 | ConvFinQA split 80/10/10 by document (`context_id`, seed 42), not by row | Ships with no split. Grouping by document (not row) stops the ~1.9 questions/doc from leaking one document across splits. Actual: 78.3/11.5/10.2 by row, zero cross-split docs |
| DATA-3 | TAT-DQA deferred from the eval set | Ships no CIK, only slugified company names — exact-match resolved only 79/173 companies (some are foreign 20-F/40-F filers, permanently out of scope, not a resolution failure). FinQA+ConvFinQA alone give full CIK-resolved coverage without it |
| DATA-4 | Corpus: real 10-Ks pulled live from EDGAR by `(cik, report_year)`, retrieval scored against full filings — not T²-RAGBench's shipped single-page contexts | The dataset ships one annotated page per question; scoring against that inflates recall@k (right page among a handful, not among hundreds). Full filings are the realistic task, but the tradeoff is our recall/nDCG **aren't directly comparable to the paper's own leaderboard** — the paper's own baseline scores <80% even on its easier per-question contexts. State this plainly in the writeup |
| DATA-5 | `edgar.py`: match a 10-K by `reportDate` (period of report) year, not a `filingDate` window | The old filingDate-window logic broke on non-calendar-fiscal-year filers (Apple, Nike, Sysco, ...) — two consecutive fiscal years' 10-Ks could both land in the window, and EDGAR lists filings most-recent-first, so the wrong fiscal year got picked. Audit found 26/100 filings affected (128/1,100 eval questions); re-fetched and re-chunked all 26 |
| DATA-6 | Corpus grown in stages: 100 → 300 → 400 → 603 → 799 filings, ending at full FinQA+ConvFinQA coverage (799 = exact count of unique `(cik, report_year)` pairs across both subsets, confirmed by direct count) | Each growth step invalidates prior Arm numbers by construction (more filings = more distractor chunks) — Arm 1-3 must be re-run unchanged after every corpus change. One growth (300→400) was an accidental second ingest run with a different seed that went unnoticed in CLAUDE.md/memory; caught only by checking the DB row count against the filesystem instead of trusting the doc. **Rule adopted: verify corpus size against the DB/filesystem, never against CLAUDE.md or memory, before trusting a number** |
| DATA-7 | Gold-relevance labeling v1: 5-gram shingle containment ≥0.3 vs whole-page `context`, not exact numeric match | Numeric match rejected (scaled `original_answer` often has no literal match). Threshold tuned 0.5→0.3 dropped zero-hit rate 28.5%→3%. **Superseded by `GOLD-1`** — whole-page matching itself had unfixed bugs |
| DATA-8 | Added numeric-literal overlap as a second signal, OR'd with shingling, threshold 0.5 | Fixed a confirmed undercounting bug (number repeated in two formats, e.g. `-17.1 ( 17.1 )`) on 2 cases; ~10 filings flagged as still affected, not fully rescued. **Superseded by `GOLD-1`** |
| DATA-9 | IDF-weighted overlap + minimum-evidence floor (`MIN_NUMERIC_EVIDENCE=3`) | Fixed two false-positive-flooding bugs (trivial 1-2-number denominator; decimal-only regex missed whole-integer tables). Zero-hit questions 2→0 on 300 dev rows. **Superseded by `GOLD-1`** — table/chunk-boundary splits remained uncaught, which is what motivated the rebuild |

## Chunking

| # | decision | why |
|---|---|---|
| CHUNK-1 | Parser: `sec-parser` (alphanome-ai, pinned `==0.58.1`), not Docling | Docling shatters prose into per-phrase fragments, leaks page-number/ToC noise into text, and mangles tables (eyeballed on AAL/JPM/ETR). `sec-parser` fixes the first two and parses ~3x faster (1.3s vs 4s/filing), but ships only 10-Q support — worked around via undocumented internals to reuse it for 10-Ks, so pinned exactly since those internals may break on upgrade. Known gap: `find_item_boundaries` returns nothing for filers using narrative headings instead of "Item N" (e.g. JPM) — accepted, not blocking. Fallback path if this ever breaks: Docling + a normalization pass (merge `<font>` runs, strip page/ToC noise) |
| CHUNK-2 | Chunk bounds 200/900/1500 tokens (min/target/max), token-counted via BGE-M3's tokenizer, not characters | Bounds come from T²-RAGBench's own evidence-span lengths (median 630 words, p90 990), not a generic RAG default. Token-counting matters because chars/token ratio measured at 4.1-4.5 for prose vs. 2.6-2.9 for tables — a character budget would silently over/under-pack depending on content mix. Tables are never split mid-row; an oversized table splits by row-group with the header row repeated in each group |
| CHUNK-3 | Two chunking-bug fixes found in the 100-filing dry run | (a) Filers lacking body-level "Item N" headings (`CHUNK-1`'s gap) produced single unstructured blocks up to 18k tokens — over BGE-M3's 8192 hard limit, i.e. actually unembeddable. Fixed with sentence-boundary packing + a raw-token-window fallback splitter. (b) `sec-parser` sometimes misclassifies a long bold paragraph (exhibit-index entries, signature blocks) as a heading — every title >100 tokens across all 100 filings was checked by hand (16 total, none genuine) and reclassified as body text. Empirical for this sample, not a proven rule; failure mode is soft (a real long heading would just lose grouping) |

## Arm 1 — dense retrieval

| # | decision | why |
|---|---|---|
| ARM1-1 | Embedding: BGE-M3 dense vector via `sentence-transformers`, not `FlagEmbedding` | `FlagEmbedding`'s `BGEM3FlagModel` also computes sparse + multi-vector (ColBERT-style) representations in one pass — not needed for a dense-only baseline. Both libraries wrap identical model weights, so the dense vector doesn't change if a later arm adds `FlagEmbedding` for sparse. 1024-dim, cosine distance, pgvector HNSW index |
| ARM1-2 | Retrieval: pure semantic search over the whole corpus, no `cik`+year metadata pre-filter | A pre-filter would raise recall by construction now that `DATA-5` makes that lookup reliable — but Arm 1 is the fixed baseline every later arm is compared against, and pre-filtering would change what's being measured, not just how well. Logged as a candidate variant for later, not blended into the baseline |
| ARM1-3 (open) | BGE-M3 uses single-`[CLS]` pooling (`sentence-transformers` default), not BAAI's MCLS long-document pooling (only exposed via `FlagEmbedding`, skipped per `ARM1-1`) | A chunk-length slice of Arm 1's results shows a real gradient — recall@10 0.50 (short chunks) vs 0.41 (medium) vs 0.26 (long) — consistent with a long-document pooling weakness. Correlational, not proof: long chunks overrepresent filers hit by `CHUNK-3`'s bug, and the labeling threshold wasn't separately validated per length bucket. Logged as a candidate to re-check with `FlagEmbedding` + MCLS before adopting |

## Arm 2 — hybrid (dense + BM25)

| # | decision | why |
|---|---|---|
| ARM2-1 | Fusion: RRF (Cormack et al., 2009), `k=60`, top-50 from each list | Cosine similarity and BM25 scores live on unrelated, incomparable scales — summing them directly would let whichever happens to produce bigger numbers dominate regardless of actual relevance. RRF fuses by rank instead |

## Arm 3 — reranker

| # | decision | why |
|---|---|---|
| ARM3-1 | Reranker: `bge-reranker-v2-m3` (local cross-encoder) over Arm 2's fused top-50, not Qwen3-Reranker-0.6B | Checked current state (Aug 2026): Qwen3-Reranker edges out on BEIR (~59 vs ~54) but is a causal yes/no-logit architecture — autoregressive decode per candidate. `bge-reranker-v2-m3` is a plain bidirectional cross-encoder: one forward pass, one score, and spec.md asks us to measure nDCG gain *against* latency cost, so the cleaner latency story won out over the leaderboard number. Cohere Rerank (hosted) deliberately deferred to conserve free-tier call budget |
| ARM3-2 | Reranking run offloaded to Northeastern's HPC (SLURM, V100) via a split-job pattern, not laptop CPU or a live SSH tunnel | Laptop CPU run swap-thrashed under normal load (~110s/query plateau, ~25h projected — not viable). A live tunnel from the GPU node to the laptop's Postgres was rejected: a dropped connection kills the whole run, same failure mode as the CPU run but now burning a SLURM allocation too. Split-job instead: laptop does the cheap retrieval + dumps a payload file; a self-contained, checkpointed GPU script reranks with no DB/network dependency; laptop scores the results after. File transfers must go through the `xfer` host — the interactive login node throttles/kills large transfers. **Stale as recorded (corrected 2026-09-04):** this row says `xfer.discovery.neu.edu` and the login host as Discovery, but the cluster is now **Explorer** — `login.explorer.northeastern.edu`. The `xfer` hostname is unchanged and still correct: `xfer.discovery.neu.edu` -> `xfer-00`, re-confirmed 2026-09-04. The full procedure now lives in `RUNBOOK.md`; this row keeps only the reasoning. **Node setup, recorded so it isn't re-derived each run:** `srun --partition=gpu --gres=gpu:v100-sxm2:1 --cpus-per-task=4 --mem=48G --time=08:00:00 --pty /bin/bash`, then `module load python/3.13.5` and `source ~/rerank-env/bin/activate` (torch 2.5.1+cu121, `torch.cuda.is_available()` True on the V100). The GPU scripts need only `sentence-transformers`, `torch`, `tqdm` — no `rag_sec`, no DB driver, by design. Run inside tmux: an `srun --pty` session dies with the SSH connection, and the jobs are hours long |
| ARM3-3 | Added cross-question batching to the HPC rerank script (20 questions' pairs per `predict()` call) | Only ~12% speedup (5.07s→4.45s) — GPU compute time, not Python call overhead, was the dominant cost, so batching couldn't buy the order-of-magnitude win hoped for. Adopted anyway since it's free |
| ARM3-4 (open) | Reranker introduces a same-company-wrong-fiscal-year failure mode not seen in Arms 1/2 | E.g. a JPM_2008 question's top hits are JPM_2009/2010 chunks; consecutive 10-Ks from the same filer share near-identical boilerplate, and a cross-encoder trained on general relevance has no inherent signal that the year token is decisive. Candidate fix: metadata-aware reranking or a `cik`+year filter — not yet built |

## Arm 4 — table indexing strategies (A/B/C)

| # | decision | why |
|---|---|---|
| ARM4-1 | Strategy B: one standalone chunk per table row (header repeated in each), bypassing the normal token packer | Tests whether row-level granularity beats retrieving the whole table. Standalone so a row never gets diluted by unrelated narrative sharing its chunk — the point of testing row granularity is lost otherwise |
| ARM4-2 | Strategy C: raw table chunk (identical serialization to Strategy A) + a separate standalone LLM-summary chunk, indexed *alongside* the raw table, not replacing it | Tests whether a paraphrased description helps recall beyond raw pipe-delimited text. Kept alongside rather than swapped in, so a bad summary can't regress below the Strategy-A baseline for that table |
| ARM4-3 | B/C scoped to only the 498 tables actually cited as gold evidence by dev questions (of 38,959 tables across 324 dev-relevant filings), identified via the same IDF-weighted shingle+numeric matching `eval.py` already uses (`DATA-7`–`DATA-9`), applied table-vs-table instead of question-vs-chunk | Embedding/summarizing all 38,959 tables costs compute and (for C) LLM calls for tables no dev question ever touches. Every non-target table stays Strategy A |
| ARM4-4 | Chunk-boundary drift bug (found in verification) + fix: `chunk_blocks_with_variant` builds plain Strategy-A chunks first, then splices a target table's atoms out of whichever chunk(s) they landed in, instead of re-running the packer with different atoms | Root cause: B/C's standalone atoms (row chunks, summary) forced the token-packer to close the current chunk early regardless of token budget, which shifted every downstream chunk boundary in that section — corrupting ~50% of filings with stray, unrelated re-chunked text that had nothing to do with any table. Splicing onto already-decided boundaries means only the one chunk containing the target table changes; verified byte-identical elsewhere on a full rebuild (281 filings) |
| ARM4-5 | Gold-relevance labeling: added a direct table-provenance match for Strategy C summary chunks (chunk text matches a cached summary for a table that's gold for this question), OR'd with the existing shingle/numeric checks | Existing labeling scores relevance by wording overlap against the dataset's gold text. An LLM-paraphrased summary routinely fails that check even when correct — measured ~19% of gold-table cases would be mislabeled "not relevant" purely from wording, understating Strategy C's real recall. Purely additive (only adds relevant labels), verified against a real case (sh_score 0.003, num_score 0.33, both below threshold, now correctly marked relevant) |
| ARM4-6 | Table summarizer: Claude Haiku 4.5, not Groq `openai/gpt-oss-120b` | A 50-table spot-check of the Groq summaries found a 22% comprehension error rate (wrong trend direction, column/label misattribution, unit confusion) plus a reasoning-token budget bug. Switched to Haiku with a checklist prompt targeting those failure classes + a forced show-your-work step. Re-audited 40 summaries post-switch: 7.5% error rate, all the same residual shape — picking the wrong endpoint for a multi-quarter/multi-year range or trend instead of the true min/max/direction, not fabrication or unit errors |
| ARM4-7 | Normalize dash/em-dash "zero" placeholder cells (e.g. `—`) before sending a table to the summarizer, not via prompt instruction alone | A prompt-only instruction to treat dashes as zero didn't reliably hold — the model still sometimes misread them as missing data. Fixed at the data layer instead of relying on the LLM to apply the rule consistently |
| ARM4-8 | Delta-only embedding: `build_table_variants.py` embeds/inserts only chunks whose text doesn't already exist as a Strategy-A row for that filing, not the whole filing per variant | Re-embedding a filing's full ~100-250 chunks for every variant would cost ~100x more than needed — B/C only ever add a handful of small new chunks (rows, summary) per filing. Runs locally on CPU in minutes; no HPC needed for this step (HPC is reserved for the full-corpus embed and Arm 3's reranking, both far larger jobs) |
| ARM4-9 (open) | 224/1235 dev questions have no matched gold table at all, excluded from the A/B/C comparison | Same shingle+numeric matching as `ARM4-3` found no table clearing the relevance threshold for these — likely a mix of real corpus gaps (per `DATA-5`/`DATA-8`) and questions whose evidence isn't in table form. Not chased further; logged as a known gap in eval coverage |
| ARM4-10 | Result: Strategy A beats B and C on every metric (baseline table above) — kept A, B/C not adopted | Splitting a table into rows (B) or adding a paraphrased summary (C) both regress recall@10 by ~0.15-0.19 vs. the whole table. B and C are close on recall/nDCG, but C's MRR (0.390) clears B's (0.291) despite lower recall — C offers only 2 candidate representations per table (raw + summary) vs. B's one-per-row, so B gets more chances to register a hit but each is a narrower target, while C's fewer candidates rank higher when they do hit |

## Arm 5 — late interaction / multi-vector (dropped)

| # | decision | why |
|---|---|---|
| ARM5-1 | Arm 5 (BGE-M3 late-interaction/multi-vector, spec.md Day 7) dropped entirely, not built even as a pilot | Measured against our real corpus before building anything: 99,654 chunks, avg 927 tokens/chunk (median 887). BGE-M3's multi-vector output is confirmed 1024-dim *per token*, no dimension reduction (unlike ColBERT's 128-dim projection) — checked current docs/HF card, Aug 2026. Storage: Arm 1 dense = 99,654 × 1024 × 4B ≈ 389MB; Arm 5 multi-vector = 99,654 × 927 × 1024 × 4B ≈ 378GB, a ~972x blowup, before even indexing it. Worse than storage: MaxSim retrieval needs an ANN index over ~92M token-vectors, not 99,654 chunk-vectors — pgvector/HNSW has no native MaxSim support, so this would mean building a custom PLAID-style index with no guaranteed quality win, since our chunks are already well inside BGE-M3's context window (median 887 tokens) — late interaction's main advantage (rescuing signal in very long documents) isn't clearly our bottleneck. Per spec.md's own framing, reporting this honestly is a stronger result than building it to prove the obvious |

## Recall ceiling audit (closes out labeling-vs-retrieval question)

| # | decision | why |
|---|---|---|
| AUDIT-1 | Audited all 401 zero-recall dev questions by hand (not a sample): 100% already had a labeler-confirmed relevant chunk, across every sub-pattern (wrong chunk, same-company-wrong-year, no matching company) | Ruled out the labeler *failing to find* the right chunk as the cause of the recall plateau. **Superseded by `GOLD-1`**: the labeler's own scoring formula had real bugs this self-consistency check couldn't see (only checks internal consistency, not whether whole-page matching was sound). 0.501 is no longer the recorded number |

## Gold-relevance labeling rebuild (`gold_inds`, not whole-page overlap)

| # | decision | why |
|---|---|---|
| GOLD-1 | Replaced the IDF-weighted shingle/numeric-overlap labeler (`DATA-7`-`DATA-9`, scored against T2-RAGBench's whole-page `context`) with a 3-layer design: (1) exact numeric match against a specific table row, (2) word-position alignment against a specific sentence, (3) word-position alignment against the whole page as fallback — using `gold_inds` from the *original* FinQA/ConvFinQA datasets (upstream of T2-RAGBench, which flattens `pre_text`/`post_text`/`table` back to strings and drops `gold_inds` entirely). Fetched + ID-matched by (ticker, year, page_number) + numeric-answer tiebreak: 95.1% of the full 11,739-row corpus resolved (FinQA ~98%, ConvFinQA ~87-90% — lower because ConvFinQA puts multiple full conversations on the same page, so the join key is ambiguous more often); the remaining 4.9% use Layer 3 only. `data/day7_gold_inds_matched_full.json` (raw match) -> `scripts/eval/resolve_gold_evidence.py` -> `data/day7_gold_evidence_resolved.json` (literal row cells / sentence text, so `eval.py` never touches the raw multi-hundred-MB dataset files at scoring time) | Auditing `AUDIT-1`'s "0 labeling misses" claim by hand (not just re-running the same formula) found the old approach's real bugs: a Table-of-Contents entry or repeated financial-statement header can share 10-20+ verbatim words with the true answer section purely from page boilerplate and score as relevant (`finqa_train_2408`/DISH: TOC entry for "Item 5" matched 43 chunks before the real Item 5 section); a paragraph split across our chunk boundary systematically under-scores the smaller half, since containment is computed against the *whole* gold page (`finqa_dev_11`/PKG: the chunk with the literal answer table scored 0.10 shingle / 0.0 numeric, both below threshold). Matching against one specific row or sentence instead of a whole page removes the ambiguity these came from, rather than patching each pattern individually — the explicit goal, not a side effect (spec.md's own framing: a whole page is inherently loose ground truth once two independent parsers are involved, `DATA-4`) |
| GOLD-2 | Tokenizer fixes needed before Layer 1 numeric matching was usable: (a) collapse comma thousands-separators before splitting words (`6,951` -> one token `6951`, not `6`+`951`); (b) keep decimals as one token (`384.3`, not `384`+`3`) | Our chunk serialization renders `$6,951`; the source renders `6951` — without (a), the exact same figure produces a different token count on each side and never matches. Found while confirming (b) doesn't already exist: `[a-z0-9]+` splits on the decimal point too, both fragmenting a real figure into two weaker signals *and* creating a generic, noisy `3`-style token as a side effect |
| GOLD-3 | Layer 1's "distinctive number" filter needed three exclusions, not one, found by running the rebuilt labeler over the full dev set and inspecting every case with an unexpectedly large relevant-chunk count before locking any values: numbers under 3 digits, years (1900-2099), and — the one IDF weighting used to catch in the old design — any number recurring in more than `ROW_NUMBER_MAX_DOC_FREQ=5` of the *same filing's* own chunks | Each was a real, confirmed false-positive case, not a hypothetical: a bare `4` (from a `(4)` footnote marker) matched an unrelated debt-rating paragraph (`finqa_dev_17`/MSI); a row's only other "distinctive" number was `2003`, the filing's own fiscal year, which matched 130/401 of that filing's chunks (`finqa_dev_321`/AES); a `100%` column-total matched 21 chunks across unrelated percentage tables in the same filing (`finqa_dev_126`). Committing to "no more IDF weighting" (`GOLD-1`'s stated goal) still left this one narrow case where *some* document-frequency check was unavoidable — full IDF re-weighting wasn't needed, just a hard cutoff, since Layer 1 only ever compares within one filing's own chunks, not across the corpus |
| GOLD-4 | Calibrated `MIN_BLOCK_PAGE=10`, `MIN_BLOCK_SENTENCE=6`, `MAX_CLUSTER_GAP=3`, `CLUSTER_SHARE_THRESHOLD=0.15`, `ROW_NUMBER_MATCH_THRESHOLD=0.6` against the full 1235-row dev set (not a small held-out sample — every row was checked automatically via the zero-hit-rate and largest-relevant-set metrics, with manual inspection of every outlier), not picked upfront | Result: 0% zero-hit rate (every dev question has >=1 relevant chunk under the new labeler — down from checking against `AUDIT-1`'s definition), median relevant-chunks-per-question 1, max 6 (down from an uncalibrated first pass's max of 130, per `GOLD-3`). All 7 named regression cases (5 recovered failures, 2 rejected false positives) verified individually after locking these values, not just in aggregate |
| GOLD-5 | Rescored every existing arm's already-computed retrieval output against the new labels — Arm 3/4 offline from saved reranked orderings (`data/day6_arm4_*_rerank_scores.jsonl`, `data/rerank_scores.jsonl`, no HPC/reranking re-run), Arm 1/2 via a local retrieval re-run (dense+BM25/RRF only, no reranking, no HPC — the ranking logic itself is unchanged, only relevance labels differ) | Every arm's recall improved substantially (see baseline table) — e.g. Arm 3/4-A recall@10 0.501 -> 0.609 — confirming the old labeler was genuinely under-counting relevant chunks, not that today's retrieval was secretly better than measured. Arm 4's qualitative finding (`ARM4-10`: A beats B and C) is unchanged under the corrected labels — B and C's recall@10 (0.350/0.352) are within noise of their old numbers (0.356/0.309), so that comparison wasn't distorted by the old labeler's bugs, only the absolute ceiling was |
| GOLD-6 (open) | ConvFinQA's `gold_inds` match is exact for only 15% of matched rows (`Double_*` dialogues, which record real per-turn evidence) — the other 85% (`Single_*` dialogues) borrow the *final* conversation turn's `gold_inds` as a proxy for our first-turn-only question, since ConvFinQA's raw annotation only records evidence on the last turn. Not chased further: the proxy is a superset (final-turn evidence is very likely a superset of first-turn evidence for short chains) and Layer 3 fallback still applies if a chunk match ends up empty. Also open: the 4.9% of the corpus (mostly ambiguous ConvFinQA joins, `DATA-2`) that never resolved a `gold_inds` match at all use Layer 3 (whole-page) only — same approach as before this rebuild for that slice |
| GOLD-7 | The ticker/year/page/numeric-tiebreak join that produced `data/day7_gold_inds_matched_full.json` was run ad hoc against manually-fetched raw FinQA/ConvFinQA files and never saved as a script — only the downstream resolver (`resolve_gold_evidence.py`) is committed | Shortcut, logged per rule 5: `data/day7_gold_inds_matched_full.json` is committed as data (not regeneratable from anything in-repo) rather than reproducible from a script. Re-deriving it would mean rewriting the join logic from scratch |

## Arm 6 — agentic loop (LangGraph), model choice

| # | decision | why |
|---|---|---|
| AGENT-1 | `plan`: Gemini 3.7 Flash, `thinking_level="low"`. `judge`: Gemini 3.1 Flash-Lite, `thinking_level="minimal"` | 3.7 Flash only accepts low/medium/high — minimal 400s. Dropped Groq `gpt-oss-20b` and a Qwen3 model (collapse on cross-entity reasoning in Fin-RATE); dropped GPT-5.6 Luna (scores below its own siblings on finance). 3.7 Flash targets agentic decomposition (fits `plan`); Flash-Lite is the cheap tier for `judge`'s one-word verdict **`judge`'s `thinking_level="minimal"` is a no-op (`COST-31`); the call still passes it, harmlessly.** |
| AGENT-2 (open) | If `plan` (only) shows real failures: swap `plan` to Claude Haiku 4.5, keep `judge` on Flash-Lite | Haiku's case rests on thinking-off-by-default cost predictability, not a proven finance-accuracy edge. Evidence-driven escalation only |
| AGENT-3 | Measured cost: ~$0.014/question (3-question sample, all single-iteration) — `plan`+`answer` (3.7 Flash) is ~78% of spend, `judge` (Flash-Lite) ~22% | Corrects AGENT-1's ~$3/dev-pass guess, which was made before any real run. Scaled to 1235 questions: ~$17 if no question loops; real number depends on loop frequency, not yet measured **Suspect per `COST-24` (thinking tokens) and superseded in shape by `OBS-10`; do not quote.** |
| AGENT-4 | Graph: `plan` -> `retrieve` -> `judge` -> loop to `plan` or finish to `answer`, capped at 4 iterations regardless of verdict. Postgres-checkpointed (`langgraph-checkpoint-postgres`), `retrieve()` reused from Arm 3 (`src/rag_sec/retrieve.py`) | `judge` kept separate from `plan` so the same model doesn't grade its own retrieval. Iteration cap bounds worst-case cost per question |
| AGENT-5 | Thread ID: stable (`question_id`) for the real eval run; a run-nonce suffix for manual smoke tests only | Postgres checkpointing resumes by thread ID — reusing a bare question ID across repeated smoke-test runs after a code fix silently resumed stale, contamination from earlier crashed attempts (doubled call counts, corrupted trajectories). Real eval run needs the stable form so a killed full pass can actually resume **Wrong as written — see `AGENT-11`. The stable thread id buys per-question inspectability, not resume; resume is file-level.** |
| AGENT-6 | Evidence sent to `judge`/`answer` deduped by `(filing_stem, chunk_index)` before formatting | Loop iterations often re-retrieve the same chunk; duplicates were billed as input tokens for zero new information |
| AGENT-7 | Kept `retrieve()`'s `k=10` (chunks returned per call), didn't lower to save tokens | Checked recall@3/5/10 against the real, already-scored full 1235-question dev set (Arm 4-A rerank orderings): 0.478 / 0.537 / 0.609. Lowering `k` would cut token cost but cost 7-13 recall points — not worth it against Day 8's whole premise of improving recall over one-shot retrieval |
| AGENT-8 | Track `cached_input_tokens` per LLM call (`usage_metadata.input_token_details.cache_read`) | Gemini's automatic prompt-prefix caching applies to both models with no code change (confirmed: 3.1 Flash-Lite cached input $0.025/1M vs $0.25 normal; 3.7 Flash $0.075/1M vs $0.75, both ~90% off). `plan`'s growing message history and `judge`'s growing evidence block are both literal shared prefixes across loop iterations, so this should fire from iteration 2+ on any question that loops — not yet confirmed on a real multi-iteration run **Confirmed by `AGENT-14` — but never quote a single cache share; it swings with the iteration mix.** |
## Day 8 cost reduction — evidence compression

| ID | Decision | Why |
|---|---|---|
| COST-1 (input/output split valid; the ~4% thinking claim revised by `COST-34`, dollar figures suspect per `COST-24`) | Cost teardown from real per-node token counts (3-question sample): `answer` input 66.6%, `judge` input 22.2%, `answer` output 6.6%, `plan` output 3.7%, `plan` input 0.9%. $0.0137/question, ~$16.97/dev pass | **89% of spend is one ~12,200-token evidence block sent twice** — to `judge`, then to `answer`. Output tokens are 10% total, so thinking-level tuning is worth ~4% and is not the lever. Supersedes AGENT-3's 78/22 split, which attributed cost by node rather than by input-vs-output |
| COST-2 | Prompt caching will not reduce the baseline; do not design around it | Cache discounts an identical *prefix*. Evidence is unique per question (nothing repeats), and `judge`/`answer` run on different models so no cache crosses them — it can only ever fire on loop iterations 2+ of the same question, and no question has ever looped. That reason alone is decisive. Also verified: implicit caching needs a 4,096-token minimum (our shared system prompt is far below it), and the ~9k–17k zero-cache bug is real and still open with no fix (googleapis/python-genai#2064) — our prompts sit inside it at 11.0k/11.4k/14.2k. **Correction (2026-09-01):** an earlier version of this row also claimed `tools` disable caching (vercel/ai#11513). That claim does not hold — the issue was closed as not-planned, and the reporter's prompts were 1.0k–1.6k, below the caching floor, which explains the zero hits without any tools hypothesis. No Google source supports it. Qualifies AGENT-8 |
| COST-3 | Gold evidence (`gold_inds`) is 38 tokens mean / 27 median across 1,177 dev questions, against ~12,200 sent | The over-send that motivates compression. Not the achievable floor — see COST-6 |
| COST-4 | Corpus interpretability ceiling: 7.0% of the dev set has gold in a chunk with a table but no scale qualifier *and* an answer needing absolute magnitude. Logged, not fixed | 34.1% of table-row questions lack an in-chunk scale qualifier, but units cancel for ratios/percentages, so only 7.0% are genuinely at risk. Fixing it means re-chunking (never split a caption from its table), which changes chunk text and invalidates every embedding, the BM25 index, and Arms 1–4. Not worth it for 7% — revisit only if it becomes the binding constraint. `scripts/analysis/chunk_interpretability.py` |
| COST-5 | Compression selects over the chunker's own `Atom`s, replayed from `data/parsed/` — not over a regex re-derivation of chunk text | `_pack_atoms` already returns each chunk paired with the atoms it was packed from; `chunk_blocks` discarded them. Exposed as `chunk_blocks_with_atoms`. Verified exact: **99,654/99,654 chunks replay byte-identically** (`scripts/checks/atom_replay.py`), so embeddings/index/scored arms are untouched. This deleted a ~250-line regex splitter (table detection, pipe heuristics, sentence fallback, NBSP handling) that was being built to recover structure the pipeline had never actually lost |
| COST-6 | **Selecting whole atoms is k-reduction relabeled — rejected.** A gold-bearing atom averages 684 tokens, 76% of its chunk, so a budget holds ~3 and the result is effectively recall@2 | Measured recall@1/2/3/5/10 on Arm 3 = 0.318/0.422/0.477/0.534/0.609. A 1,500–2,000 token budget of whole atoms is recall@2 territory: a 31% relative loss of gold. Same failure AGENT-7 already recorded for lowering `k`. The binding constraint is ranking quality, not granularity |
| COST-7 | Slice atoms to a token target instead: prose on sentence boundaries, tables into row-groups with the **full preamble repeated** in each | Changes the trade from "fewer chunks" to "thinner slices of more chunks" — a 2,000-token budget holds two 900-token chunks, or eight 250-token slices spanning eight different chunks. Ceiling (perfect scorer): gold footprint 684 -> 281 tokens, **96.8% of questions' gold fits in 1,000 tokens** vs 94.0% needing 2,000 for whole atoms. `scripts/analysis/slice_floor.py` |
| COST-8 | Table preamble = every row above the first row with a worded first cell *and* a non-year figure of 3+ digits; repeated into every row-group | Repeating only the first line stamps `Years ended` on each group and drops the actual column dates (AAPL_2012 income statement). Testing whole cells for digits instead of the number tokens inside them makes `September 24, 2011` read as a data row — same distinction eval.py draws at `MIN_ROW_NUMBER_DIGITS`/`_YEAR_TOKEN_RE`. Guard is a 30%-of-tokens cap, not a row count: one real table put 18 of 22 rows above its first figure, while a genuine balance sheet legitimately stacks 6 (date, years, two-line units note, `ASSETS`) |
| COST-9 | Where a header can't be identified, keep the table whole rather than slice it | Failing to slice costs compression; fabricating a header boundary puts numbers under unlabelled columns in every slice. Residual: 16.7% of >400-token tables unsliced, but only **3.8% of gold-bearing** ones — the rest are exhibit indexes (`4.6 \| Pass Through Trust Agreement...`) and footnote lists, which never carry gold. There is no ground truth for where a header ends; this stays heuristic by nature |
| COST-10 | `compress.py` deliberately duplicates `chunking._split_table`/`_split_text` rather than reusing them | Those run inside `_blocks_to_atoms` at ingest — parameterizing them changes chunk text and invalidates the corpus. The slicer also needs a sentence regex the ingest one can't have: `(?<=[.!?])(?:\s+\|(?=[A-Z]))`, because the HTML->text conversion drops the space in 31.5% of prose atoms (`patterns.Working`). Duplication is the price of a frozen corpus |
| COST-11 (magnitudes superseded — `COST-18`; ranking survives) | **Measured (1235 dev questions, HPC V100, 550,121 slice pairs).** Gold survival vs token budget, `chunks` / `slices` / `slices50`: @500 9.3/58.8/53.8, @1000 54.6/66.9/61.8, @1500 64.4/70.1/65.8, @2000 69.9/73.3/69.0, @3000 74.7/76.4/71.3. Uncompressed control: 85.5% @ 10,056 tok | Three findings. (1) **Slices beat whole chunks at every budget**, by most where it matters: +12.3 points @1000, +49.5 @500 (a 900-token chunk cannot fit in a 500 budget at all — `chunks` admits a mean of 84 tokens). Confirms COST-6/COST-7 empirically rather than by ceiling argument. (2) **`slices50` is worse than `slices` everywhere — the recall play is dead.** Widening from the top-10 to all 50 fused candidates lets the reranker promote noise from low-ranked chunks over real evidence; more candidates makes a ranking-quality problem worse, not better. Consistent with recall@1 = 0.318. Arm dropped. (3) **The gap splits in two, and only half of it is the reranker.** COST-7's 96.8% ceiling was a *packing* measure ("would the gold fit in 1,000 tokens if it were retrieved and picked perfectly"), so it was never reachable here. The reachable ceiling is the uncompressed control on the same top-10 pool: 85.5%. So 96.8 -> 85.5 (11.3 pts) is retrieval failing to put gold in the top 10, which compression cannot fix; 85.5 -> 66.9 (**18.6 pts**) is the reranker not picking gold it was shown. Only the second is a selection problem. Compression is a genuine accuracy-for-cost trade, not free money: ~19 points of survival buys a 10x token cut ($16.97 -> ~$3.10/pass — **both figures predate `COST-24` and likely understate output cost**); @3000 costs ~9 points for ~$5.70/pass. Note the survival matcher normalizes by *gold* length (`CLUSTER_SHARE_THRESHOLD`, `eval.py`), so the 1,000- and 10,000-token prompts are not mechanically advantaged against each other; if anything the longer control accumulates more coincidental matches, making 18.6 an upper bound |
| COST-12 (decided against — `COST-30`) | **Merge `judge` into `answer` (one send, not two) rather than just deleting `judge`.** Use Batch API for offline passes, not `service_tier="flex"` | Modelled on COST-1's own token counts: dropping `judge` saves 22%, but merging saves the same 22% *and* keeps a sufficiency signal — the saving is the second send, not the model. **Two corrections from a 2026-09-01 research pass.** (a) *Flex is unreachable* — **WRONG, superseded by `COST-19`; flex is GA for Gemini via the raw SDK, only the LangChain path is blocked**: `langchain-google-genai>=4.4.0` does not exist (PyPI latest 4.3.7, 2026-08-27; LangChain's docs cite an unreleased version; PR langchain-ai/langchain-google#1937 unmerged since April). Batch API gives the identical 50% and ships today, via the raw `google-genai` SDK — async, 24h SLO, and a 3M enqueued-token cap means a ~30M-token pass needs ~10 submissions. (b) *The proposed replacement signal is not supported*: post-retrieval score features predict answer quality at Spearman ρ≈0.19–0.22 (arXiv:2601.14546), and CRAG's own thresholds span 0.50–0.95 across four datasets with no transferable constant. A calibrated reranker threshold is not a viable judge. If a real sufficiency signal is ever needed, the evidenced route is a small local verifier (SURE-RAG 0.9075 macro-F1 vs GPT-4o's 0.7284), trained in-domain on `gold_inds` — but sequence that after RETR-7 |
| COST-13 (answered — `COST-23`) | Gold survival is not answer accuracy — not yet measured. **Deferred behind the RETR work**, on evidence that retrieval, not the reader, is the binding constraint | The model may answer correctly from partial evidence, or fail with full evidence. Whether 19 points of survival costs 19 points of accuracy or nearly nothing is what should pick the budget. Priced properly: **$1.49**, not "$1" — 100 questions × 3 arms (uncompressed / slices@1000 / slices@3000), answer call only, at COST-16's prices; $2.90 if outputs run to 1,500 tokens, $0.74 via Batch. The cost risk is the *follow-up*: n=100 detects a 19-point drop but cannot certify a small one, and n=400 is $5.95. Design: same questions across arms compared pairwise (McNemar), scored by Number Match not string equality. Why deferred — T²-RAGBench's own gap is ~32 points oracle-vs-retrieval against ~18 points between a 7B and a 32B reader, and our own triage (RETR-1) puts 19.6% of questions outside the candidate pool entirely |
| COST-14 | Pipeline: `slice_prepare.py` (laptop, reuses day6_arm4_A's payload so all arms share a byte-identical 50-candidate pool) -> `slice_rerank_hpc.py` (HPC, self-contained, checkpointed) -> `slice_budget_sweep.py` (laptop, sweeps budgets). Slice target travels in the payload filename, is stamped into every scores record, and stage 3 refuses a file with a missing or inconsistent target | Same three-stage split as ARM3-2/ARM4. Slices travel as `(atom_index, piece_index)` positions rather than text, so the round-trip carries one copy of the corpus (266 MB) instead of two — but that makes stage 3's slicing target load-bearing: at any other target every position silently resolves to different text. Hence the explicit guard rather than a shared default. All 61,750 candidates replayed byte-identically from `data/parsed/` during payload build, so the slices are provably of the documents retrieval actually returned |
| COST-15 | The `judge` premise was overstated: "sufficient 3/3" is not proof the judge is broken | Sufficiency in the literature is *answer-free* ("does some plausible answer follow"), a weaker condition than "contains the `gold_inds` chunk", so 3/3 does not contradict recall@10 = 0.609. Prompted LLM autoraters reach 93% acc / 0.935 F1 on this task (Sufficient Context, ICLR 2025). The real reason to distrust ours: prompt-based abstention holds above 97% when context is *missing* but collapses to 13.6–74.3% answer rates under *misleading but fluent* context (arXiv:2608.22228) — which is exactly what related-but-wrong 10-K prose is. Still delete it, as a cost decision, not a correctness one |
| COST-16 | Model/price facts to design against (verified 2026-08-31, ai.google.dev) | `gemini-3.7-flash` $0.75/$3.75 per 1M in/out and `gemini-3.1-flash-lite` $0.25/$1.50 — independently confirmed by back-solving COST-1's measured shares, which reproduce $16.97 exactly. Two deadlines: 3.7 Flash's price is **introductory through 2026-12-31 and doubles to $1.50/$7.50 on 2027-01-01** (same baseline becomes $29.39, +73%), and `3.1-flash-lite` shuts down 2027-05-07 with a *more expensive* mandated successor (3.5-flash-lite, $0.30/$2.50). `gemini-2.5-flash-lite` at $0.10/$0.40 is the cheap tier worth moving to. This is the argument for compression over tier discounts: compression is the only lever that survives the price change **The `3.1-flash-lite` shutdown date is unsourced (`COST-19`) — re-verify before quoting it or the migration advice.** |
| COST-17 | **COST-11's compression table was measured on the wrong retrieval ordering; recomputed against `RETR-29` (`filtered_stripped`), 1235 dev.** Whole-chunk arm, old -> new: FULL control **85.5 -> 93.3%**, @500 9.3 -> 15.8, @1000 54.6 -> **70.0**, @1500 64.4 -> 80.2, @2000 69.9 -> 84.6, @3000 74.7 -> **88.3** | Free to recompute: `full`/`chunks` depend only on chunk ordering, so no GPU. Old ordering re-run through the same refactored script reproduces COST-11 to the decimal, so the delta is the ordering, not the code. Three consequences. (1) **The reachable ceiling is now 93.3%**, so COST-7's 96.8% packing ceiling is only **3.5 pts** away — the "retrieval never surfaced the gold" share of the gap collapses from 11.3 to 3.5. (2) **COST-11's headline no longer holds as stated.** "Slices beat chunks at every budget" was measured against a `chunks` arm crippled by the bad ordering; new `chunks` @1000 (70.0) already beats old `slices` @1000 (66.9). Slicing is not disproven — it is unmeasured on the correct pool, and its complexity is unjustified until re-run. (3) **@500 remains structurally slices-only**: chunks reach 15.8% because a ~900-token chunk cannot fit a 500 budget at any ordering. Slice arms could not be recomputed — old slice scores cover only **77.8%** of the new top-10 chunks and were scored against raw (unstripped) queries, the exact condition RETR-29 rules out. Needs the GPU booking |

## Day 8 retrieval diagnosis — where recall is actually lost

Triage, then 100 hand-read failures, then an A/B. Ordered so each row is measured before
the next is proposed. All read-only: no re-embedding, no re-index, no LLM spend.

| ID | Decision | Why |
|---|---|---|
| RETR-1 | **Every dev question sorted into one bucket** (`scripts/analysis/failure_triage.py`, n=1235): gold reached top-10 **73.5%**; gold in the 50 but not top-10 (**reranker's fault**) **6.9%**; gold chunk exists in the filing but never entered the 50 (**candidate generation's fault**) **19.6%**; no chunk in the filing holds the gold (**chunking's fault**) **0%** | Reframes the whole optimization. The reranker only reorders the 50 it is handed, so its ceiling is recall@50 — it converts 0.685 into 0.609, **89% efficiency, 7.6 points of headroom**. Candidate generation leaves 31.5% unreachable, ~4x the prize. Spend upstream, not on the reranker. Caveat on the 0%: the matcher's third layer is lenient whole-page alignment, so this rules out the catastrophic version of the chunking hypothesis (gold absent entirely), not the subtle one (caption separated from table). Note these are "any gold chunk" rates; the baseline table's recall@k is the fractional metric, hence 73.5% vs 0.609 |
| RETR-2 | Widening `TOP_K` 10 -> 20 recovers 4.3 points of the reranker bucket (15 -> 2.2, 25 -> 5.4, 50 -> 6.9); gold sits at median rank 18 when it misses | A one-constant change, but it doubles the evidence sent to the LLM and runs straight into COST-1. Only free once compression ships, because the budget then caps tokens regardless of `k`. Sequence it after COST-11, not before |
| RETR-3 | **The dominant failure is document selection, not chunk ranking.** Across 100 hand-read failures (300 retrieved chunks, 5 parallel analyses): **26% came from the correct filing, 50% from the right company's wrong year, 24% from a different company** | 10-Ks reprint themselves near-verbatim each year, so sibling years are equally good lexical and dense matches and RRF has no tiebreaker. Canonical case: UNP prints "in 20XX, we expect our total capital investments to be approximately $N billion" every year under the same heading — four filings match every content word of the question equally, and the year token carries no weight in either retriever. Nothing is wrong with those gold chunks |
| RETR-4 | **The entity wording in the question is a filter, not a search term.** 19-20 of every 20 questions name the company outright; the gold chunks say "the Company" | The questions are rewritten FinQA ("…for Philip Morris International, as reflected in the company's 2017 financial report"), so the company name and filing framing are the strongest signal reaching both retrievers — and they match every year of that issuer, and every issuer's boilerplate, equally. The system is being handed the document identity and throwing it away, then being misled by it. This one observation drives RETR-5 and RETR-6 |
| RETR-5 | **Company filter, resolved from question text only: recall@50 80.1% -> 87.7% (+7.6 pts), 96 questions recovered, 2 lost** (`scripts/analysis/company_filter_ab.py`, n=1235) | Lexicon built from the corpus's own `company_name` column (136 names / 137 tickers, ambiguous names dropped); matching reads only the question string, never the answer key — hard-filtering to the gold filing would be exactly the inflation `DATA-4` refused. Resolver (final, `RETR-11`) on the **untouched test split: 94.2% coverage, 99.8% precision, 0.19% of questions lose the gold**; dev 94.8%/100.0%/0.00%, train 94.2%/99.6%/0.33%. Quote the test number, not dev — the multi-ticker-fragment guard was found by reading dev failures, so dev's 100% is optimistic. Consistency across three splits, one never inspected, is the evidence the rules generalize. Fixes 37% of RETR-1's candidate-generation bucket for no re-embedding and no GPU. **Year is deliberately not filtered**: the year named is often the *data* year, not the filing year, and a 10-K carries 3-5 years of columns — one analysis found max-year-mentioned picks the right filing in only 13/20. Hard company filter, soft year boost |
| RETR-6 (open) | **Strip company/filing framing from the query before reranking**, while leaving the full question for candidate generation and for the generator | Measured on 20 rerank-miss cases by re-running `bge-reranker-v2-m3` locally: with the question as-is, gold loses to all three distractors **20/20**; with the framing stripped, gold beats all three **10/20**, and distractor scores collapse from 0.97-0.99 to 0.01-0.06. Those chunks were scoring on the company name, not the question — the reranker preferred UNP's CEO shareholder letter (0.99) over the accounts-payable table holding the answer (0.90). The framing helps pick the document and is pure noise at rerank time, where all 50 candidates already share it. Directional only: compares gold against 3 distractors, not all 50. Needs one HPC rerank re-run to confirm; no re-embedding |
| RETR-7 (diagnosis; fix built, see `RETR-7 (built, dark)` below) | **Heading bug in `chunking._pack_atoms`: 46.7% of chunks carry a heading set *after* some of their own body text** (2,683 / 5,748 over 40 sampled filings) | A title atom only flushes the buffer when `current_tokens >= TARGET_CHUNK_TOKENS`; below that it just overwrites `current_heading`, so a short section's text is emitted under the *next* section's heading. Observed: a Zimmer balance sheet headed `# CREDIT RISK`, a comprehensive-income statement headed `# Consolidated Balance Sheets`, a factory-count table headed `# Performance Graph`. The heading is a signal we deliberately inject to help retrieval and it is currently misleading on nearly half the corpus. A correctness bug regardless of recall impact — but fixing it changes chunk text, so it invalidates every embedding, the BM25 index and Arms 1-4. **Batch it with RETR-8 into one re-index cycle; do RETR-5/6 first since they need none** |
| RETR-8 (diagnosis; fix built, see `RETR-8 (built, dark)` below) | Also queued for that same cycle: strip page furniture and filer-agent junk from the heading slot | Observed headings include `# F-58`, `# PART II`, `# /s/ KPMG LLP`, `# 102013 PPG ANNUAL REPORT AND FORM 10-K`, and pre-2008 EDGAR typesetter metadata (`# ZEQ.=3,SEQ=66,EFW="2112402",CP="ABIOMED,INC."...`). In one case the running-header chunk beat the real answer because the question said "according to the 2013 PPG Annual Report and Form 10-K" — page furniture matching query furniture. Cheap to strip in `parsing.py`; only worth a re-index alongside RETR-7 |
| RETR-9 | **~15% of the candidate-miss bucket looks mislabeled** — measure before optimizing against 19.6% | All four analyses independently found cases where the retrieved chunk answers the question and the gold label points elsewhere: `convfinqa_2530` has empty `gold_table_rows` *and* `gold_sentences`; `convfinqa_1124`'s rank-2 chunk carries the identical table from the adjacent year; `finqa_dev_840`'s labeled gold is cross-reference boilerplate while retrieval returned the actual stock-compensation note. Conversely the reranker bucket is **not** noise: 19/20 are real failures, with 49 of 57 distractor slots containing none of the gold numbers |
| RETR-10 | Slicing hurts the cross-encoder — independent confirmation of COST-11's dropped `slices50` arm | Scoring a table-only slice of each gold chunk instead of the whole chunk made the score *worse* in 12/20 cases and better in 6. Stripping surrounding prose removes the context the cross-encoder scores on. Two different methods (COST-11's budget sweep, this probe) reach the same conclusion, so it is a property of the reranker, not an artifact of either measurement |
| RETR-11 | Company resolver (`src/rag_sec/company.py`) is built precision-first: **a wrong filter deletes the gold, a failed resolution only costs the speed-up**, so every guard resolves ties toward "don't filter" | Aliases per company: full legal name, name minus legal suffixes, and the leading token (which is also what catches a question naming a *subsidiary* — "Entergy Mississippi, Inc." files under ETR). Normalization folds possessives (straight and curly), ampersands, and embedded punctuation (`E*TRADE`, `Macy's`). Guards, each for a specific observed collision: **(a)** a single-token alias that is an ordinary English word (`/usr/share/dict/words`) needs the question to capitalize it *away from a sentence start* — `Visa Inc.` resolves, `visa applications` and `Apple prices rose 12%` do not; **(b)** tickers match uppercase runs in the original text, lowercase only at 3+ chars, never when the symbol is an English word — 5 of 137 tickers are single characters (`A`, `C`, `F`, `T`, `V`) and others are `CAT`/`GAP`/`ALL`; **(c)** share-class suffixes are tried both ways (`BRK.B` -> `BRK`); **(d)** aliases match on word boundaries, so "aeon" never fires inside "bacon". **Ambiguity is searched, not guessed**: an alias mapping to several tickers (Under Armour -> UA + UAA, a real dual-class case) returns all of them, as does a question naming two issuers — so mentioning an extra company cannot delete the gold. Above 3 tickers the match is treated as spurious (peer-group and performance-graph questions list index members) and the filter is dropped. Checked by `scripts/checks/company_resolver.py`: 27 hand-written cases plus per-split precision |
| RETR-12 | Residual resolver failure mode, accepted and instrumented rather than fixed: **the question names an acquired business or a counterparty instead of the filer** | The only pattern behind train's 18 misses (0.20%): "the $94.1 million purchase price for the **Packaging** Acquisition" resolves to PKG when the filer is WRK; "the FIS Gaming Business" resolves to FIS when the filer is GPN. Zero such cases in dev. Not fixable from the question text — the named entity genuinely is a company, just not this one. `retrieve(reserve=N)` keeps N candidate slots unfiltered as insurance; defaults to 0 because at 0.20%/0.00% the reserve costs more recall than it buys. Revisit if a future split shows this rising |
| RETR-13 | Resolver contains **zero company names in its logic** — verified by AST, every string literal in executable code is a regex, a path, a dataframe column or a separator | The generalization argument, and it has to be checkable rather than asserted: names appear only in docstrings explaining why a rule exists. Strip every comment and behaviour is identical. Rules are structural — is the alias an English word (system wordlist, not a hand-written list), is it capitalized away from a sentence start, does a single-token alias map to several companies, is it <=3 chars, is the ticker uppercase in the original. A corpus grown to 5,000 filings rebuilds the lexicon from metadata and the rules still hold |
| RETR-14 | Spurious extra tickers are **dilution, not deletion**, and are left alone: 3.30% of test questions resolve the right company plus a wrong one | "the locations at 2 Discovery Drive" in an Aon question resolves `['AON', 'DISCA']` — the gold company is still searched, Discovery's filings just come along and cost some candidate slots. Tightening this means more rules justified by fewer cases, which is how a resolver starts memorizing one split. Left as is deliberately |
| RETR-15 | **Only 37% of the recall the filter buys reaches the top 10 before reranking** — Arm 2 gains +8.3 pts recall@50 but +3.1 recall@10 | Two thirds of newly-found gold lands in ranks 11-50, so RRF's own ordering doesn't surface it. This *promotes* `RETR-6` from side experiment to the deciding one: the filter fills the candidate pool, the reranker has to do the rest. Also moves the reranker's ceiling from recall@50 0.685 to **0.767**. At the 89% rerank efficiency measured in `RETR-1`, Arm 3 would land near 0.683 against today's 0.609 — a **projection from an unchanged-efficiency assumption, not a measurement**, and the 37% pass-through is itself a reason to doubt efficiency holds, since the pool is now denser with same-company near-duplicates. Secondary finding: the filter helps dense-only far more than hybrid (+12.6 vs +8.3 recall@50), because a 900-token chunk's single vector barely encodes a year token while BM25 was already matching company and year literally — which is part of why Arm 2 beat Arm 1 in the first place |
| RETR-16 (open) | One HPC rerank pass covering **three new cells of a 2x2** (candidates: unfiltered / company-filtered) x (query: raw / entity-framing stripped). Unfiltered+raw already exists as `day6_arm4_A_rerank_scores.jsonl` | Both `RETR-5` and `RETR-6` need a cross-encoder pass over the same 50 candidates — only the query text differs — so running two jobs would waste a GPU booking. The fourth cell is what makes the gain attributable: without it, a better number can't be assigned to the filter or to the query cleanup. ~185,250 pairs, roughly a third of `COST-14`'s slice job on the same hardware |
| RETR-17 | Arms 1/2 keep `--company-filter` **off by default** and write `*_companyfilter.json` sidecars rather than overwriting | The published numbers must stay reproducible from the same script, and both sets are wanted side by side for the writeup — same reasoning as the `.bak_old_labeler` files. Company logic itself has exactly one implementation (`rag_sec.company`), imported by both arms, `retrieve.py` and the A/B script; the dense/BM25/RRF code is still duplicated across those scripts and is a known, deliberate debt |
| RETR-18 | Dev is no longer clean for resolver-related numbers | The multi-ticker-fragment guard (`RETR-11`) was found by reading dev failures, so every recall figure above is measured on a split that informed the design. The resolver's *generalization* rests on the untouched test split (`RETR-5`); the retrieval gains do not yet. A final test-split run is owed once the pipeline settles — state this in the writeup rather than quoting dev as if it were held out |
| RETR-19 | Entity-framing strip (`company.strip_entity_framing`) applies to the **reranker only** — candidate generation and the generator both keep the full question | Three stages, three needs: retrieval needs the company name (it is what the filter runs on), the reranker is actively hurt by it (every candidate already matches the company, so it only rewards corporate boilerplate), and the generator must know which company and year it is answering about. The payload therefore carries both question forms and the GPU scores both, so the choice is made from numbers rather than committed up front. Removes two things: filing-provenance clauses ("as reported in their 2017 Form 10-K", "according to its annual report", "based on its reported...") and the company's own name plus the preposition leading it. Measured on dev: ~24% of words removed, 5.0% of questions left untouched by a guard that keeps the original when stripping would leave under 4 words |
| RETR-20 | Two generic normalizations the stripper needed, both found by inspection and both structural rather than company-specific | (a) **Inverted names.** Reference metadata stores some names as "Interpublic Group of Companies, The" / "Hartford, The" / "Estée Lauder Companies, The" (3 of 137 here), so the alias ends in "the" and never matches natural word order in a question. De-inverted forms are emitted alongside. (b) **Orphaned legal suffixes.** Removing the name from "Entergy Texas, Inc. and its subsidiaries" left ", Inc. and its subsidiaries"; removing it from "The Interpublic Group of Companies" left "The Group of Companies". Those leftovers are exactly the corporate boilerplate the strip exists to delete, so suffix words adjacent to the cut are absorbed on both sides. **Verified non-overfitting**: held-out test split unchanged at 94.2% coverage / 99.8% precision / 0.19% harmful before and after both fixes |
| RETR-21 | Payload stores chunk text once in a shared `texts` map, not inlined per candidate | The filtered and unfiltered pools overlap heavily and chunks repeat across questions; the older payloads inlined text per candidate and paid ~240MB for a single pool. Also keeps the HPC stage self-contained: the stripped query is computed on the laptop and travels as a field, so the GPU script imports no `rag_sec` and touches no DB, same contract as `ARM3-2` |
| RETR-22 | **The company filter and the query strip are superadditive: +1.6 / -0.2 / +10.5 points recall@10 alone / alone / together.** 4.31 GPU-hours, 185,230 pairs, `rerank_score.py` | Filter alone +0.016, strip alone **-0.002** (i.e. nothing, slightly negative), both +0.105 — about 7x the additive prediction of +0.014. **The fourth cell is what makes this readable**: with three cells the conclusion would have been "stripping helps", which is false. Mechanism, exactly as `RETR-4` predicted and now measured: *without* the filter the company name genuinely discriminates among 799 filings, so removing it costs a real signal; *with* the filter every candidate already comes from that company, so the name carries zero information and can only reward whichever chunk repeats corporate boilerplate hardest. The filter creates the condition under which stripping is correct. Also settles `RETR-15`: filter alone converted +0.080 recall@50 into only +0.016 recall@10 — **20% pass-through, worse than RRF's 37%** — so the reranker was the thing discarding newly-found gold, and the query cleanup is what unlocked it. nDCG@10 (+0.119) and MRR (+0.135) both move more than recall@10 (+0.105), so gold is not merely present in the top 10 but rising to the top of it: MRR 0.658 puts the first relevant chunk at ~rank 1.5 |
| RETR-23 | `filtered_stripped` is the arm to carry forward; `unfiltered_stripped` is dropped | Query stripping is not a standalone improvement and must never ship without the filter. Kept as a scored arm in the log because the negative result is what licenses the positive one — dropping it from the record would leave the +10.5 unexplained |
| RETR-24 | **Variant-contamination bug: every non-Arm-4 retrieval query said `FROM chunks` with no `variant` predicate, so Arm 4's B (4,708) and C (1,665) rows were eligible candidates in Arms 1-3 and in all of Day 8's retrieval work.** Found 2026-09-01 when the DB reported 106,027 chunks against the documented 99,654 | B/C are alternative indexings of gold tables and **number their chunks from 0 independently of A**, so a retrieved B chunk at index 5 is indistinguishable from A's chunk 5 once retrieval drops the variant column — it is then scored against gold as if it were a different chunk. Measured on 120 company-filtered dev questions: **9.2% of candidate slots non-A, 75.8% of questions affected, and all 552 non-A candidates collided with a real A index.** Contamination is far worse *with* the company filter — unfiltered, B/C are 6% of a 99,654-chunk pool and rarely surface; filtered, the pool collapses to one company's ~625 chunks and its own B/C rows become competitive. Fixed by adding `variant = 'A'` to every retrieval query outside `day6_*` (`retrieve.py`, Arms 1/2/3, both rerank-payload builders, the A/B). **`DATA-6` already said to verify corpus size against the DB rather than the docs; that rule would have caught this and was not followed** |
| RETR-25 | Effect of the fix: **recall@50 barely moves, every top-of-ranking metric jumps** — Arm 1 nDCG@10 0.190 → 0.233, MRR 0.194 → 0.247; Arm 2 nDCG@10 0.274 → 0.344, MRR 0.255 → 0.348, while recall@50 shifts by ≤0.003 | Exactly the signature of B/C chunks occupying *top-10 slots* without changing the composition of the 50. So the published Arm 1/2 rows were **understating** dense and hybrid retrieval, which in turn overstated how much the reranker adds on top. Arm 3/4 rows were always clean (`day6_*` is variant-aware), so the old table mixed clean and contaminated rows. Independent confirmation the fix is right: clean Arm 2 recall@50 (0.687) now matches Arm 3's (0.685), which it must, since Arm 3 reranks Arm 2's pool |
| RETR-26 | The company filter's measured gain **grew** after the fix: recall@50 80.1 → 87.7 (+7.6) contaminated, **80.4 → 88.5 (+8.1) clean**, 102 recovered / 2 lost | The bias ran against the hypothesis — B/C rows were mostly *displacing* real A chunks rather than manufacturing false hits, so contamination suppressed the measured gain. Same direction for Arms 1/2: clean filter gains are larger than the contaminated ones (Arm 1 +0.141 recall@50 vs +0.126). The `RETR-22` superadditive reranked result is therefore likely to survive the re-run, but it is **withdrawn until measured**, not assumed |
| RETR-27 | Two regression guards against `RETR-24`, both hard-fail. (1) `store.assert_corpus` pins `EXPECTED_CHUNK_COUNTS = {A: 99654, B: 4708, C: 1665}` plus a no-NULL-embedding check, raised on the first `get_conn()` per process; build-time writers pass `check_corpus=False`. (2) `scripts/checks/variant_predicates.py` AST-scans every `FROM chunks` literal and fails any read whose **WHERE clause** omits `variant` | The obvious one-liner (assert `count(*) WHERE variant='A' == 99654`) would **not** have caught this — that count was correct throughout; the bug was a missing predicate, and what diverged was the *total*. So the count is pinned per-variant: it is a tripwire on corpus *change*, and it would have fired back on Day 6 the moment `day6_build_table_variants` inserted the first B row, forcing the question "do all existing reads exclude these?" before the constant could be bumped. The predicate scanner is the guard that actually catches the failure mode, at author time with no DB. It checks post-`WHERE` specifically because two Arm 4 reads name `variant` only in the SELECT list — a substring match would have passed them for the wrong reason; those two are exempted by normalized SQL in `ALLOWED`, so editing the query revokes its exemption. Verified by reintroducing both bug shapes (predicate stripped; `variant` moved to SELECT-only) and by running the assertion against a simulated A-only expectation |
| RETR-28 | Both guards moved onto the automatic path: `get_conn(check=True)` runs the source scan **before** `psycopg.connect()` and the corpus count after. `check=False` for the five build-time writers. Renamed `assert_corpus` -> `preflight`; scan logic lives in `src/rag_sec/preflight.py`, the diagnostics script is now a thin CLI over it | A guard you must remember to run is the same failure class as `DATA-6`, which existed and was not followed. Ordering matters twice over: `get_conn()` sits immediately before each arm's per-question loop, so a bad query dies after the ~30s model load instead of after a full 1235-question pass; and since the scan needs no DB, running it pre-connect means it still fires when Postgres is down — verified exactly that way, with Docker out of memory. Cost is ~55ms once per process (44ms scan scoped to `src/`+`scripts/`, ~10ms count), against passes measured in minutes to hours. Residual gap, accepted: this stops a *contaminated pass from starting*, not a wrong result already written to `data/` |
| RETR-29 | **`RETR-22` re-measured variant-clean and it survives, stronger: filter +0.031, strip +0.010, both +0.126 recall@10 — 3.1x the additive prediction of +0.041.** Headline 0.715 → **0.736**; nDCG@10 0.496 → 0.635, MRR 0.522 → 0.679. 4.37 GPU-h, `day8_retr16v2_dev_scores.jsonl` | Every cell rose once B/C rows were excluded, confirming `RETR-26`'s reading that contamination suppressed rather than manufactured the effect. **Corrects `RETR-22`'s sharpest claim**: strip-alone was reported as -0.002 ("does nothing"); clean it is **+0.010**, a small positive. The defensible statement is "each helps a little, together they help far more" — the interaction is the finding, not the strip-alone null. Mechanism unchanged: without the filter the company name discriminates among 799 filings and is worth keeping; with it every candidate already matches the company, so the name only rewards boilerplate. recall@50 for both stripped/raw filtered cells is identical (0.776) as it must be — same candidate pool, and stripping only reorders |
| RETR-30 | Both GPU payloads built for one booking. **Slices** on the corrected pool: `day8_slice_payload_t150_filtered_stripped.json`, 555,337 slices over 61,750 candidates (9.0/candidate), 276 MB, **0 replay mismatches**. **Test split** (`RETR-18`): `day8_retr18_test_payload.json`, 1546 questions x full 2x2, **309,160 pairs**, 141 MB. `slice_prepare.py` gained `--source/--cands/--question/--tag` and reads both payload shapes | Slice scores are query-dependent, so `COST-17` made a re-score mandatory rather than optional: the old file was built off `day6_arm4_A` (unfiltered, variant-contaminated, raw queries) and its scores cover only 77.8% of the new top-10 chunks. Test needs all four cells, not dev's three — it has no `unfiltered_raw` baseline, and without that cell a better number cannot be attributed to the filter vs. the strip. Two output bugs fixed while here: the pair-count line hardcoded "3 new cells" (wrong for test's 4), and the suggested HPC output filename was the literal `day8_retr16_scores.jsonl`, which would have **clobbered the dev scores on copy-back** — now derived from the payload name. Independent check that fell out of the build: resolver coverage on the untouched test split is 1456/1546 = **94.2%**, matching `RETR-17` exactly. Run as two parallel interactive `srun` allocations (one tmux session each, jobs 9895953/9896088 on d1009/d1007), not sequentially. **Measured pace once live: test split 348s per 20-question batch -> 7.5h (11.8 pairs/s); slices 42s per 8-question batch -> ~1.8h (85.7 pairs/s).** The slice job was pre-estimated at 4.3h by mis-anchoring on `RETR-22`'s chunk-level 4.31 GPU-h — the old slice run's wall time was never logged. Corrected reading: throughput is **token-bound and near-linear** (7.3x faster on 6.4x shorter pairs), so `predict()`'s batch padding is not measurably costing anything at BATCH_SIZE=128. Stagger the submissions past model load — both `CrossEncoder(...)` calls read `BAAI/bge-reranker-v2-m3` from one `~/.cache/huggingface`, and two cold downloads to the same path is an avoidable corruption risk. `--time=08:00:00` is a real risk for the test job at 7.3h +/-20%; checkpointing makes an overrun a resume, not a loss. `.sbatch` equivalents exist (`scripts/retrieval/rerank_hpc.sbatch`, `scripts/compression/slice_rerank_hpc.sbatch`) and are the better tool for unattended runs, but interactive was chosen for live visibility |
| COST-18 (current survival table; nothing shipped from it — `COST-36`) | **Slice arms re-scored on the corrected pool (555,337 slices, 1:51:38 GPU, `day8_slice_scores_t150_filtered_stripped.jsonl`). `COST-11`'s ranking survives; its magnitude does not.** chunks / slices / slices50 by budget: @500 15.8/**75.1**/73.6, @1000 70.0/**81.9**/80.4, @1500 80.2/**85.2**/82.3, @2000 84.6/**86.4**/84.2, @3000 88.3/**88.6**/85.5. FULL control 93.3% | Three findings. (1) **Slices still beat whole chunks at every budget, but the margin decays with budget**: +59.3 pts @500, +11.9 @1000, +5.0 @1500, +1.8 @2000, **+0.3 @3000**. So the correct claim is not "slicing is better" but **"slicing is what makes a small budget viable"** — at $6/pass the machinery earns 0.3 points and should not ship; at $3.40/pass it earns 11.9. This resolves `COST-17`'s open question in slicing's favour while narrowing what it may be used to justify. (2) **`slices50` loses to `slices` at all five budgets on a clean, filtered, stripped pool** — a replication of `COST-11`'s finding under the conditions that invalidated everything else, so the recall play is dead on independent evidence, not on the contaminated measurement it was originally dropped for. Consistent with `RETR-10`. (3) Both arms rose ~12-16 pts versus the stale table, i.e. retrieval quality lifts compression uniformly rather than trading against it. New operating recommendation: **slices@1500 = 85.2% for ~$4.16/pass** (**superseded by `COST-24`: ~$5.27, and the cut is 2.5x not 4.1x**) (4.1x cost cut, 8.1 pts off the 93.3% ceiling), or slices@1000 = 81.9% for ~$3.40 if cost dominates. Dollar column is derived from `COST-1`'s 89/11 split scaled by tokens, not separately measured. `COST-11`'s survival-matcher caveat still applies: `CLUSTER_SHARE_THRESHOLD` was calibrated on whole chunks, so absolute levels carry that assumption while between-arm comparisons do not |
| RETR-31 | **`RETR-18` done: the `RETR-29` result replicates on the untouched test split, and the effect is larger out of sample than in.** 1545/1546 test questions (1 has no gold label), full 2x2, 309,160 pairs, 7:11:34 GPU. recall@10 0.581 baseline -> **0.726** filtered+stripped (**+0.145**, vs dev's +0.126); filter alone +0.042, strip alone +0.015; recall@50 0.670 -> 0.769 (+0.099); nDCG@10 0.461 -> 0.618 (+0.157); MRR 0.490 -> 0.658 (+0.168) | **This is the row that makes the headline quotable.** Every structural feature of the dev finding survives: superadditivity (additive prediction +0.057, actual +0.145 = **2.5x**, a ~8-stderr gap; dev was 3.1x), the sign and ordering of both isolation cells, and recall@50 identical across raw/stripped filtered cells as it must be (same pool, stripping only reorders). Test's baseline is genuinely harder (0.581 vs dev 0.609), so the absolute headline is 0.726 rather than 0.736 — quote **test**. The strongest available anti-overfitting evidence: dev informed the resolver's design, yet the out-of-sample gain *exceeds* the in-sample gain on all four metrics, so the +0.126 was if anything an underestimate rather than a fitted artifact. Ran under the 8h wall with ~48min spare; no resume needed. Downstream caveat unchanged: `COST-18`'s compression table was measured on dev's `filtered_stripped` pool, so it inherits dev, not this |
| RETR-32 | **`retrieve()` now ships `RETR-31`'s winning cell**: `strip_entity_framing` is applied to the cross-encoder query only, candidate generation and the returned text unchanged. New `strip_query` flag (default `True`), so the earlier arms stay reproducible from the same code path | Closes the gap `SESSION.md` 2(c) flagged: the shipping path implemented filter-only (+0.042) while the confirmed result is filter+strip (+0.145). Verified against `rerank_prepare.py`, which is what the measurement ran — raw question to dense/BM25 and to `resolve()`, stripped question only in the rerank pair — and with fakes standing in for the DB and both models, asserting which query reaches which stage. No re-measurement needed: this makes the code do what `filtered_stripped` already did. Two resolver precision leaks quantified on the measured payload while here, both left alone because `RETR-31` was measured with them: **3/1235 questions strip to a degenerate query** ("What was.'s rent expense in 2011?") which still clears the 4-word guard, and **51/1235 resolve to more than one ticker** from leading-token aliases colliding with real subsidiary names — `('ETR','TXN')` 16x from "Entergy **Texas**" hitting Texas Instruments, `('IP','PKG')` 14x. Both are dilution, not deletion (`RETR-14`): the gold company is still in the pool and 2 tickers is under `MAX_TICKERS` |
| RETR-33 | **`failure_triage.py` re-run on the shipped `filtered_stripped` ordering — the analysis that redirected the whole project, refreshed.** Day 6 -> now: hit@10 73.5% -> **87.0%**, rerank_miss 6.9% -> **1.5%**, candidate_miss 19.6% -> **11.5%**, no_gold_chunk **0% both times**. Script gained `--scores`/`--cell`/`--out`; the old ordering still runs from the same code and reproduces 73.5/6.9/19.6 and the old top-k recovery curve exactly (re-verified independently), which is what makes the two columns comparable rather than two different measurements. Both score files carry all 50 candidates, so the rerank/candidate split is reproduced, not redefined. New cases in `day8_failure_cases_retr16v2.json`; the old file kept | **Retrieval is still the binding constraint and now nearly the only one: `candidate_miss` is 142 of 160 remaining failures (88%).** The filter+strip gain did not merely add recall, it **absorbed the reranker's headroom** — the 6.9 points that once justified reranker work are 1.5, and widening top-10 -> top-25 now recovers 1.2 of them against 5.4 before, so **`RETR-2` is no longer a lever worth its token budget**. Gold's median rank inside the 50 when missed is unchanged (18) but the tail collapsed (max 50 -> 33). `no_gold_chunk` = 0 a second time: the gold chunk always exists in the filing, so a re-chunk + re-embed cycle still has no evidence behind it and the remaining loss is first-stage candidate generation. Part of the rerank_miss shrinkage is mechanical — questions that were rerank misses are now hits, so the reranker got less to fix rather than better — but the conclusion holds either way. Dev only, and dev informed the resolver; quote `RETR-31` for recall |
| RETR-34 | **`RETR-9` answered by a blinded audit of the relevance labels themselves: label noise is real at ~16%, but it is NOT concentrated in the failures, so it does not explain the candidate-miss bucket.** 660 cases (24% of the 2,781 scored questions) across 15 independent auditors: the census of all 160 dev failures, 300 dev hit@10 controls, and 200 test questions. Auditors saw only the benchmark's question/answer/stated evidence and the chunk our pipeline assigned — retrieval outcomes were absent from the case files, so the stratum could not be inferred. **dev failures 21/142 = 14.8% not-fully-correct; dev successes 49/300 = 16.3%; difference -1.5 pt, se 3.7, z = -0.42.** Test rates track dev (13.7% on hit@10). Overall 554 correct / 85 partial / 7 wrong chunk / 5 evidence absent / 9 unjudgeable | **`RETR-9`'s premise is refuted and the 142 candidate-miss questions are genuine retrieval failures.** The ~15% mislabel rate it flagged is real — and is the *base rate everywhere*, not something special about failures, which is exactly what hand-reading only failures could never have shown. The control stratum is what made this decidable; a failures-only audit would have found 21 bad labels and licensed a correction that the data does not support. **The dominant mechanism is over-collection, 65 of ~106 non-correct verdicts**: the labeler assigns several chunks where one holds the evidence (partially_correct averages **2.88** assigned chunks against 1.54 for correct, max 7), because a gold row is matched on figures that are not distinctive inside one filing — six chunks pulled in by a shared `500`, or a `$381` that is an intangibles line in one table and loan accretion in another. **Consequence for every published recall number: they are deflated, uniformly.** recall@k is the *fraction* of gold chunks retrieved, so a question with 3 bogus gold chunks caps at ~0.39 even under perfect retrieval; ~13% of questions are in that state. This lowers absolute recall across all arms without changing any between-arm comparison or the bucket attribution in `RETR-33`. The fix is a tightening of `eval.py`'s matcher guards (`MIN_ROW_NUMBER_DIGITS`, `ROW_NUMBER_MATCH_THRESHOLD`, `ROW_NUMBER_MAX_DOC_FREQ`) plus a row-label check for the numerically-coincident cases that no digit or frequency guard can catch — derived on dev, applied mechanically, never hand-edited on test. **Caveat: the auditors are models, not humans**, so these verdicts are themselves judgments; blinding was structural (retrieval data absent from the files), and 9 cases were genuinely undecidable because the benchmark supplied no evidence at all |
| RETR-35 | **Layer-1 gold matching rewritten from filter-by-threshold to minimal coverage, and every published recall number rises.** The old rule admitted *every* chunk scoring over `ROW_NUMBER_MATCH_THRESHOLD`; the new one assembles the smallest set of chunks that covers the gold row's distinctive figures, admitting a chunk only when it contributes a figure no admitted chunk already supplies. dev: mean gold chunks 1.688 -> **1.205**, recall@10 0.736 -> **0.765**, recall@50 0.776 -> 0.806. test: 1.689 -> **1.235**, max 11 -> **4**, recall@10 **0.726 -> 0.752**, recall@50 0.769 -> 0.787. 71% of dev labels are unchanged and **99% of the changes are removals only** (285 identical / 111 strict subset / 4 added-or-swapped of 400) | **Structural, not fitted — that is the whole point.** The benchmark names one row and a row exists at one place in a filing, so recovering it is a *selection* problem; the old code implemented *filtering*, which no setting of the constant repairs. Coverage generalises over arity without capping it: one chunk holding the whole row leaves nothing for a second to add, a row genuinely split across n chunks admits all n, and a coincidental match only re-supplies covered figures so it is rejected however well it scores. Two guards from the same reasoning that already justifies `MIN_ROW_NUMBER_DIGITS`: a chunk carrying one of the row's numbers needs the row *label* to corroborate (the label was previously discarded entirely, which is how a `shares outstanding` row matched `weighted-average diluted shares`), and the *union* must still recover most of the row or we return empty. **No new constants** — `ROW_NUMBER_MATCH_THRESHOLD` is reused for union coverage and label overlap. **Test replicates dev almost exactly** (+0.026 vs +0.029 recall@10, mean gold 1.235 vs 1.205), which is the evidence it is a rule rather than a dev fit. **Independent check against `RETR-34`'s audit, which played no part in the design:** cases auditors called `partially_correct` shrink 2.88 -> 1.62 (66% shrunk) against `correct` 1.54 -> 1.16 (23%), and **zero questions in any category lost their label**. **Every replayable ordering re-scored** (`rescore_labels.py`), legacy -> coverage recall@10: test baseline 0.581 -> 0.604, filter-only 0.623 -> 0.644, strip-only 0.596 -> 0.625, filter+strip 0.726 -> **0.752**; dev filter-only 0.641 -> 0.666, strip-only 0.619 -> 0.648, filter+strip 0.736 -> 0.765; Arm4-A 0.609 -> 0.634. **The superadditivity decomposition now sits entirely under one labeling: filter alone +0.040, strip alone +0.021, together +0.148 — 2.4x the additive prediction, against 2.5x before.** The correction is *not* uniform: Arm4-B/C move -0.002/-0.004, because their chunks are single rows and summaries, so there was little over-collection to remove — evidence the rule targets a real defect rather than adding a constant. **Arm 1 and Arm 2 cannot be re-scored at all**: their results files persist only `top_5_retrieved`, so recall@10/@50 is unrecoverable and they need a retrieval re-run against Postgres — the concrete cost of the results-persistence debt in SESSION.md 4. **Caveat that matters for the README: MRR and nDCG cannot be compared across matcher versions.** MRR is 1/rank of the *first* relevant hit, so removing bogus gold chunks removes chances of an early hit and MRR falls mechanically (dev -0.064, test -0.045) regardless of retrieval quality. Recall is the metric whose denominator was being corrupted and is the one that corrects. Every arm-vs-arm comparison stays valid — all arms were scored under one labeling — but no number measured under the old labels may be set beside one measured under the new |
| COST-19 (docs correct, outcome wrong — see `COST-29`) | **Re-verified `COST-12`'s flex finding one day later (all checks 2026-09-02) and its headline is wrong: `service_tier="flex"` is GA on the Gemini API and reachable today.** `client.models.generate_content(model=..., config={"service_tier": "flex"})` via the raw `google-genai` SDK — 50% off standard, target latency 1-15 min, best-effort, supports both `gemini-3.7-flash` and `gemini-3.1-flash-lite` | The blocked path was only ever LangChain, and `COST-12` already prescribes the raw SDK for Batch, so **flex costs zero extra integration over the Batch plan**. Three corrections to how `COST-12` argued it: (1) `langchain-google-genai` **4.4.0 does exist** (published 2026-09-01T20:15:44Z, hours after that research pass) — it just doesn't contain `service_tier`, so the conclusion survived for the wrong reason; (2) PR #1937 is still open (created 2026-08-11), and "unmerged since April" conflated it with **issue #1682**, the April feature request; (3) the right justification is "PR #1937 unmerged", not "the version doesn't exist" — a version-existence argument expires in days. **Operational split, since flex and Batch are the same 50%:** use **flex for `COST-13`** (~300 requests — one config kwarg, results in minutes, no 3M enqueued-token cap to chunk around) and **Batch for full 1235/1546-question passes** (its real product is *extended* rate limits; flex traffic counts against normal RPM/TPM, so a 30M-token pass would need a self-managed concurrency pool). Batch's three numbers from `COST-12` all re-confirmed: 50%, 24h SLO, Tier-1 cap 3M for 3.7-flash (Tier 2 = 400M — worth checking which tier this key is on, since Tier 2 removes the chunking objection entirely). `COST-16` pricing confirmed unchanged and **the 2027-01-01 doubling of 3.7 Flash still applies to flex and Batch too** ($0.375/$1.875 -> $0.75/$3.75), so the discount tiers do not shelter us from it. Not confirmed: `COST-16`'s claim that `3.1-flash-lite` shuts down 2027-05-07 — the pricing page shows no deprecation date; not contradicted, just unsourced. Pricing came from ai.google.dev only, not cross-checked |
| COST-20 | **`COST-13`'s random-sampling design was underpowered and is replaced by a stratified one, measured from per-question survival flags (`data/day8_survival_flags.json`, new `--dump` flag).** Survival-discordant pairs out of 1235: uncompressed vs slices@1500 **100 (8.1%)**, vs slices@1000 140, vs slices@500 **224 (18.1%)**; slices@1500 vs @500 136 | McNemar's power comes from discordant pairs, not marginal rates, and `COST-18`'s table cannot reveal them. At n=100 random, uncompressed vs slices@1500 yields only **~8 discordant pairs** — the 8.1-point survival gap is spread thin because 85% of questions survive in *both* arms, so most of the spend buys pairs identical by construction. The old design would most likely have returned "no significant difference" and been misread as "compression is free," when it would only have meant too few informative questions were sampled. **New design: 2 arms (uncompressed, slices@1500), 150 questions — all 100 of the gold-lost stratum (no sampling; that is the whole population) plus 50 gold-kept controls, post-stratified back to the population using the exact 8.1/91.9 split known from all 1235 flags.** $0.79 on flex vs $2.90 for equivalent power at random (n=552), and it measures the budget actually proposed for deployment. The control stratum isolates a confound the original design could not: whether compression hurts merely by removing surrounding context, separately from removing the gold. Two by-products: (1) **nesting is exact** — uncompressed vs every slice arm loses 100/140/224 and gains **0**, so compression never manufactures survival, an internal consistency check on the whole sweep; (2) **6 questions survive at slices@500 but not @1500** (0.5%), real per-question non-monotonicity from `_pack_by_score`'s skip-don't-break rule admitting one large slice over several small ones — the aggregate curve stays monotone, so this bounds the effect rather than contradicting `COST-18` |
| COST-21 | **The dataset ships two gold answers per question and they disagree on 10.1% of dev — found while building COST-13's scorer, before spending anything.** `program_answer` vs `original_answer` over 1235 dev: 688 agree within 1%, **380 differ by exactly x100** (`0.935` vs `93.5%`), 8 differ only in sign, **125 (10.1%) are irreconcilable**, 34 unparseable. New `src/rag_sec/answer_eval.py` | Scoring against either field alone would have injected ~10-40% label noise into the headline. Number Match accepts a prediction matching **either** field at 1% relative tolerance — which also handles the sign cases without making the scorer sign-blind, since one field carries each sign, so a wrong-direction answer must still match some stated reference. **Seven scale factors, not three:** identity, x100 / /100 for the percentage convention, and x1e3 / x1e6 (and inverses) for the "(in thousands)"/"(in millions)" table headers, where gold is the table figure but the model answers in dollars (`convfinqa_1431`, gold 567048, table in thousands). The thousands/millions pair is not free: it flips **5 of COST-23's 278 verdicts**, all genuine, 3 uncompressed against 2 compressed — ~0.7pt on the paired delta. Relative tolerance is required, not cosmetic: the fields differ mainly by rounding (24.691358 vs 24.69, 1041.531 vs 1041.5) and exact match would score those wrong for reasons unrelated to the model. **127 unscoreable questions are excluded from the strata up front** rather than scored — the 125 irreconcilable plus the 2 with *neither* field parseable; the other 32 of the 34 have exactly one parseable field and are KEPT and scored against it, so the exclusion is 127, not 125+34=159 (`gold_is_scoreable`, re-verified against dev on 2026-09-03). They would fail in both arms, land in McNemar's concordant cell, and consume budget while informing nothing — which shrinks stratum A from 100 to **89** and is why the sample is 139, not 150. Distinct from `RETR-9`: that is noise in the *evidence* labels, this is noise in the *answer* labels, and neither was known when `COST-13` was first priced. Note this cannot bias the paired result even if left in — both arms are scored against the same wrong target — but it does consume power, which is the scarce thing here |
| COST-22 | **COST-13 harness built and priced; nothing sent yet.** `src/rag_sec/answer_eval.py` (Number Match), `answer_ab_prepare.py` (strata + prompts, offline), `answer_ab_run.py` (flex, checkpointed), `answer_ab_score.py` (exact McNemar + post-stratification). Payload: 139 questions x 2 arms = 278 calls, 1,615,830 input tokens, **est $0.76 on flex**; means 10,047 tok uncompressed vs 1,578 compressed | Three-stage split matches the HPC pattern (`ARM3-2`): the expensive stage is self-contained and resumable, and both cheap stages run offline so the prompts can be inspected (`--show`) and priced (`--dry-run`) before any spend. Four choices worth defending. (1) **`pack_by_score` moved into `compress.py`** and the sweep script now imports it — COST-13 must reproduce byte-for-byte the prompt whose survival the sweep measured, and a second copy of that rule would silently invalidate the flags the strata are built from. (2) **Prompt is `agent._ANSWER_PROMPT` verbatim plus one `ANSWER: <number>` line** — the scorer's free-text fallback ("last number") would work on average but could differ *between arms* if format compliance differed, which is a confound in the measured quantity; if this ships, `agent.py` should adopt the same line. (3) **Calls interleave by question, not grouped by arm** — API drift mid-run (throttling, a model update) would otherwise land entirely in one arm. (4) **A failed call raises after 5 retries rather than recording empty** — a missing response would score as a wrong answer and bias whichever arm it hit. Actual `usage_metadata` is recorded per call so spend is reconciled against the estimate, not assumed |
| COST-23 | **`COST-13` run and answered: compression at slices@1500 costs real answer accuracy. Population delta -8.97% +/- 4.73% (1 se); 139 questions, 278 calls, **$2.22 actual** (corrected 2026-09-04).** The original "$1.53 actual against $1.53 estimated" was two offsetting errors, not a validation: the estimate priced output at a 300-token guess and the actual priced `candidates` only, both at standard rates. Accuracy numbers below are unaffected and re-reproduce exactly from disk.** Stratum A (gold removed, 8.0% of corpus): 53.9% -> **27.0%**, n10=25 / n01=1, exact **p<0.0001**. Stratum B (gold survived, 85.0%): 68.0% -> 60.0%, n10=6 / n01=2, p=0.289. Refusals 21 -> 50 | **The mechanism is settled: gold survival is a genuine proxy, not a bookkeeping artifact** — removing the labeled evidence costs 27 points, and the failure mode is *declining* (refusals more than double) rather than fabricating, which is the safer failure. Three findings beyond the headline. (1) **Stratum B dropped 8 points even though the gold survived**, so compression hurts through a second channel — most likely slicing fragmenting tables or removing the context needed to interpret a figure. Not significant alone (p=0.289), but at 85% weight it supplies **-6.8 of the -9.0 points**. Only the stratified design can see this; the original unstratified COST-13 would have reported one pooled number and attributed all of it to evidence loss. (2) **27.0% in stratum A is far above the ~0% expected if the labels were strictly necessary** — a quarter of these are answerable without the labeled gold (answer figures recur elsewhere in the prompt, cf. `convfinqa_1431`), so gold survival *overstates* the damage, exactly as `COST-13` suspected. (3) **`COST-20`'s design optimized the wrong target, my error.** Enriching stratum A maximized power for the mechanism question, but the decision-relevant population estimate is precision-limited by stratum B (n=50), whose 0.055 se times its 0.85 weight is essentially the entire 0.047 population se. 95% CI ~[-18, +0.3] does not cleanly exclude zero. **To size the trade rather than merely establish it, sample B, not A**: n=200 in B (~$1.66 standard, ~$0.83 flex) would roughly halve the interval. Two harness bugs found and fixed during the run, both logged in `COST-22`'s successor notes: the tqdm label read `flex` while running standard, and the scorer hardcoded flex prices and reported $0.76 for a $1.53 run — the tier is now recorded per response |
| COST-24 | **Every dollar figure in this log may understate cost, because `gemini-3.7-flash` bills internal reasoning tokens as output and they are absent from `candidates_token_count`.** Found by reconciling the billing dashboard's $2.23 against my reported $1.54 for `COST-23`'s run. Measured over 279 calls: **~662 thinking tokens/call against ~110 visible (6x)**, 184,822 unaccounted tokens x $3.75/1M = the exact $0.69 gap | The data was recorded and not used: I stored `total_token_count` but priced from `prompt + candidates`, and `total - (prompt + candidates)` is precisely the thinking tokens. **Thinking is near-constant across arms — 694 uncompressed vs 627 at slices@1500 — so it does not shrink with the prompt and acts as a hard floor compression cannot touch (~$3.9/pass).** Corrected per-pass costs (1235 dev, one answer send), old -> new: uncompressed $9.93 -> **$13.00**, slices@3000 $3.60 -> **$6.66**, slices@1500 $2.21 -> **$5.27** (+139%), slices@500 $1.28 -> **$4.35** (+239%). **So `COST-18`/`COST-23`'s headline trade is 2.5x, not 4.1x** — same decision, worse terms — and 1500 -> 500 now saves only $0.92 while costing 10 points of survival, because it squeezes the part of the bill that is already small. **Unverified and now suspect:** `COST-1`'s $16.97 teardown, `AGENT-3`'s $0.014/question, `COST-11`'s ~$3.10/pass, and every `$/pass` derived from them — all predate this and may or may not have included thinking; re-derive before quoting. Fixed in `answer_ab_run.py` (records `thinking_tokens` + `billed_output_tokens`, estimator now uses 772 tok/call not 300) and `answer_ab_score.py` (prices visible + thinking, prints the split; falls back to deriving it from `total` for responses recorded earlier). **Worth testing next:** Gemini exposes a thinking-budget control — if reasoning can be capped for a task whose answer is one number, that attacks the floor directly and is worth more than further input compression. Parameter name and support for 3.7-flash not yet verified |
| COST-25 | **`COST-23`'s stratum B is mislabelled, so its decomposition is wrong: where the flag said the gold survived, the gold *figures* were usually gone.** 5 of the 6 B-losses carry 0% of their gold rows' distinctive figures in the compressed prompt against 100% uncompressed, and 5 of 6 are refusals, not wrong numbers. At scale (`stratum_b_channel.py --survival`): matcher survival 84.9% but **every-gold-figure survival 69.3%** at slices@1500, against 93.6/86.9% uncompressed. **Provenance correction, 2026-09-03: that run scored only 251 questions because `data/day8_survival_flags.json` then covered roughly the first 374 dev questions rather than all 1,235.** The file was rewritten and no prior copy was kept (single commit, no earlier revision), so the run is **not reproducible**. Every other input and all the code are byte-identical to HEAD — verified by re-running the pool build against HEAD-extracted modules, which gives the same pool — so nothing here was a code change. The same command now draws 300 from a **775**-question pool and reads 84.3/67.0 against 91.7/83.7. **Quote 67.0%, not 69.3%.** Within-run comparisons below are unaffected (matcher vs figure criterion, uncompressed vs slices@1500, 20/213, 18.3%): the same questions are judged by both criteria in both arms **The inflation is asymmetric — 6.7 pts on the control, 15.6 on the compressed arm — so the arm-to-arm gap doubles, 8.7 -> 17.6 pts.** 9.4% of stratum B (20/213) holds not one gold figure; 18.3% is missing at least one | `slice_budget_sweep.py`'s docstring conceded the absolute survival numbers carry `CLUSTER_SHARE_THRESHOLD`'s whole-chunk calibration but claimed "the comparison *between* arms is unaffected, since all three are judged alike". That is refuted: the matcher fires on a prose `gold_ind` sentence or a partial cluster, and a compressed prompt is exactly the shape that keeps a table's caption while dropping its rows, so the loose criterion flatters the compressed arm twice as much. Consequence for the open decision: **do not buy a tighter CI on stratum B yet** (`COST-20`'s n=200, ~$1.66) — the stratum leaks ~9-18% stratum-A cases, and the honest damage from compression is larger than -8.97%, not more precisely -8.97%. Coverage predicts correctness where the matcher did not: 98% mean coverage in both-right, 20% in B-LOSS |
| COST-26 | **The B-channel is a slice-*ranking* failure inside the correct chunk, not header severance.** In all 6 B-losses the gold-bearing table slice comes from chunk rank 0-2 — retrieval was right — but the slice cross-encoder scores it +0.004 to +0.107 and lands it at rank 22/30/41/46, while prose from the *same chunk* scores +0.68 to +0.99. slices@1500 packs only 12-19 slices, so rank 22+ never enters. What outranks the gold: the table's own caption ("Future minimum commitments under these operating leases are as follows"), and the right company's wrong-year copy of the same table | `slice_atom` is exonerated — the packed table slices carry their preamble correctly (`(in millions) \| 2007 / Third-party \| $19,636`), and there is no global anti-table bias (table slices mean +0.152 vs prose +0.114; 27/32 gold slices do get packed). The reranker rewards the *description* of the evidence over the evidence: a caption carries every question word, the row group that answers it carries none. The chunk ranked #0 **because** it held both halves, and slicing splits them and keeps the wrong one — a within-chunk restatement of `RETR-30`'s wrong-document finding. Points at three offline, free fixes (slice scores are already on disk): require a 3+ digit non-year figure in some packed slice, bind a caption slice to its table's rows, or cap the tokens any one chunk can take. Measure them against 69.3% figure survival, not 84.9% matcher survival |
| COST-27 | **All three of `COST-26`'s proposed slice-ranking fixes were built and measured, and none of them works. Fix A — the labelling/ordering fix — ships anyway, on structural grounds rather than on a score.** `scripts/analysis/pack_variants.py`, 300 dev questions with numeric gold rows, budget 1500, offline (slice scores already on disk; no GPU, no API). Every-gold-figure survival, and paired discordant counts vs today's packer: base **67.0%**; A grouped+labelled+document-order 66.0% (0 gained / 3 lost); A+figure-guard 64.7% (2/9); A+caption-binding 66.0% (0/3); A+per-chunk-cap 65.0% (3/9); all three 64.3% (3/11) | Read on discordant pairs, not marginal rates — `COST-20`'s lesson. **Every pre-registered rule loses more questions than it gains**, so the negative result needs no fitting and cannot be an artifact of tuning. Two mechanism findings behind it. (1) **`COST-26`'s caption channel is real but rare**: only 25 figure-less table slices are admitted across 100 questions, and the binding rule drops 2 captions and pulls in 3 mate rows — the mechanism was diagnosed from 6 hand-read losses and does not generalise to the population. (2) The figure guard actively hurts because it promotes a figure in *every* packed chunk, and 96% of prompts hold two or more filing-years of the same company, so most promotions buy a wrong-year decoy. **A sensitivity sweep is logged as a warning, not a result:** restricting the guard to the top chunk alone scores 68.3% (+1.3, 6 gained / 2 lost) — but that is McNemar exact **p=0.29**, and it is the best of six swept variants, so the honest reading is noise dressed as a finding. It ships only if it survives the untouched test split; until then it is a hypothesis. A pilot at n=30 read +3.3/+3.3/+6.7 for the three rules — all sign-flipped at n=300, which is why the pilot was never the number |
| COST-28 | **`compress.pack_grouped` ships: the compressed arm is now labelled, chunk-grouped and document-ordered, so it differs from its uncompressed control in *how much text* and nothing else.** Measured cost, 300 dev questions: 66.0% vs 67.0% every-gold-figure survival — 0 gained, 3 lost, entirely the heading tokens, which are charged to the 1500-token budget so the arm stays the one `COST-18` priced. Bought with it: prompts carrying attribution **0% -> 100%**, and tables whose rows are split apart by unrelated text **39.7% -> 0%** | `COST-23` was written up as a one-variable comparison and was in fact a three-variable one — how much text, what order, and whether the reader can see which filing and year each block came from. This closes two of the three. It is a correctness fix, not a tuned one: nothing in it is fitted to dev, so its -1 point is a known price rather than a result that might not replicate. The case for paying it is `RETR-3` restated inside a single prompt — **96% of compressed prompts hold two or more filing-years of the same company**, and unlabelled, nothing tells the reader which year a number belongs to (a worked pair holds three near-identical AMT deferred-tax tables from 2010, 2008 and 2016). `pack_by_score` is kept and reachable via `--legacy-packing`, because `COST-13`'s payload on disk has to stay reproducible byte-for-byte — verified, not assumed: regenerating it matches all 139 questions in both arms exactly, so the survival flags the strata are built from remain valid. Guard: 60/60 questions verified identical between the measured cell and the shipped function — a bug caught that way, `pack_grouped` had assumed a chunk's first appearance in score order was its retrieval rank, which is the chunk with the best *slice*, not the best chunk |
| INFRA-5 | **`pyproject.toml` reconciled against what the code actually imports, and the three model-deciding packages pinned exactly.** Removed `docling` (0 imports), `fsspec` and `psycopg-pool` (transitive-only). Added `torch` (imported in **8** files), `tqdm` (14), `langchain-core` (2), `numpy` (1), `beautifulsoup4` (1). Pinned `sentence-transformers==6.0.0`, `transformers==5.8.1`, `huggingface-hub==1.28.0`. 129 packages resolved; `.venv` synced, all direct imports verified | Undeclared imports are a manifest defect independent of the lockfile: `torch` and `tqdm` were present only because `sentence-transformers` drags them, so any upgrade that drops them takes 22 files down with no warning. `uv lock` proved the risk is live rather than theoretical — the lock held **two** `transformers` versions (5.8.1 and 5.15.1) and the pin collapsed it to one, i.e. some resolution path was already free to float the tokenizer out from under every embedding in this log. The three pins are the ones whose minor versions can silently move every number here; `torch` is deliberately left a floor (`>=2.13.0`) because an exact pin fights per-platform CUDA/CPU wheel selection and the HPC resolves a different variant than the Mac. `docling` stays in `CHUNK-1`, `spec.md` and `parsing.py`'s comments as the recorded fallback path — removing the dependency does not retract the decision, it just means that fallback now starts with an install. Tests/lint deliberately still absent. **Not fixed, found while here:** all 8 device selections read `"cuda" if available else "cpu"` and never check `mps`, so Apple Silicon silently runs on CPU; and `slice_rerank_hpc.sbatch` builds its env with `module load python/3.13.5` + `~/rerank-env`, so every reranking number in this log came from an environment neither `pyproject.toml` nor `uv.lock` describes |
| INFRA-6 | **Device selection now picks `cuda > mps > cpu` at all 8 sites; it was `cuda`-only, so Apple Silicon silently ran on CPU.** New `rag_sec.config.pick_device()` used by the 3 sites that import `rag_sec`; inlined in the 5 self-contained HPC scripts. Measured on the reranker, 32 pairs: **mps 0.36s vs cpu 0.75s (2.1x)**, scores identical to **2e-6** | Verified rather than assumed — an accelerator that silently changed ranking would be worse than a slow one, and 2e-6 is fp32 reduction-order noise, orders below any score gap that decides a rank. Duplicated in the HPC five on purpose: `ARM3-2` makes them runnable on a node with no `rag_sec` installed, and they already duplicate `RERANK_MODEL_NAME` under the same rule — importing a helper to save 6 lines would trade that guarantee away. `torch` is imported lazily inside `pick_device()` because `config.py` is on the chunking/eval paths that never touch a GPU and importing torch costs ~1s. **Only explicitly-passed `device=` sites were affected**: bare `SentenceTransformer(...)` calls already auto-select mps, so the embedding path was never the bug **Power-state caveat (2026-09-04): laptop rerank latency measured 2.9x slower on battery — 22.9s to 66.8s mean — with no thermal warning ever recorded. Every laptop timing in this file is a ratio at best, and the power state must be recorded alongside it.** |
| INFRA-7 | **`src/rag_sec` docstring/comment pass, plus three portability fixes in `company.py`.** Module docstrings cut to 1-2 lines with the reasoning moved next to the constant or function it explains (or left to the `DECISIONS.md` row that already holds it): 2,437 -> 2,276 lines, no behaviour change outside the fixes below | Four things were wrong rather than merely verbose. **(a)** `company._aliases_for(name, ticker)` never used `ticker` — ticker matching is `_tickers_from_symbols`' job — and `answer_eval.parse_prediction` had no callers; both removed. **(b)** `eval._chunk_file_index` unpacked every entry `os.listdir` returned, so a stray `.DS_Store` in `data/chunks` would crash the index build on the machine this runs on; now skips non-`.json`. **(c)** `company.LEXICON_PATH` was cwd-relative, so importing retrieval from a different directory made the lexicon "missing" and silently dropped the company filter on every question — now anchored to the repo via `__file__`. **(d)** the English-word guard fell back to no guard whenever `/usr/share/dict/words` was absent (i.e. on Linux and the HPC nodes) and said nothing; it now tries the Debian paths and a `COMPANY_WORDLIST` override, and warns loudly if it finds none, because the failure is invisible in the results — recall just drops on the questions whose alias is an English word (`RETR-11`) Second pass: the three retrieval metrics were annotated `list[int]` while every arm has always passed `(stem, index)` or `(stem, index, variant)` tuples — now `Sequence[Hashable]`, since the ids are opaque to them; `chunk_blocks_with_variant` with Strategy C and no summarizer died inside the atom builder *after* packing the whole filing, and now raises before that work; and `agent.py` hardcoded `k=10` in two places next to `retrieve.TOP_K`'s own 10, so it now takes the default. Noted in place, not changed: `retrieve_tool`'s body never executes — `plan` emits the call and `retrieve_node` runs it, so the tool is bound for its schema alone, which reads like live code and isn't |
| INFRA-8 | **Three staged ingest scripts (`day2_ingest.py` 100 filings/seed 42, `day4_ingest_next200.py` 250/seed 45, `day2_chunk.py`) folded into one `scripts/corpus/build_corpus.py` that takes the whole 799-filing pool — no sampling, no seed.** Per-stage skip on raw/parsed/chunks, so one run reaches 799 and re-running is a no-op; `--rechunk` (~4 min) and `--reparse` (~30 min) rebuild without re-downloading, `--limit N` smoke-tests. Old three deleted (recoverable from git), their logs kept. Timings measured from the ingest logs, not estimated: fetch median 1.23s/filing, parse 1.45s, chunk 0.27s | The sampling was an artifact of growing the corpus in stages (`DATA-6`), not a design: once 799 *is* the pool, `SEED`/`N_SAMPLES` are pure downside — they are what let `DATA-6`'s accidental duplicate run happen. **The documented rebuild was broken**: README ran day2 then day4, but day4 samples 250 and tracks progress via `data/chunks/`, which day2 never writes — so a newcomer got ~300 filings while every published number is against 799. Verified non-destructively before deleting anything: 25 random filings re-chunked in memory through the new path give **identical chunk count, text, headings and `n_tokens`**. `--rechunk` over the live corpus would have been the natural check and would have overwritten the thing being checked. That check also surfaced pre-existing drift: stored chunks predate the `standalone` field `chunking.py` gained in `ARM4-2`, so `--rechunk` now rewrites all 799 files to add one key (content-identical) — worth knowing before running it casually. `--reparse` closes a real gap: the old script reused `data/parsed/` whenever it existed, so a `parsing.py` change was silently ignored, the same stale-artifact shape as `company_lexicon.json`. `build_corpus.py` also calls `parsing.load_parsed_blocks` instead of day4's inline copy — exactly the divergence that helper's docstring warns about |
| INFRA-9 | **Pre-commit sweep of the reorganised tree: two data-loss guards, the headline number persisted, and the day tags taken out of the docstrings.** (1) `resolve_gold_evidence.py` hard-fails when `RAW_DATA_DIR` holds no source files, and refuses to write when a run resolves fewer ids than the file already on disk (`--force` overrides). (2) `rerank_score.py` gained `--out`, writing mean *and* stderr for every cell rather than the baseline row alone. (3) 21 non-archive scripts opened with `"""Day N:` after the rename stripped the day from their filenames; the tag is gone, `Arm N`/`stage N` kept because those are structural. (4) README gained the test results table and a warning that `images/results_chart.png` predates `RETR-35`. (5) `data/day8_failure_cases_retr16v2.json` (7.9 MB) and all of `data/label_audit/` (38 MB) ignored | The guard is the one that mattered: with `data/raw` absent every lookup missed, `resolved` came out `{}`, and the old code wrote that straight over the 2.4 MB of gold evidence `eval.py` scores against — a silent total loss with a zero-length symptom. Neither check is a size heuristic: the first says nothing *can* resolve, the second says this run did strictly worse than the artifact it would replace. Persisting the score table closes the gap where the project's headline lived only in a terminal scrollback; running it end-to-end reproduced `RETR-35`'s test row exactly (0.604 / 0.644 / 0.625 / **0.752**), which is also the check that the new README table is right. The chart is marked rather than regenerated because nothing in the repo produces it — `matplotlib` appears in no script, so the numbers behind it cannot be recovered. Deliberately **not** done: the 24-38 line module docstrings in seven `scripts/` files, which are the explanations rule 4 exists to protect **Recall figures here are pre-`RETR-7`; the live pair is 0.760 dev / 0.747 test.** |
| RETR-36 | **Dense / BM25 / RRF candidate generation consolidated into one module, `src/rag_sec/candidates.py`, and `variant` is a required argument there — no default.** The four helpers existed as 11 hand-copied definitions across 5 live files (`retrieve.py`, `arm1_dense.py`, `arm2_hybrid.py`, `company_filter_ab.py`, `rerank_prepare.py`), each hardcoding `variant = 'A'` | This is the duplication that let `RETR-24` — a *missing* `variant` predicate — need the identical fix in seven places. Defaulting `variant` would put the same footgun back one level down, so callers pass `LIVE_VARIANT` explicitly. Behaviour-preserving, proved not asserted: the fully-bound SQL (placeholders substituted in order, so a reordered parameter is caught too) is byte-identical across every old copy and the new helper, for all 5 query shapes, and the RRF tie-break ordering is unchanged. New `scripts/checks/candidate_sql.py` pins that; `variant_predicates.py`, `company_resolver.py` and `atom_replay.py` all still pass. `eval.py`'s `gold_relevant_chunk_evidence_db` was deliberately left out — it is the Arm 4 per-variant pool (`excluded_by_variant`), a different query, and folding it in would mean inventing a shared abstraction for two things that are not the same |
| INFRA-10 | **`--out` on the three finalize scripts that only printed, and the four results files re-derived off the score files already on disk (no GPU, no API).** `slice_budget_sweep.py` and `answer_ab_score.py` gained the flag; `rerank_score.py` already had it and only dev was unwritten. Wrote `day8_retr16v2_dev_results.json` (RETR-29/RETR-31 dev cells), `day8_slice_budget_dev_results.json` (COST-18), `day8_cost13_dev_results.json` (COST-23) | `RETR-29`/`COST-18`/`COST-23` existed only as prose here, while every earlier arm had a `data/*_dev_results.json` — a number with no file behind it is the `SESSION.md` failure mode (reusing a value without its producing job). Flag is optional, so default behaviour is unchanged. All three re-runs reproduce the published numbers exactly (dev `filtered_stripped` recall@10 0.765, matcher survival 84.9% @1500, population delta -8.97%), which is itself the check that the scripts and the score files still agree. Per-question rows go in alongside the marginals: `COST-23`'s McNemar counts and `RETR-31`'s per-cell stderrs cannot be recovered from aggregates **Recall figures here are pre-`RETR-7`; the live pair is 0.760 dev / 0.747 test.** |
| RETR-37 | **Layer 3 diagnosed by locating the true chunk for all 66 layer-3 cases by hand (6 parallel agents, 11 each): it over-labels rather than mislabels, and the binding cause is upstream in Layer 1.** Normalised from raw indices, not the agents' own verdicts: **exact 33 / over-labelled 28 (true chunk inside the assigned set) / flat wrong 5** — 50% not-fully-correct, independently reproducing `RETR-34`'s 54.5% audit signal. `C_absent_from_filing` = **0/66**: every gold text is present verbatim in our chunks. Five named causes — (a) `MIN_ROW_NUMBER_DIGITS=3` empties `gold_nums` for whole classes of rows (percentages, whole millions, day/case counts, and small decimals since `len('4.2'.replace('.',''))==2`), so Layer 1 bails at its first guard and the row's *label* is unreachable because it is only consulted inside the numeric loop; (b) `ROW_NUMBER_MAX_DOC_FREQ=5` rejects 5-6 digit fingerprints a filing legitimately restates 7x; (c) our pipe serialization never shares a `MIN_BLOCK_PAGE=10` contiguous run with the benchmark's markdown tables, so a chunk that *is* the gold table scores 0.03 and survives only as a prose neighbour's cluster-mate; (d) `difflib` alignment is monotonic over the concatenated document, so a reprinted prior-year table consumes the gold page's positions and the true chunk becomes **unreachable, not merely outranked** (repro'd 3x — AAL/MRO/DVN; restricting MRO candidates to index >= 51 returns the correct `{53, 54}`); (e) `CLUSTER_SHARE_THRESHOLD` counts prose volume, so at HIG the spurious chunk contributed 384 matching words against the true chunk's 136 | **The fix list is upstream: 35 of 66 had a usable pointer and should never have reached Layer 3.** Also settles what *not* to build. Requiring each Layer-3 cluster to contain a distinctive gold figure — the fix two agents proposed independently — is dead on arrival: `gold_distinctive_figures` is **empty in 56/66** cases, and where non-empty it can be junk (`convfinqa_2167`'s only "figure" is `201`, from the dataset's unescaped `“`/`”` codes leaking in as literal text — the gold row reads `201clights/ultra lights 201d`). Any future numeric fallback keyed on that field chases a quote mark. Deferred deliberately, not dropped: the per-chunk-share fix needs a threshold with no principled value, and it plus the table-serialization fix are both artifacts of chunk boundaries that `RETR-7` is about to move — tuning them now fits boundaries that will not exist. The monotonic-alignment bug (d) is the exception: it is independent of chunking, since 10-Ks will still reprint prior-year tables after any re-index |
| RETR-38 | **Exempting wide values from `ROW_NUMBER_MAX_DOC_FREQ` was built, measured against `RETR-37`'s hand-verified truth, and reverted — it made labels worse: exact 33 -> 30, flat wrong 5 -> 10.** All five regressions jump to an *earlier* chunk (14/60/15/13) — the front-of-filing "selected financial data" summary. Attributed by running each fix alone: baseline 33/28/5, label-fallback alone 34/28/4, digit-exemption alone 30/26/10, both 31/26/9 | **The premise was right and the conclusion still wrong, which is the part worth keeping.** A 5+ digit figure recurring inside one filing genuinely is a restatement rather than a collision. But the safety argument — "coverage protects us, extra candidates cannot become extra labels" — does not hold: coverage rejects a *redundant* candidate, and a summary table is not redundant, it is a **more efficient cover**. One chunk holding every figure beats two chunks at the real location, so the coverage rule actively *prefers* the restatement over the real evidence. Recorded as a comment at the constant so it is not re-tried. **The companion fix — locating a row by its LABEL when `MIN_ROW_NUMBER_DIGITS` leaves no usable numbers — was also built and also reverted, and its two failures are the more useful lesson.** Placed inside `_table_row_relevant_chunks` it measured *worse* corpus-wide (dev recall@10 0.7653 -> 0.7609, mean gold chunks 1.205 -> 1.234) because a question with several gold rows got label-only matches bolted onto rows that had already resolved numerically — widening a correct label rather than rescuing a missing one. On `RETR-37`'s 66 hand-verified cases the same code looked like a win (exact 33 -> 34, wrong 5 -> 4): **those 66 are selected for having no Layer-1/2 match, the one population where a rescue-only fix cannot look bad.** Repositioned as a true last resort before Layer 3 it stopped hurting and did nothing: dev +0.0008, test **+0.0000**, over 9/1235 and 18/1546 changed labels. The diagnostic that explains why is that unlabelled questions stayed 0 -> 0 and 1 -> 1 — Layer 3 already labels every one of these, so the fallback only ever *substitutes* for Layer 3 and there is no unlabelled population to recover. Reverted rather than kept on a structural argument with no measured gain; `eval.py`'s diff for this session is comments only |
| RETR-7 (built, dark) | **The heading bug fixed without moving a single chunk boundary: `_pack_atoms` now records the heading each atom sits under when the atom is buffered, and labels each chunk with every section it spans.** Damage re-measured at the atom level over all 799 filings: **45.9% of atoms sat under a heading they did not come from**, and on a 40-filing subsample **54.7% of those were an unrelated topic** (difflib <0.30 against the correct heading) — cover-page text emitted as `# ITEM 1. BUSINESS`. Four alternatives were built and measured over 60 filings before this one was picked, all rejected on evidence: *label by the chunk's first body atom* (zero churn, but only 51.7→40.8%, because 35.2% of chunks genuinely span two sections); *blank the heading when a chunk is mixed* (**worse, 57.1%**, and it deletes the signal on a third of the corpus); *flush at every title* (0.0%, but +21.6% chunks and it moves 45% of boundaries); *flush + heading-aware fold-back* (0.0%, but **28.1% of chunks fall under 200 tokens** and the median drops 862→440). Multi-heading gets the same 0.0% at +0.0% chunks, paying in label dilution instead: 35.2% of chunks carry 2+ headings | **Boundary preservation is the whole point, and it is enforced, not hoped for.** `eval.py` stores no labels — it recomputes them from chunk *text* every run and returns bare chunk indices — so moving boundaries would renumber every gold label and make the new numbers incomparable to every published one. Two things had to be engineered for that: the fold-back thresholds on a count that *includes* the heading line, so it is now evaluated against a `legacy_heading` that follows the pre-fix rule and is never emitted (without it, 3/60 filings moved). `scripts/checks/heading_fix.py` asserts all four properties — flags off is byte-identical to the pre-fix packer (**799/799 filings**), flags on preserves every boundary (**799/799**), misattribution 45.9%→0.0%, and **gold labels unchanged on 300/300 dev questions, 0 lost, 0 gained**. **No recall claim is attached**: `RETR-33` measured `no_gold_chunk` = 0% twice, and the heading is a median **0.91%** of a chunk's tokens (mean 2.37%), so there is very little surface to move retrieval through. Shipped as a correctness fix with a measured-zero expectation. The 0.0% is partly by construction — an atom counts as correct if its true heading is *among* the chunk's labels — and dilution is the untested cost |
| RETR-8 (built, dark) | **Page furniture no longer becomes a heading, by structural class rather than by blocklist**, applied at the blocks→atoms boundary so it needs **no re-parse** — only the re-chunk we already owed. Shares of the corpus's 122,524 title instances: EDGAR typesetter metadata (`ZEQ.=/SEQ=/CP=/EFW=`) 3.41%, `/s/` signature blocks 0.28%, titles with no letters at all 0.70% (phone numbers), strict page labels, and running headers. Furniture atoms **keep `is_title`**, so they still trigger the packer's flush check and boundaries do not move; they simply never set the heading, and the real section heading they used to clobber survives them | **The frequency rule is the one worth defending, because the obvious version of it is wrong.** "A title repeating N times in one filing is a running header" deletes *real* financial-statement headings at N≥3 — `income taxes` repeats ≥3x in 69 filings, `revenue recognition` in 19, `condensed consolidating statement of cash flows` in 18. At **N≥10** all 49 distinct strings in the corpus are furniture, every one of the form `table of contents <company> notes to consolidated financial statements — (continued)`. The page-label regex was tightened for the same reason: `[A-Z]{0,3}-?\d+` also swallows the exhibit index (`EX-1`…`EX-8`) and accounting references (`FIN 48`, `AB5000`), which are content. `PART I`/`PART II` (2.33%) were deliberately **left in** — non-discriminative, but genuine structure, and dropping them has no evidence behind it. **Ordering is not free: RETR-8 cannot follow RETR-7.** `retrieve.py` strips company framing from the query for the *reranker only* — `dense()`/`bm25()` still see the full question — so RETR-7's extra headings would push more company/filing boilerplate (6.2% of titles, p90 19.1% per filing, max 82.7%) into the text scored by an unstripped query, at the exact stage `RETR-33` says is 88% of remaining failures. Both flags therefore ship together |
| INFRA-11 | **Both fixes default OFF behind `RAG_SEC_MULTI_HEADING` / `RAG_SEC_STRIP_TITLE_FURNITURE`, flippable from the environment with no code edit.** Off is byte-identical to the pre-fix packer, verified over 60 filings and re-verified corpus-wide by `atom_replay.py`: **99,654/99,654 chunks (100.00%) replay byte-for-byte** | Not caution for its own sake — `rag_sec.compress` replays this packer from `data/parsed/` and requires it to reproduce the already-built `data/chunks/` exactly. Turning either flag on without a re-chunk would make compression select atoms from a different document than the one that was embedded and retrieved, silently. The flags make the fix inert until the re-index cycle and make the A/B a one-variable change rather than a revert **Superseded by `RETR-39`: both flags now default ON (`chunking.py`), so the byte-identity that matters is flags-on against the post-re-index `data/chunks/`, which no row records re-verifying — re-run `atom_replay.py` before trusting `compress`. Same caveat applies to `COST-5`'s replay claim.** |
| COST-29 | **`service_tier="flex"` is documented GA and was unusable in practice: ~9 of every 10 requests came back 503 "high demand", so `COST-13` was sent at `standard`. `answer_ab_run.py` now defaults to standard and the module docstring says why.** Confirmed from disk on 2026-09-04, since the stored rows carry no `tier` field: latency p50 **2.74s** (max 8.74s) against flex's documented 1-15 min target, and the run prices to **$2.22** at standard against `COST-24`'s dashboard **$2.23** — flex would be $1.11 | A 503 is capacity, not a malformed request: flex is *best-effort by design*, so this is the documented failure mode arriving at a rate that makes the tier unusable, not a bug. **`COST-19` was right about the docs and wrong about the traffic** — a GA label states that a feature exists, not that there is capacity for your key. Two consequences. (1) **Batch, not flex, is the 50% route.** Same discount, and they do not stack: batch is asynchronous and flex synchronous, so they are mutually exclusive modes. (2) Batch additionally carries the extended rate limits, making it the right tool for a full pass — at ~11.6k input tokens/question, Tier 1's 3M enqueued-token cap needs ~5 sequential jobs for dev, ~11 for dev+test; Tier 2's 400M cap needs one, so **check the key's tier in AI Studio before designing the runner**. Use the JSONL `key` format rather than inline requests (inline relies on positional ordering) and check `failedRequestCount` — `PARTIALLY_SUCCEEDED` is a terminal state, so a finished job does not mean every row landed |
| COST-30 | **`COST-12` closed, not built: do not merge `judge` into `answer`. The question worth asking is whether the loop belongs on single-document filings at all.** | Three findings, 2026-09-04. (1) **The 22% saving assumed a loop rate of zero.** Measured on the 278 stored `COST-13` responses, the insufficiency rate on this task is **15.1%** (uncompressed arm; 36.0% at slices@1500), against a break-even of ~15-25% — on a looping question the merge pays 3.7-flash for an answer it discards, so it is a coin flip, not a win. (2) **On T²-RAGBench itself** (arXiv:2604.01733), CRAG's corrective loop scores recall@5 **0.658** against hybrid+rerank's **0.816** — the looping arm loses to the arm we already ship, which upgrades `SESSION.md`'s bare assertion to a cited number on our own benchmark. (3) **A judge can destroy correct answers**: Joren et al. (arXiv:2411.06037, ICLR 2025) measure models answering correctly **35-62% of the time under insufficient context**, so looping on 15.1% discards answers that were often already right. Also settles what *not* to build: Gemini logprobs are withdrawn for 3.x (disabled Mar 2026, absent from the 3.7-flash capability table) and saturate >0.999 under structured output; self-consistency tops out at ρ=0.31, below the ρ≈0.19-0.22 `COST-12` already rejected reranker scores for; and **caching cannot help because a Gemini cache is per-model** — judge on 3.1-flash-lite and answer on 3.7-flash can never share one. Prompt-merging is unmeasured in the literature; the SOTA that removes a judge *trains* the decision in (Self-RAG, Search-R1) rather than prompting for it, and the systems that keep one (S2G-RAG, REVEAL) make it return a **gap description**, not a boolean — if the judge stays, that is the change worth making. **Not deleted outright:** iteration's published wins are multi-hop composition across documents, which is Day 9's question, not Day 8's. Near-miss worth recording: FinAgent-RAG (arXiv:2605.05409), the most citable evidence *for* an agentic loop on financial table QA, was **withdrawn 2026-07-05** for methodological errors — never cited here, but it is the paper an interviewer would raise **Reason (1) is retired: `COST-36` removes cost as a constraint and `AGENT-13` forbids transferring the 15.1% one-shot rate to a loop. The decision stands unchanged on reasons (2) and (3).** |
| COST-31 | **`answer_ab_run.py --thinking {low,medium,high}` ships; nothing sent. The finding that motivated it: `gemini-3.7-flash` defaults to `thinking_level=medium` and the script passed no thinking config, so every cost figure in this log was billed at medium.** Reproduced from disk: **660.8 thinking tokens/call against 109.3 visible — 86% of billed output** (independently re-deriving `COST-24`'s 662 from a second file) | Three choices worth defending. (1) **`thinking_level`, not `thinking_budget`.** Both exist in `google-genai` 2.21.0, but the numeric budget is the Gemini 2.5-generation control with **no published range for 3.7-flash** and a documented performance warning on Gemini 3; `thinking_level` is the 3.x path. 3.7-flash accepts low/medium/high only (`minimal` 400s) and **thinking cannot be disabled at any level** — the floor is irreducible, not removable. Correcting a live error in the code: `agent.py`'s `thinking_level="minimal"` on `gemini-3.1-flash-lite` is a **no-op**, since that is already that model's default. (2) **`thinking` is part of the checkpoint key**, not merely recorded, because a second reasoning level is a new arm rather than a rerun. Rows written before the flag carry no `thinking` field and backfill to `medium`, which is what they actually ran at — so **the 278 stored rows become the `medium` arm at zero cost** and only `low` needs paying for. (3) **The estimate is knowingly wrong at low**: the 772-token output constant is a medium measurement and Google publishes no per-level counts, so the script prints a warning rather than pretending to price a level nobody has measured. Also fixed here: `_call` rebound its own `prompt` argument to an int, so a raise after that line would have retried with an int as `contents`. **Unspent caveat:** the medium rows are from 2026-09-02, so reusing them puts any API drift entirely in one arm — the confound this script's interleaved ordering exists to avoid |
| INFRA-12 | **The `RETR-7`/`RETR-8` re-index is an incremental re-embed of 47.5% of the corpus, not a full one — measured, not estimated.** Because boundaries are preserved, `(filing_stem, chunk_index, variant)` — already the UNIQUE key in `store.py` — still identifies the same body text, so only chunks whose *text* changed need a new vector. Over all 799 filings: chunk count identical (**99,654, 0 filings with a mismatch**), **52,342 chunks (52.5%) keep their existing embedding**, 47,312 (47.5%) need a new one — 65.1% of those because a wrong heading was replaced, 34.9% because a second heading was added. The BM25 index still needs a full rebuild, but that is Postgres/`pg_search` work, no GPU | **This is the payoff for refusing the flush variant.** Under `flush-on-title` every chunk index shifts, so 100% of the corpus needs re-embedding *and* every gold label has to be re-derived; under boundary preservation it is a keyed `UPDATE` of just over half. It also means the re-index does not have to be one atomic cycle — it can be resumed, or run filing-by-filing, because unchanged rows are already correct. **No GPU-hour figure is quoted here on purpose**: `SESSION.md` records that `RETR-30`'s 4.3 GPU-h was mis-anchored (it was `RETR-22`'s chunk-level figure) and no durable artifact on disk gives the embed pass's wall time. Re-derive it from the last embed log before scheduling. The only durable anchor on disk is the **day-5 HPC embed payload: 26,737 chunks**, so this pass is **1.77x** that run — scale from whatever that one took. Runs as the standard three stages (`--prepare` -> `embed_hpc.py` **unchanged, its payload format already matches** -> `--load`), because GPU work belongs on HPC and needs no DB access (ARM3-2). The load stage drops and rebuilds the HNSW and BM25 indexes around the bulk UPDATE. **Recorded because it was a real mistake:** a first attempt ran the embedding locally on MPS and measured 1.4 chunks/s, a 9.3-hour ETA; it was aborted after 382 of 47,312 rows. Those rows are included in the payload by construction — the changed set is computed by diffing new `data/chunks/` against the pre-fix backup, never against DB state — so every vector still comes from one GPU pass |
| COST-32 | **Batch API wired and proven end to end on a real 10-request job: `scripts/compression/answer_batch_run.py`, `JOB_STATE_SUCCEEDED`, 10/10 rows returned, 0 failed, $0.0325 actual against $0.03 estimated, turnaround well under the 24h SLO.** Separate script rather than a `--batch` flag on `answer_ab_run.py`: the control flow is disjoint (loop-over-calls vs split/upload/submit/poll/download/reconcile) and so are the failure modes, but the two share an output contract so `answer_ab_score.py` reads both unchanged | Four choices worth defending. (1) **JSONL with an explicit `key`, not inline requests** — inline results join to inputs *by position*, and since `JOB_STATE_PARTIALLY_SUCCEEDED` is a real terminal state, one dropped row would silently shift every later answer onto the wrong question. Key is `id|arm|thinking`, the same triple that keys resume. (2) **Failed rows are counted and left unwritten**, never written blank: an absent row stays in the resume set and is retried, whereas a blank scores as a wrong answer and biases whichever arm it landed in. (3) **Greedy job splitting, not optimal bin-packing** — the objective is staying under Tier 1's 3M enqueued-token cap, and optimal packing would make job boundaries depend on the whole set, so appending one question could reshuffle every job and break resume. `--max-enqueued 400000000` makes it one job on Tier 2. (4) `service_tier` deliberately unset inside jobs: batch already prices at the flex rate and no primary source says the field is honoured there. **Also fixed:** `answer_ab_score.py` priced only `{"flex"}` at 50%, so batch rows would have been billed at full price — the same under-report class as `COST-23`'s wrong $1.53 |
| COST-33 | **`--limit` is a wiring check, not a sample, and the 10-request batch pilot proves why: `data/day8_cost13_payload.json` is sorted 89 `A_gold_lost` then 50 `B_gold_kept`, so every `--limit` below 89 returns a 100% stratum-A sample.** Both runners' `--limit` help now says so | Caught by reading the pilot's 0/10 correct rather than accepting it: stratum A is the arm where gold was *deliberately removed*, so 0/10 is the expected result there and would have been badly misread as a thinking-level regression. **The medium-vs-low pilot is therefore not a result** — 10 paired requests, all stratum A, thinking 518.6 -> 396.7 tokens (-23.5%) with 8/10 pairs lower, but the 95% CI on a 10-sample thinking mean is +/-247 tokens and the observed -122 sits well inside it. Of the 10 pairs, 6 were refusals on both sides, so only 4 carried a number to compare; those 4 agreed. `COST-27`'s lesson applies directly — its n=30 pilot sign-flipped at n=300 — so this is logged as a wiring proof and a hypothesis, not a measurement. A real medium/low comparison needs the full 278 and should wait for the `RETR-7`/`RETR-8` re-index, which rewrites every prompt |
| INFRA-13 | **BGE-M3 embedding throughput on the HPC V100, measured — the wall time this project has quoted around but never had.** `sacct` on job 9949705: allocation `2026-09-04T05:07:11 -> 06:23:31`, **Elapsed 01:16:20**, `gres/gpu=1`, `cpu=4`, `mem=48G`. Inside it, `embed_hpc.py` wrote all **47,312** chunks (1,078,929,606 bytes) by 06:21, so the embedding itself was **~72 minutes ≈ 11 chunks/s** at `BATCH_SIZE=64`. Extrapolated, a full 99,654-chunk corpus pass is **~2.5 GPU-h** | **Recorded because its absence has been a standing provenance hole**, alongside the 9.4x loop multiplier with no source and `RETR-30`'s mis-anchored 4.3 GPU-h. Anyone sizing an embed run can now scale from a real figure instead of a remembered one. Two caveats kept deliberately: the 01:16:20 is the *allocation*, which includes model load and the ~2 min of idle before `scancel`, so 72 min is the embedding and 76 the booking; and this is a V100 at ~900-token chunks — it does not transfer to the reranker, which is a different model on different input. **Counter-anchor worth keeping next to it:** the same job on this laptop's MPS measured **1.4 chunks/s**, ~8x slower and a 9.3-hour ETA, which is why embedding belongs on HPC (`ARM3-2`) |
| RETR-39 | **The `RETR-7`/`RETR-8` re-index was executed end to end and every arm re-measured. The verdict: a corpus-wide correctness fix with no measurable effect on retrieval, exactly as forecast.** Applied 2026-09-04: 47,312 of 99,654 chunks re-embedded (`INFRA-12`), boundaries and chunk count unchanged, gold labels unmoved. **All four Arm 3 cells are directly comparable to the published ones — both sides on `RETR-35` corrected labels, only the corpus differs.** Dev: Arm 3 0.634 -> **0.629**, filter+strip 0.765 -> **0.760** (recall@50 0.806 -> 0.802). Test: filter+strip **0.752 -> 0.747** recall@10, 0.787 -> 0.785 recall@50, nDCG 0.621 -> 0.617. **Every cell moves -0.002 to -0.005, inside the +/-0.012-0.013 stderr.** The superadditive interaction replicates on an independently re-embedded corpus: dev filter +0.040 / strip +0.017 / together **+0.131** against an additive 0.057 (**2.3x**, was 2.4x); test +0.037 / +0.025 / **+0.140** against 0.062 (**2.3x**). `unfiltered_stripped` moves recall@50 by exactly **+0.000** on both splits while moving recall@10 — a rerank-only change reordering the top without touching the pool, which is the internal consistency check that the cells are wired correctly | **The prediction was made before building and it held.** `RETR-33` measured `no_gold_chunk` = 0% twice and the heading is a median **0.91%** of a chunk's tokens, so ~0 was the stated expectation; -0.005 is that. **Do not retroactively reframe this as a recall play because a number moved.** What it bought is real but different: 45.9% of atoms no longer sit under a heading from a different section, and 54.7% of those were an unrelated topic. **Also resolved: Arm 1 and Arm 2 are finally off the old labels** — the last two rows `SESSION.md` flagged as un-rescorable are now measured on the corrected labeller (dev **0.337/0.473** and **0.514/0.708**). Their comparison to the published 0.329/0.495 stays **confounded** (corpus *and* labels changed at once) and no delta should be quoted for them; `data/retr7_ANALYSIS.md` records how to get the missing cell from the pre-RETR-7 `pg_dump`. **Where the numbers live:** `data/day3_arm1_dev_results.json` and `day4_arm2_dev_results.json` were overwritten in place and now hold the post-RETR-7 values — the old-label ones survive only in git history and in this file. Arm 3 is in `data/retr7_arm3_{dev,test}_results.json`. **Two verified `pg_dump`s sit outside the repo** in `~/rag-sec-backups/`: `chunks_pre_retr7_20260904.dump` (652 MB, the old corpus, and the only cheap route to the missing Arm 1/Arm 2 cell) and `chunks_post_retr7_20260904.dump` (655 MB, the current corpus). The second was taken specifically so `retr7_embed_results.jsonl` could be deleted — that file was 72 minutes of GPU and is not cheaply regenerable, so it needed a home first. **Not restored:** Arm 4-B/C (deliberately left on pre-RETR-7 headings, no longer text-comparable with A), `RETR-33`'s triage, and every `COST-*` number |
| COST-34 | **`thinking_level=low` measured on the full 278: thinking tokens fall 35.3% (662.0 -> 428.2/call) for a 1.5-point accuracy drop that is not significant (4 discordant pairs, `p=0.13`).** Both arms re-run today via Batch so neither carries a stale date. Per-pass, standard tier: uncompressed $13.17 -> **$12.03** (-8.7%), slices@1500 $4.82 -> **$3.72** (-22.8%) | **This is the first thing that moves `COST-24`'s floor.** That row established thinking as a hard ~$3.9/pass cost compression cannot touch, because it stays near-constant as the prompt shrinks (694 uncompressed vs 627 at slices@1500). `low` cuts the floor itself by a third, and the saving is **orthogonal to compression** — near-identical in absolute dollars on both arms ($1.14 and $1.10), which is exactly what a per-call constant should do. Stacked with batch, slices@1500+low is **$1.86/pass** against uncompressed+medium+standard's $13.17. It also revises `COST-1`, which sized thinking-level tuning at ~4% and dismissed it as "not the lever": that estimate predates `COST-24` and priced only *visible* output, which is 14% of billed output. **Three caveats, none optional.** (1) **The accuracy result is underpowered, and the direction is unfavourable**: all 4 discordant pairs went medium's way, 0 to low. `p=0.13` means no detectable harm at n=278, *not* that low is free — and `COST-27`'s n=30 pilot sign-flipping at n=300 is the standing warning against reading a null as a green light. (2) **The prompts are the 2026-09-02 pre-`RETR-7` payload.** The medium/low delta is clean because both arms share it, but every absolute `$/pass` here is pre-re-index and must not be quoted beside `RETR-39`. (3) **Absolute accuracy (49.3%/47.8%) is not dev accuracy** — the sample is deliberately stratum-A-enriched (89 of 139 are gold-lost), so only the paired within-cell delta is meaningful. **Decision: default stays `medium`; `low` is for runs whose numbers are never published** (sweeps, wiring checks, pilots). The saving is **$1.1/pass in absolute terms** on either arm — the 8.7%/22.8% percentages only diverge because the compressed denominator is small — and that is the figure the decision turns on, not the percentage. The bet is asymmetric: one side is the project's headline accuracy, the other is a dollar, and the accuracy evidence points the wrong way. **Not worth resolving further, and the arithmetic says so:** at a 1.4% discordant rate you would need ~7 one-way discordants for `p<0.05`, so ~490 paired cells (~1.75x this run, ~$3.60 batch) — affordable, but it buys a $1.1/pass decision that break-evens at ~4 passes, and if the effect is genuinely null the discordants split evenly and no n ever resolves it. Measured and parked. **What it points to is bigger than the decision:** the uncompressed bill is ~64% input, ~30% thinking, ~5% visible output; compression attacks the input at 9-11 accuracy points, `low` attacks the thinking floor for $1.1, and there is no third lever. Both known levers now carry measured price tags, which is the signal that **cost work is finished** — go back to `spec.md`. **Drift, now measured rather than feared:** re-running medium today against the stored 2026-09-02 rows moved stratum A not at all (identical, 25/1) and stratum B by one question. `SESSION.md`'s worry was worth $1.01 to retire, and the answer is that it was negligible |
| COST-35 | **Batch's 3M enqueued-token cap is account-wide across concurrent jobs, not per job: two 1.63M submissions two seconds apart 429'd the second at `batches.create`. Sequential submission of the identical file succeeded.** Nothing was billed for the failed call — the 429 fires before the job exists | `COST-32`'s `--max-enqueued` splits *within* one invocation, so it cannot see a sibling job and two invocations each "under the cap" can still exceed it together. Read as a leading hypothesis, not proof: a concurrent-*job* limit of 1 fits the same evidence, and the key's tier was never confirmed in AI Studio (`COST-29` asks for exactly that check and it still has not been done). The operational rule is identical either way — **run arms sequentially and treat a level comparison as same-day, not simultaneous.** Cost of the misdiagnosis if it matters later: an arm gap of ~7 minutes here instead of 0 |
| COST-36 | **Budget envelope stated: ~$30 for the whole project, against a measured $14.69 to run dev+test uncompressed on batch at `medium` (`COST-34`'s rates, one answer send each). Cost is no longer a design constraint.** Checked against every prior cost decision: **none flips** | The full grid, batch, both splits (2,781 questions), evidence tokens / gold survival / cost: uncompressed 9,834 / 93.3% / **$14.69**; slices@3000 2,994 / 88.1% / $7.10; @2000 1,994 / 86.1% / $5.98; @1500 1,494 / 84.9% / $5.43; @500 494 / 74.8% / $4.32. **So compression's entire realistic saving across the whole benchmark is ~$9, for 5-18 points of gold survival and `COST-13`'s measured 9-11 points of answer accuracy.** Not worth slicing at any budget, including the 3000 that looks like the sensible compromise. **Why compression ever looked compelling: `COST-1` sized it against a projected multi-pass agentic workload; at 2,781 one-shot questions the denominator is too small for a percentage to mean anything.** Two consequences. (1) **Cost arguments should be dropped from decisions that also have accuracy arguments** — `COST-30`'s reason (1) (the merge is a break-even coin flip) is now moot, but the decision stands unchanged on its reasons (2) and (3), which are about the loop losing to hybrid+rerank and about judges discarding correct answers. Demote the cost leg, do not re-open the decision. (2) **Batch's justification is now rate limits, not price**: the 50% no longer matters and it costs a 24h SLO, so standard is the better call for small jobs; batch still earns a full-split pass because flex/standard traffic counts against normal RPM/TPM (`COST-19`). **Stated assumptions:** test per-question evidence length is assumed equal to dev's and has never been measured; output tokens at budgets 2000/3000 are interpolated between the two measured points (825/call uncompressed, 718 at slices@1500), safe only because thinking is near-constant against prompt length (`COST-24`). **The one place prompt size still has leverage:** Arm 6 sends the evidence block twice (judge, then answer), so a Day 9 loop over a full split roughly doubles the input side **Understated ~11.8% (`COST-39`): priced off 9,834 *evidence* tokens against 11,621 measured *prompt* tokens, so quote ~$16.42 and scale every grid row likewise. Conclusion unchanged — still inside the ~$30 envelope.** |
| COST-37 | **`COST-35`'s hedge removed: the 429 was the enqueued-token cap, and the competing "concurrent-job limit of 1" is contradicted by the docs.** Verified 2026-09-04 at ai.google.dev/gemini-api/docs/rate-limits: the cap is **per model, per project, summed across active jobs**, and Tier 1 (confirmed as ours) is **3M for `gemini-3.7-flash` but 10M for `gemini-3.1-flash-lite`** — 2x1.63M = 3.26M clears 3M, which is the whole of `COST-35`'s observation. Documented **concurrent batch requests = 100**, so job count was never the constraint | Three operational consequences. (1) **`--max-enqueued` must be per-model, not one global constant** — the current single cap under-fills every flash-lite job by 3.3x. For a Day 9 wave plan on dev this is 14 sequential jobs, not 17. (2) **The quota is a semaphore over *active* jobs, not a daily budget** — doc wording is "across all your active batch jobs", so a completed job frees its allocation and waves can chain within a day. **Caveat, and it is not documented:** Google never states the release trigger; the supporting evidence is a cross-vendor OpenAI analogy, and 2026 forum reports of jobs stuck in `PENDING` past 26h imply a stuck job keeps holding its allocation. (3) **A 429 cannot be classified from the error body** — no `quotaId`/`quotaMetric` string distinguishes a token-cap breach from a concurrency breach (forum threads show only a generic "exceeded your current quota", no staff resolution), so the cap stays preventative and cannot be caught-and-retried by reason. **Turnaround remains the real risk, not price:** the 24h figure is an SLO with no published median; 2026 forum reports cluster at 2-3h typical but include >26h stalls, and `batches.get()` returns coarse state only — no percent-complete, no ETA. **Tier 2 ($100 cumulative spend + 3 days) raises 3.7-flash 3M -> 400M**, collapsing the same plan to 9 jobs; worth re-checking after Day 9's spend |

## Day 10 observability — tracing

| # | decision | why |
|---|---|---|
| OBS-1 | **Langfuse Cloud (Hobby, free) over self-hosting.** 50k units/month, 30-day retention, 2 users | Two hard constraints, not a preference. (1) **Self-hosting does not fit this machine**: Langfuse v3+ needs Postgres 15+, ClickHouse 25.12+, Redis 7+, S3/MinIO and two app containers — documented at 4+ cores, **16 GiB RAM and ~100 GiB storage**, with ClickHouse alone wanting 8 GiB. This laptop is an M3 with **16 GiB total and 25 GiB free disk**, Docker allotted 8.2 GB, and it must simultaneously hold the bge-m3 reranker on MPS for 4.4 hours — the same MPS path that already killed the machine once (`INFRA-6`). (2) **Day 12 puts the app on App Runner/ECS, which cannot reach a collector on this laptop.** Self-hosting would mean also deploying that six-service stack to AWS, breaking both the ~10h Days 12-13 budget and the $40 project total. Cloud is the same two env vars locally, in Fargate, and from a GitHub Actions runner. **The caveat is retention:** Hobby keeps traces 30 days, so Day 14's README screenshots must be captured during Day 10, not at the end. Data sensitivity is a non-issue — the corpus is public SEC filings and the questions are an open benchmark |
| OBS-2 | **Hand-written spans behind `src/rag_sec/tracing.py`, not `langfuse.langchain.CallbackHandler`.** That module is the only place `langfuse` may be imported | Three reasons, and the bug is the weakest of them. (1) **A LangChain callback only sees LangChain components.** `embed`, `resolve`, `search` and `rerank` are our own code in `retrieve.py`, and the reranker alone is ~94% of the run's wall clock — the single most important thing to trace would have been invisible. `spec.md:387` wants spans through *every* stage, so manual instrumentation was required regardless; the handler would only have added a second overlapping mechanism. (2) **The handler hard-requires the `langchain` umbrella package** purely as a version sentinel — verified by reading `langfuse/langchain/CallbackHandler.py` in the shipping 4.15.1 wheel, where on LangChain v1 every real import comes from `langchain_core` and the sentinel raises `ModuleNotFoundError` otherwise. This project installs `langchain-core` and not the umbrella. Upstream `langfuse/langfuse#13651`, open since 2026-05-15. (3) We must name our own attributes anyway — see `OBS-3`. **Cost of the choice:** the LLM span attributes the handler would have given free are hand-written, though `_record_usage` already extracted exactly those counters. **Vendor coupling is one file**, so a later move to self-hosted Langfuse, Jaeger or Grafana is a wrapper change — *not* the env-var swap a raw-OTel design would have given, which is the price paid for Langfuse's typed `generation` observations doing token→cost mapping |
| OBS-3 | **`spec.md` drift: "OTel GenAI spans" cannot be pinned, so we emit Langfuse's typed observations instead of raw `gen_ai.*` attributes.** Checked 2026-09-04 | The GenAI semantic conventions are **not stable**. At semantic-conventions **v1.42.0 (2026-06-12)** every GenAI convention was deprecated out of the main repo and moved to `open-telemetry/semantic-conventions-genai`, which **has no tagged release** — so there is no versioned schema URL to pin against, and hand-emitting `gen_ai.*` names would be coding to a moving target that Langfuse's ingest maps by convention. `spec.md` Day 10 is written as though a stable standard existed; it does not. Spans are still OTel underneath (the Langfuse SDK v4 is OTel-based), so this is a choice of attribute vocabulary, not of protocol |
| OBS-4 | **Tracing is fail-open and fully no-op without keys.** No keys ⇒ `langfuse` is never imported; any tracing error degrades to no-op for the process after logging once | The consumer is a **4.4-hour, ~$11.50, one-off** live-API run whose output feeds Days 9-13, so re-running it is real money. A Langfuse outage, a rate limit or one bad attribute must not be able to kill it — observability is not allowed to be a new failure mode for the thing it observes. The no-op path is equally load-bearing: every existing offline script imports this pipeline, and the Day 11 CI gate must run with no keys and no network. Logged once rather than per span because a broken backend would otherwise flood a 200-question run's stdout |
| OBS-5 | **`plan` and `judge` raw completion text is now stored in the results row. Prompts deliberately are not** | The metrics half of Day 10 was always reconstructible offline from `stage_latency` — durations are all there and the graph is strictly sequential. **The debugging half was not, and that was unrecoverable without paying for the run twice.** `_record_usage` kept only token counters; `judge_node` collapsed the model's entire response to the string `finish`/`loop`; `plan`'s reasoning survived only as the tool-call argument. So *"why did the judge call insufficient evidence sufficient?"* — the highest-value question about this arm, and the one `COST-30` turns on — was unanswerable from the file at any effort. Prompts stay out because the judge and answer prompts each embed the full evidence block (~10k tokens) and are reconstructable from the stored chunks plus the fixed template; the answer completion is already `final_answer`. **This is stored in the JSONL, not only in traces, precisely because Hobby retention is 30 days (`OBS-1`) — the file is the durable artifact and the dashboard is not** |
| OBS-6 | **`_gpu_lock` wait time split out from GPU compute**, as additive `embed_lock_wait_s`/`rerank_lock_wait_s` keys; `total_s` unchanged | Both waits previously sat inside `embed_s`/`rerank_s` indistinguishably from real compute, so at concurrency >1 a queued stage and slow hardware look identical — and since the reranker holds this lock for ~94% of wall clock, that is exactly the measurement a future concurrency decision would need. Measures ~1e-6 s at concurrency 1, which is the expected null and the point: the number is now attributable rather than absent. The new keys are strict subsets of their parent stages and are **never summed into `total_s`**, which keeps every latency figure in the Day 9 JSONL comparable to the pre-instrumentation ones **Superseded by `AGENT-17`(6): first-call model construction moved out of `embed_s` into `model_init_s`, so pre- and post-fix `embed_s` — and first-question `total_s` — are NOT comparable.** |
| OBS-7 | **Langfuse pinned at `4.15.1` exactly, and four SDK behaviours worth recording because none is in the quickstart** | (1) **`client.flush()` blocks the caller with retry backoff — measured ~9s against a closed port.** It now runs on a daemon thread joined with a budget, and the budget is deliberately **two values**: 3s for the `atexit` hook, which fires on every interactive script and must never hang one, and 30s for the explicit end-of-run flush, which is called once after ~4h and holds the only copy of the un-exported tail — giving up early there would discard exactly what the flush exists to save. (2) **A silently failed export is invisible**: OTel's `force_flush` reports failure by return value and Langfuse's `flush()` drops it, so *no error means nothing*. The only proof traces actually arrive is looking at the dashboard, which is why a 2-question pilot runs before the real pass rather than after. (3) **`usage_details` sub-categories are priced as exclusive**: `input_token_details.cache_read` maps to `input_cache_read` and Langfuse's own handler does `input = max(0, input - cached)`, so passing langchain's inclusive `input_tokens` unmodified would bill cached tokens at the full input rate on top of the cache-read rate. Verified by reading `_parse_usage_model` in the shipping wheel, not from docs. (4) **`update()` swallows unknown kwargs silently**, so a typo'd attribute name would simply never appear — the wrapper allowlists the real Langfuse fields and folds everything else into `metadata` rather than trusting the call. Also: `create_trace_id` is a `staticmethod` on `Langfuse`, not module-level as the docs read. **Pinned exactly, unlike the `>=` used for non-numeric deps**, because a 4.x minor renaming an observation method would fail mid-run, and this is the one dependency whose breakage is invisible until the traces are missing |
| OBS-8 | **`trace_id` written into every results row**, derived from the same `create_trace_id(seed=qid)` the trace itself uses | Day 10's task is to read the ten worst failures out of the JSONL and then open each one's trace; without the id in the row that is a manual search through 200 traces by question text. Seeding on the question id rather than storing a nonce means the join survives a resumed run — a retried question reuses its trace id instead of forking a second trace. **The id deliberately outlives what it points at:** Hobby retention is 30 days (`OBS-1`), so after that the row still records which trace existed and the JSONL remains the durable artifact (`OBS-5`) |
| OBS-9 | **Instrumentation restructured against Langfuse's own best-practices guidance (installed as a skill from `github.com/langfuse/skills`, MIT, commit `fc57426`). Four gaps found, all fixed and verified by fetching the traces back.** Permanent observation names: `answer-question` (root), `plan-query`, `retrieve-chunks`, `embed-query`, `resolve-company`, `search-candidates`, `rerank-candidates`, `judge-sufficiency`, `generate-answer` | (1) **The loop arm and the paired static arm shared one trace, so trace-level cost was the two arms summed** — which makes the one comparison this whole run exists for unreadable. Now one trace per arm, grouped by `session_id` = the question id, with disjoint `arm:` tags. Verified on the wire: **loop $0.08804 vs static $0.01108** where the single trace previously reported only their $0.099 total. (2) **Observation types were all `span`/`generation`**; retrieval is now `retriever` and the query embedding `embedding`, which is what drives the Agent Graph and per-type filtering. **The judge deliberately stays `generation` and not the semantically-correct `evaluator`:** only `generation` and `embedding` accept `model`/`usage_details` in 4.15.1, so typing it `evaluator` would have silently dropped 18% of loop spend from every cost figure. (3) **No trace-level input/output** — the root observation feeds the tracing table, evaluators and dataset experiments, so both rendered empty; root input is now the question and output that arm's answer. (4) **The root was named with the question id**, the exact high-cardinality anti-pattern the docs warn against, since names are targeted by evaluators and dashboards and silently stop matching when they change. The qid moved to `session_id` + metadata. **Names are now treated as a frozen API.** No masking layer: the corpus is public SEC filings and the questions an open benchmark. **Bug found only because the traces were fetched back:** Langfuse merges propagated tag lists downward, so the static trace opened inside the loop's context inherited `arm:loop` too — `question_trace` now enters under an empty OTel context, asserted by check (g). **A silently failed export is invisible (`OBS-7`), so "no error" proved nothing; every claim here is from a re-fetch via `langfuse-cli`** |
| OBS-10 | **Where Arm 6's cost actually goes, measured on 3 piloted questions: `answer` 47%, `plan` 36%, `judge` 18%, at a 3.6x multiple over the static arm** | **This corrects the mental model `COST-36` left behind**, which said the loop "sends the evidence block twice (judge, then answer)" and implied ~2x. It is three times, and the third is the expensive one: **`plan` replays the entire accumulated message history including every prior tool result**, so its input grows 157 -> 13,842 -> 26,012 -> 37,284 tokens across four iterations to produce a ~40-token search query. That is why the multiple is 3.6x rather than 2x, and it is a property of the ReAct pattern, not of this prompt. **Prefix caching is what keeps it from being worse** (`AGENT-8` confirmed: 26.1% of input tokens served from cache). **Correction, same day:** this row first attributed the late engagement to 3.7 Flash's 4,096-token minimum. That explanation is insufficient — `finqa_dev_582` got **zero** cache on an 18,577-token judge call, four times the floor — so the floor is a necessary condition and not the mechanism, and the real trigger is unestablished (`COST-2` records a still-open ~9k-17k zero-cache bug that fits better). Do not repeat the 4,096 explanation. **Do not quote the 3.6x or the per-node split as results — n=3.** The projected full-run cost swings $10.3-$13.0 per 200 questions purely on the cap-hit rate (33% at n=3, 50% at n=2), which brackets `SESSION.md`'s $11.5 estimate and neither confirms nor moves it. **The one thing already worth watching:** on 2 of 2 multi-iteration questions the union of all iterations found no gold the first iteration had not already found, which is the direction `COST-30` predicted **RESTATED at n=200 (2026-09-05), and the shape changed because the iteration mix did.** Cost split is now **answer 64% ($0.0131/q), judge 19% ($0.0040), plan 17% ($0.0034)** against the pilot's answer 47% / plan 36% / judge 18%. The loop multiple is **1.77x** ($0.0205/q vs the static arm's $0.0116), not 3.6x. Prefix cache is **4.0%** of input tokens (275,250 of 6,817,457), not 26.1%. **All three moved for one reason: 174 of 200 questions ran a single iteration.** `plan`'s history replay — the mechanism this row identified, and it stands — only bills on a question that loops, and a single-iteration question caches almost nothing because nothing precedes it. So the pilot did not measure a different system, it measured a 3-question sample whose iteration mix was nothing like the population's. **These n=200 figures are quotable; every figure above this sentence in this row is not.** |

## Day 9 — Arm 6 run: pre-run bugs, corrections, and the negative result

| # | decision | why |
|---|---|---|
| AGENT-9 | **The answer prompt never asked for the format the scorer requires, so both arms scored 0.0% on the 19 pilot rows. `agent.py`'s `_ANSWER_PROMPT` now carries `COST-23`'s `FORMAT_LINE`; the 19 rows are archived, not deleted, at `data/archive/day9_no_answer_line_19q.jsonl.bak`** | `answer_eval.parse_reason` is deliberately strict — it matches ONLY an `ANSWER:` line and has no free-text fallback, because the fallback ("last number mentioned") was observed turning `ANSWER: Insufficient information` into `2007.0`. That strictness is right and the prompt was wrong: Arm 6's prompt was written independently of `COST-23`'s and asked for prose. Re-running `parse_reason` over the archived file confirms the failure was total and symmetric — **19/19 `no_answer_line` on the loop arm and 19/19 on the static arm**, i.e. not a model failure but a harness one, which is why it read as a plausible 0.0% rather than as a crash. Post-fix on the live run (n=20 in flight): loop 18 `ok` / 2 `refused`, static 15 `ok` / 5 `refused`, **zero `no_answer_line`**. Archived rather than deleted because the trajectories and `usage` in those rows are valid — only the answer text is unscoreable. `FORMAT_LINE` is restated in `agent.py` rather than imported, so `rag_sec` keeps no dependency on `scripts/`; the duplication is the deliberate cost of that boundary and is exactly what let the two drift apart in the first place **Provenance note: the rows cited here are now `data/archive/day9_static_unsorted_21q.jsonl.bak` — 21 rows, being 3 pilot rows plus the 18 the aborted attempt completed before `AGENT-16` stopped it. The live results file is the post-fix run.** |
| AGENT-10 | **Torch's MPS backend is not thread-safe. Model calls are serialised behind `retrieve._gpu_lock`, and `RAG_SEC_DEVICE` can force a backend. Run serial — concurrency buys nothing** | SIGSEGV in `MetalShaderLibrary::exec_unary_kernel` with no traceback and zero rows written, i.e. the process dies rather than raising, so no retry logic can catch it. **A discrepancy worth recording rather than smoothing over: `SESSION.md` says the crash took the machine down at concurrency 4; the code comment written at the time (`retrieve.py`) says reproduced at concurrency 2.** Both were observed on 2026-09-04 and no log survives to arbitrate, so the safe statement is "reproduced at concurrency >1" — do not quote a specific concurrency. Serial is not a cost, because ~94% of a question's wall clock is the reranker, which the lock serialises anyway: concurrency would only queue on `_gpu_lock`, which is why `OBS-6` splits lock-wait out of GPU compute as separate keys. `RAG_SEC_DEVICE=cpu` is safe for published numbers *only* because `INFRA-6` measured cpu score-identical to mps at 2e-6 — it is a speed knob (2.1x), not an accuracy one. `SentenceTransformer` is now constructed with an explicit `device=`, without which it auto-picks mps and silently ignores the override |
| AGENT-11 | **`AGENT-5` is wrong as written: stable thread ids do not give safe resume. Resume is FILE-level — `run_one` calls `checkpointer.delete_thread(qid)` before every question** | `AGENT-5` assumed LangGraph's Postgres checkpointing made a killed pass resumable mid-question. It does not, for a reason specific to the ReAct shape: a mid-question checkpoint's last entry is a `plan` turn whose `ToolMessage` was never written, and replaying it sends Gemini a history ending in a model turn — `400 "Requests ending with a model turn are not supported"`. Measured, not theorised: two ids with 5 stale checkpoints each failed 2/2, and the same questions passed on a fresh thread id. So the stable id is kept for **inspectability** (one thread per question, `session_id` = question id joins to Langfuse per `OBS-8`), and correctness comes from the output file instead: any id reaching `run_one` is absent from the results file, therefore never finished, therefore its checkpoint is garbage — wipe it. **The general lesson: checkpointing a graph is not the same as checkpointing a resumable unit of work.** The resumable unit here is the question, and its commit point is the appended JSONL line |
| AGENT-12 | **Errored rows counted as done, making any transient failure permanent. Resume now keeps successes only and rewrites the file without errors or duplicates** | The first resume logic built `done` from every id present in the output file. A row carrying an `error` key is a *record of failure*, not a result, so a 500 or a rate-limit blip would have been baked into the pass and silently reduced n — and on a 4.4h paid run the retry is the whole point of resuming. The file is rewritten rather than appended-to so a retry cannot leave two rows for one id, which would otherwise make the analysis depend on which one the reader saw last. Paired with `AGENT-11`: one bug made resume unsafe, the other made it useless, and both had to be fixed before spending money |
| AGENT-13 | **`COST-30`'s 15.1% insufficiency rate does not transfer to the loop and must not be quoted for it. What the loop measures is 11.1%-60.0% depending on sample, and questions terminate on the iteration cap rather than on a verdict** | 15.1% was measured on the 278 stored **one-shot** `COST-13` responses — a model shown a complete static evidence block once. The loop's `judge` grades **partial** evidence at iteration 1 and a different, growing block after that, so it is a different measurement on a different input and the two are not comparable. Measured first-iteration "insufficient" (`judge_verdicts[0] == 'loop'`): **3/5 = 60.0%** on `data/archive/day9_pilot_cpu_5q.jsonl.bak`, **2/18 = 11.1%** on the first 18 rows of the in-flight `data/day9_arm6_dev_results.jsonl`. **Note the range disagrees with `SESSION.md`'s "16-60%" at the low end** — 16% is not what either file on disk now produces, and it most likely came from an intermediate snapshot of a file that has since grown; the reproducible statement is "11-60% across the two samples available, n too small to call either". Mean iterations 2.40 (n=5) against 1.22 (n=18), cap-hit 40% against 5.6%. **The sample sensitivity is the finding, not the rate**: at n=5 the pilot happened to draw 3 loopers, which is why `OBS-10`'s per-node split and 3.6x multiple are also flagged n=3. Do not quote any of these as results; the run's own `agent_analyze.py` output supersedes them **Provenance note: the rows cited here are now `data/archive/day9_static_unsorted_21q.jsonl.bak` — 21 rows, being 3 pilot rows plus the 18 the aborted attempt completed before `AGENT-16` stopped it. The live results file is the post-fix run.** **REVISED at n=200 (Day 9 complete, 2026-09-05): it roughly DOES transfer, and the pilot range was noise.** First-iteration 'insufficient' is **26/200 = 13.0%** against `COST-30`'s 15.1% — inside a percentage point or two, not the 11-60% spread this row was built on. Mean iterations **1.24**, cap-hit **7/200 = 3.5%** (8 questions reached iteration 4; one of them ended on a genuine `finish`, so the cap forced only 7). The pilot's 60.0% came from n=5 and its 11.1% from the first 18 rows; **both were sample noise and neither should be quoted again.** What survives from this row is the *reasoning* — one-shot and partial-evidence judging are different measurements — which happens to have been right in principle and wrong in magnitude. The n=200 figures supersede every number above. |
| AGENT-14 | **`AGENT-8` confirmed: Gemini's automatic prefix caching does fire on loop iterations, with no code change. But the cache share is a function of the iteration mix, not a property of the system, so it must never be quoted as a single number** | Measured cache share (`cached_input_tokens`/`input_tokens`, summed over all calls): **22.9%** on the 5-question pilot (mean 2.40 iterations), **12.4%** on the in-flight run's first 18 rows (mean 1.22), against `OBS-10`'s **26.1%** at n=3. The three differ by ~2x and none is wrong: **cache share is arithmetically pinned by how many questions loop**, because 0 of 16 single-iteration questions cached a single token while 5 of 5 multi-iteration ones did. So a full-run cache share is a report of the loop rate, and the honest way to quote it is per-question conditional on iteration count. **Two structural facts the per-call breakdown makes visible.** (1) `answer` can never cache — it is called once per question, so it has no prior identical prefix; the cache lives entirely in `plan` and `judge`. (2) **Onset is not simply "iteration 2+", and `OBS-10`'s 4,096-token-minimum explanation does not fully account for it**: `finqa_dev_174` first cached on its 4th call (judge, iteration 2) but `finqa_dev_582` got 0 on a judge call with 18,577 input tokens and only cached from its 6th call onward. Both prefixes were far above 4,096, so the minimum is necessary but not sufficient — implicit caching is best-effort and its onset is not predictable from prefix length in this data. Treat the minimum as a floor, not a rule |
| AGENT-15 | **Day 9's specced experiment is impossible on this benchmark and the response is to run the trajectory half and publish the negative. Write-up: `data/day9_multidoc_negative_ANALYSIS.md`** | `spec.md:383` asks for Arm 6 measured "specifically on the multi-document questions"; that subset is **empty**. Re-verified independently 2026-09-04 from `load_matched_questions()`: max distinct `chunk_file` per question id is **1** in every split, **0** ids with >1 filing — dev 1235, test 1546, and **train 8,958 as well**, so there is no rescue by widening the sampling pool. `page_number` is single-valued per id too: the evidence is one page of one filing, which is `DATA-4` arriving from the other direction. **The trap this closes:** 50.4% of dev and 52.0% of test questions name ≥2 distinct years and still need only one filing, because 10-Ks print prior-year comparatives — the same reprinting `RETR-3` blames for document-selection failures. **Why this sharpens `COST-30`:** its one open door was "iteration's published wins are multi-hop composition across documents, which is Day 9's question" — that test does not exist here, while the regime that *is* represented is the one where `COST-30`'s cited CRAG loop scores recall@5 0.658 against hybrid+rerank's 0.816. Also: **do not build the subset from `pack_variants.py:287`'s `multi_doc`** — that flag is 293/300 = 97.7% (`data/day8_pack_variants.json`) because it measures whether *retrieved* chunks span filings, i.e. `RETR-3`'s contamination, a property of the result. Selecting on it would select on retrieval error. What the run still establishes — calls/tokens/dollars/wall clock, sufficiency-judge accuracy, and whether iterations 2-4 add evidence at all — is in §6 of the write-up |
| AGENT-18 | **`spec.md` drift, never logged until now: `spec.md:222`/`:241` specify AWS Bedrock for generation, and every model call in this project runs against Gemini instead** | Verified 2026-09-04: zero occurrences of `bedrock` or `boto3` anywhere in `src/`, `scripts/` or `pyproject.toml` — the dependencies are `google-genai>=2.21.0` and `langchain-google-genai==4.3.7`, and every model-invoking site is Gemini (`agent.py`'s plan/judge/answer, `answer_ab_run.py`, `answer_batch_run.py`). This was never a decision, it was an accretion: `AGENT-1` chose Gemini models on *capability* grounds (3.7 Flash for agentic decomposition, Flash-Lite for the judge) without noting that doing so overrode the stack table, and `COST-13`/`COST-23`/`COST-34` then priced everything at Gemini rates. **Consequence for Day 12-13, and it is the reason this needs a row:** `spec.md:398` budgets ~10h for AWS partly *because* Bedrock was to be the generation backend, and `spec.md:296` explicitly leans on it ("Bedrock fits, since it's already in the stack") for the pinned-model requirement. It is not in the stack. So Day 12 must either (a) keep Gemini and drop Bedrock from the AWS scope — cheaper, but the published numbers then come from outside the AWS account, which is a fair interview question — or (b) port generation to Bedrock, which invalidates every dollar figure in this log and needs a re-measurement, not a re-price, since `COST-24`'s thinking-token behaviour is model-specific. **Recommendation: (a).** The pinned-model requirement is already satisfied — model ids are written into every results row's `config` block — and Bedrock's contribution to the AWS gap is one API surface against RDS/S3/App Runner, which Day 12 covers regardless |
| COST-38 | **`service_tier="flex"` stays declined. Recording the decision, NOT the numbers: the re-measurement's figures have no artifact on disk and could not be verified** | The decision is safe on `COST-29`'s own grounds, which are reproducible: flex and batch are both 50% off standard and do not stack, batch additionally carries the extended rate limits, and `answer_ab_run.py` already defaults to standard with `--tier flex` kept only so the state can be re-probed. **What could not be established.** `SESSION.md` records a 2026-09-04 re-measurement — "85% vs `COST-29`'s 10%, still declined: p50 15.58s, 15% hard failures, unstable". Searched 2026-09-04: nothing in `data/`, no log, and `git log -S'15.58'` across all branches returns nothing; the figures exist only in that one `SESSION.md` line. Worse, **`15.58836131002754` does appear on disk — as `static_baseline.retrieval_latency_s` for `finqa_dev_420` in `data/day9_arm6_dev_results.jsonl`**, i.e. a reranker latency, not an API latency. That is not proof of a mix-up, but it is precisely `SESSION.md` §4's failure mode (a value correct in one context, silently wrong in the next) and it means the number cannot be cited. The prose is also internally inconsistent: 85% cannot be a *failure* rate alongside "15% hard failures", so 85% is presumably a success rate against `COST-29`'s ~10%, which would mean flex got **better**, not worse — and that reading is a reason to re-probe, not to decline. **So: per `SESSION.md` §4's precedent on the 9.4x multiplier, no number is adopted here.** If flex matters later, re-run it and write the JSONL first **Provenance note: the rows cited here are now `data/archive/day9_static_unsorted_21q.jsonl.bak` — 21 rows, being 3 pilot rows plus the 18 the aborted attempt completed before `AGENT-16` stopped it. The live results file is the post-fix run.** |
| COST-39 | **Split the CI quality gate: retrieval metrics every push, answer accuracy on a fixed ~200-question set on a schedule. But `$10.47/push` does not survive checking — the measured figure is $23.62/push at standard, and the dollar framing hides a worse wall-clock problem** | `spec.md:170` specifies a fixed 2,000-question CI slice; gating **numeric-match accuracy** on it means 2,000 answer sends per push. **Recomputed from `data/cost31_thinking_medium.jsonl`** (n=139 uncompressed rows at `medium`, the `COST-34` configuration that ships): 11,621 input and 825 billed-output tokens/question at 3.7 Flash's $0.75/$3.75 per 1M = **$0.011811/question standard, $0.005905 batch** → 2,000 questions = **$23.62 standard / $11.81 batch**. `$10.47` reproduces nothing; the nearest figure is $10.56, which is `COST-36`'s $14.69/2,781 scaled up — i.e. a **batch** rate, and batch's 24h SLO makes it unusable for a push gate by construction. **A correction to `COST-36` falls out of this:** it priced the input side at its stated 9,834 *evidence* tokens, but the measured prompt is 11,621 tokens/question, so its $14.69 understates by ~11.8% and should be ~$16.42. Still inside the ~$30 envelope, so `COST-36`'s conclusion stands unchanged — but quote $16.42 for dev+test on batch, not $14.69. **The gate split is right and the reason is stronger than cost.** (1) A ~200-question answer gate is $2.36/run at standard, which is affordable per run and **not** affordable literally nightly: 30 nights is ~$71 against a ~$30 project envelope, so schedule it on merges to the default branch, not on a timer. (2) **The retrieval leg is not free either, in the dimension that actually blocks CI** — 2,000 questions of rerank is ~2.4h at the published Arm 4-A p50 of 4.32s (V100) and ~9h at the Day 9 laptop rate (median static-baseline `retrieval_latency_s` 16.58s, n=18). A per-push gate needs a retrieval slice of a few hundred too, or replay-scoring from stored rankings where only the scorer changed — and replay cannot vet a change to retrieval itself, which is the case the gate exists for. Size both legs; do not assume "retrieval is the cheap half" **Correction: the second wall-clock leg is wrong. `static_baseline.retrieval_latency_s` is copied from `retr7_rr_dev_scores.jsonl:latency_s`, which `rerank_hpc.py` writes as a V100 batch average across all four cells — ~4.2s/cell, the same hardware and figure as the 4.32s it was contrasted with. There is no separate laptop 9h figure; drop that leg.** |
| AGENT-16 | **The Day 9 static baseline was not the arm it claimed to be, and the run was stopped 18 questions in to fix it.** `agent_run.py`'s `load_static_rankings` took `ranked[:10]` straight from `data/retr7_rr_dev_scores.jsonl`, which stores candidates in **first-stage RRF order with the rerank score merely attached as the third tuple element** (producer: `rerank_hpc.py:132-135`). The published scorer sorts by `-x[2]` on load (`rerank_score.py:62`); this path never did | **Blast radius, measured, not estimated: 0 of 1235 dev cells are in descending score order; mean overlap between delivered and correct top-10 is 4.52/10; and 50.4% of questions got fewer than 5 of the right 10 chunks.** On the exact 197-question sample the delivered ordering scores recall@10 **0.552** / nDCG@10 0.379 / MRR 0.348 against the correct **0.739 / 0.609 / 0.595**. **What the baseline actually was: Arm 2 + company filter.** The filter survived because it is baked into which 50 candidates exist; the reranker's ordering was discarded; and the query strip therefore did nothing at all, since strip exists only to change rerank *scores* and the scores went unread. So the paired comparison was running Arm 6 against a crippled opponent, biasing the one question Day 9 exists to answer — "is the loop worth 3.6x the cost?" — toward yes. **The published `RETR-39` numbers are unaffected** (0.760 dev / 0.747 test); that path sorts. **Why it hid:** the variable was named `ranked`, the docstring asserted "50 reranked chunks", and the design decision was *correct* — reuse the published rankings so the baseline **is** the published arm rather than a re-run that could drift. Reusing the file felt safe, so nobody asked whether the file carried the guarantee the code assumed. **The signal was already on screen and was misread as small-sample noise:** `agent_analyze.py` reported the static arm at nDCG@10 0.250 against a published 0.627 for that same arm. A baseline reporting less than half its published score is not a sample-size story. **This is the fourth instance of one bug class** — an assumption about an on-disk artifact the producer does not guarantee — after `RETR-24` (variant B/C chunks scored as A), `RETR-30`'s mis-anchored GPU-hour figure, and the unsourced 9.4x multiplier. A guard now fails the run if the static rankings are not descending at point of use, because the lesson has not stuck as a habit and needs to be mechanical |
| AGENT-17 | **A full read-only correctness audit was run before restarting, on the user's instruction that this be the last stop. Eight findings beyond `AGENT-16`, three of them run-killers** | Fixed in one batch. **(1) `--concurrency` defaulted to 8** while every document requires 1 — and `functools.lru_cache` **does not serialize concurrent misses** (measured: 8 threads, 8 executions), with model construction sitting outside `_gpu_lock`, so the default would load eight CrossEncoder copies onto MPS simultaneously: exactly `AGENT-10`'s SIGSEGV. Default is now 1 and construction is double-checked under a dedicated lock, deliberately not `_gpu_lock`, which is non-reentrant. **(2) `tracing.py` could kill the run, against its own `OBS-4` contract** — the `with cm as obs:` line was unguarded, and `question_trace`'s `with` in `run_one` sits outside the try/except, so a Langfuse raise on enter/exit reached `fut.result()`. **(3) `checkpointer.delete_thread` sat outside the try**, so a transient Postgres blip cost the run rather than one row. **(4) `hit_iteration_cap` conflated the cap with a judge finish**, because `route_after_judge` tests the cap first — it overstated a published rate, latent at 0/21 rows. **(5) `agent_analyze.py`'s all-iterations union did not dedupe**, so a gold chunk retrieved twice could be counted twice by `ndcg_at_k`. **Corrected on verification: this changed no number here.** Repeats occur in 3 of 21 rows but every one sits past position 10, and nDCG is truncated at k=10 — union nDCG@10 is 0.4961 both before and after. The fix removes a latent error rather than a published one; recall and MRR were never exposed, being set- and first-hit-based. **(6) `embed_s` was not embed latency** — first-call model construction landed inside the timer, measured 0.114s to 19.0s for identical work, so `spec.md:121`'s published p50/p95 would have been machine warm-up. **Pre-fix and post-fix `embed_s` are not comparable.** **(7) The loop arm's root span contained the static arm's generation**, since `static_baseline()` ran inside the loop's trace — the JSONL `wall_clock_s` was always clean, but a latency read off the dashboard overstated Arm 6. **(8) `judge-sufficiency` carried `verdict` without `iteration`**, making cap-rate and per-iteration judge accuracy inexpressible as widgets. **What the audit cleared is as valuable as what it found**, and is recorded so it is not re-litigated: gold-label id-space alignment (1958/1958 chunks byte-identical, 0/1235 stem mismatches), question-id uniqueness across all 11,739 rows, the static cell's provenance genuinely matching the live loop's flags, token accounting exact on 98/98 usage records with no cached double-count, thinking tokens confirmed inside `output_tokens` (so `COST-24`'s 45% understatement does not apply here), both arms sharing prompt/model/thinking level by import rather than copy, and resume idempotency |
| OBS-11 | **The dashboard is code, not clicks: `scripts/observability/build_langfuse_dashboard.py` builds 12 widgets through Langfuse's `unstable/` dashboard API, idempotent by name** | Rejected the two alternatives on provenance grounds: UI clicking leaves no reviewable artifact and cannot be recreated after the 30-day retention window (`OBS-1`), and a hand-saved JSON blob is not runnable. Verified by running it four times and reading both list endpoints back — 12 widgets, 1 dashboard, no duplicates. Deletion is behind an explicit `--delete`. **Four things the API genuinely cannot express, recorded so they are not re-attempted:** per-*question* cost (observation cost is per-observation, the root AGENT span costs nothing, and the `traces` view is unsupported on this surface — so the dashboard publishes sum-cost and question-count separately and **`agent_analyze.py` stays the authoritative $/question**); grouping by metadata (fixed dimension list, so the iteration distribution is four filtered tiles, a survival curve); `usageType` is groupable but not filterable; and grouping by `tags` groups the whole array, so arm bars come out as arm x source. **Unstable-surface landmines:** unknown `chartConfig` keys are **silently stripped** (a `dimensions` key vanished, which is why every field is read back), widget metrics use `agg` not `aggregation` — differing from `metrics get` — metadata filters need `type: stringObject`, tag filters need `arrayOptions`, and Cloud rate-limits to 30 req/min, which one full build exceeds, so the script honours `retryAfterSeconds` |
| OBS-12 | **`spec.md` 2.2 rule 5 discharged: 20 worst failures per arm, one markdown file per arm, human-read rather than machine-read.** `scripts/eval/worst_failures.py`, read-only | **"Worst" is a choice and the file says so.** Every failure is equally wrong — a number either matches gold or it does not — so wrongness cannot order them. Failures are instead bucketed by **how close the gold chunk got to the answer model**, worst first: **reasoning** (gold was in the delivered top-k and the answer is still wrong, so nothing upstream is to blame) -> **rerank** (gold was among the 50 candidates but never reached the top-k) -> **retrieval** (gold never entered the pool, which says nothing about the model). That axis is the entire diagnostic value; collapsing it into one wrongness score would waste the exercise. Ties break on best gold rerank rank, then iterations, then id, so the file does not churn between runs. Each entry carries the question, both gold fields (`COST-21`), the full answer, why it scored wrong, the delivered top-k per iteration with the gold marked, gold's top-k / rerank / first-stage ranks, per-iteration queries and the judge's **raw** verdict text (`OBS-5`), and `trace_id`/`session_id` — the join that makes Day 10's "diagnose it from its trace" a click rather than a search |
| DEPLOY-1 | **Serving layer is a new `src/rag_sec/api.py` (FastAPI) on a CPU-only container, serving Arm 3 + filter + strip — not Arm 6, and not a hosted reranker.** Deps go in an optional `serve` extra, not the default set | `spec.md` Day 12 owes "a live URL someone else can hit" and the repo had **no serving code at all** — 50 scripts and a library, nothing that answers a request. Four choices, each forced by something measured. (1) **Arm 3, not Arm 6:** the loop is an experiment whose verdict was still being computed when this was written, and its n=60 signal was that it adds no retrieval; `retrieve()`'s defaults already are the winning `filtered_stripped` cell, so serving the shipped arm required no arm-selection code. The answer call imports `_ANSWER_PROMPT`/`_answer_llm`/`_evidence_text` from `agent.py` — the same three `agent_run.py:static_baseline` imports — so the deployed system is the arm 0.747 was measured on, with retrieval computed rather than replayed. (2) **Reranker stays in-process:** the corpus is embedded with `bge-m3`, so the query embedder cannot be hosted without invalidating dense search, which puts torch in the image regardless; adding the cross-encoder to an image that already carries torch is nearly free, where a hosted reranker (`spec.md`'s named fallback) is a *different model* and would force a paid test-split re-measurement to stay honest. (3) **CPU:** `INFRA-6` measured mps and cpu score-identical to 2e-6, so `RAG_SEC_DEVICE=cpu` is a speed knob, not an accuracy one — the container serves the published rankings, slower. **Day 13's p95 must therefore be measured in this container, not on the laptop.** (4) **Weights baked into the image, not fetched at boot:** `config.py` pins both models by name but **not by revision**, so an upstream repo move would silently change retrieval; freezing the weights into the artifact is what makes the pin real and drops HuggingFace out of the runtime path. **One build detail that is not stylistic:** `--torch-backend` exists on `uv pip install` and not on `uv sync` (checked against uv 0.12.1), so the image exports the lock and `uv pip install`s it — without that, linux/amd64 pulls PyPI's CUDA-bundled torch and adds ~2.5GB to an image that will never see a GPU. **`serve` is an extra rather than a default dependency** because `uv run` syncs default groups, and reinstalling into the venv a multi-hour eval run is executing from is the provenance failure `AGENT-16` already cost a restart over. **Unbuilt as of writing:** authored during the Day 9 run, so `uv lock` and `docker build` are deliberately deferred — re-locking mid-run would mean a resumed run resolved a different environment |
| CI-1 | **CI gate ships as two jobs on every push: four self-contained guards, then a retrieval gate that REPLAYS a committed 400-question dev fixture. Gating on the held-out test split was rejected, and so was re-running retrieval in CI** | `spec.md` 2.4 wants recall@10 / nDCG@10 / numeric accuracy / p95 / cost gated per push, and `spec.md` 2.3 asks for a fixed 2,000-question slice. **Neither is executable as written on GitHub Actions**, which has no GPU and no Postgres holding the 99,654 embedded chunks — so the gate replays `retr7_rr_dev_scores.jsonl`'s `filtered_stripped` cell, the exact arm `RETR-39` published. Both that file (11MB) and `data/chunks/` are gitignored, so `ci_fixture_build.py` distills them to one committed ~1MB file of gold ids + ranked ids. **The honest limit, and it must stay in the README:** replay vets the **scorer and the labels** — a change to `eval.py`'s metrics, the gold matcher, or ranking-order handling fails the build — but it **cannot vet a change to retrieval itself**, because a new embedder or fusion rule produces different candidates while the fixture still holds the old ones. That case needs a GPU pass and belongs on a schedule, not a push. **Dev, never test:** a per-push gate on the holdout would consult it continuously and destroy the one thing that makes 0.747 credible. **The baseline is measured on the fixture, not copied from `DECISIONS.md`** — a 400-question sample does not land on the full-split number, and hardcoding the published figure would make the gate fire on the sampling difference. Slice fixed at `SEED=17` so a rebuild cannot move the baseline. **`n` is asserted against the baseline's `n`**, because a fixture that quietly lost questions is the likeliest way a green gate goes wrong. **Two legs deliberately absent:** answer accuracy, because 2,000 sends is **$23.62/push** at standard and the batch rate that makes it affordable carries a 24h SLO (`COST-39`) — it goes on merges to the default branch; and p95, which needs a measurement in the serving container (`DEPLOY-1`), not a replay. **Verified 2026-09-04 on a synthetic fixture:** clean passes, a 10-point recall@10 regression exits 1, a truncated fixture exits 1. All four guards pass locally with no DB. `heading_fix.py` stays local — it reads the gitignored corpus. **Still unbuilt:** the real fixture, deferred until the Day 9 run frees the machine |
| EVAL-1 | **Unanswerable set built: 48 questions in five categories, 30 of them machine-verified against the corpus rather than asserted. The reprint rule is what makes the year category valid, and it is not obvious** | `spec.md:107` asks for refusal correctness and notes most projects skip it. Every T²-RAGBench question has an answer in the corpus, so nothing here had ever tested the opposite case — and a retriever has no empty result, so the answer model always gets ten plausible chunks about roughly the right thing. Categories: **out_of_corpus_company** (12), **out_of_corpus_year** (10), **cross_company_premise** (8) — all machine-verifiable — plus **out_of_scope_metric** (12) and **underspecified** (6), which rest on what a 10-K contains and are marked `verifiable: false` for a human read. **The subtle one is the year category.** A year is NOT absent just because no filename carries it: a 10-K reprints prior-year comparatives *and* this era's filings carried Item 6's five-year Selected Financial Data table — the same reprinting `RETR-3` blames for retrieval failures and `AGENT-15` measured at 50.4% of dev questions naming 2+ years. So "Apple's 2009 revenue" is answerable from the 2010 10-K despite AAPL having no 2009 filing. The rule is structural: a year is safe only if **later than the company's last filing, or ≥6 years earlier than its first**. `scripts/checks/unanswerable_validate.py` enforces it, because an answerable question in a refusal benchmark means the model refuses, scores correct, and the number is a lie — the `RETR-24`/`AGENT-16` bug class pointed at a label file. **Verified 2026-09-04: 48 questions, 30 claims checked against 799 filings, and three planted faults all caught** — a present company claimed absent, `AAPL 2009` inside the reporting reach, and a cross-company question whose host was itself absent (which would have tested the wrong thing). **The per-category split is the deliverable, not the aggregate rate:** the first two categories should be catchable *before generation* (the resolver finds a company with no filings) while `out_of_scope_metric` retrieves perfectly and can only be caught by the answer model, so one pooled number carried by the easy group would say nothing about the hard one. The runner records `resolved_companies` per question to size that pre-generation guard. **Unrun:** ~$0.60 of answer calls plus local reranking, deferred behind the Day 9 run |
| DEPLOY-2 | **`spec.md`'s "RDS Postgres with pgvector" cannot run this system, and Day 12 self-hosts ParadeDB on AWS instead.** BM25 here is not Postgres full-text: it is `pg_search` 0.25.1 (ParadeDB), queried as `id @@@ paradedb.match('text', ...)` (`candidates.py:44`) over a `USING bm25` index (`store.py:47`), and loaded via `shared_preload_libraries=pg_search` (`docker-compose.yml`). Verified 2026-09-05 against AWS's supported-extension list and ParadeDB's own deploy docs: `pg_search` is on neither RDS's nor Aurora's list, and RDS permits `shared_preload_libraries` only through parameter groups for extensions it already ships — two independent blockers, not one workaround. **Deploy the existing `Dockerfile.postgres` image (ECS Fargate or EC2+EBS) rather than a managed instance** | The alternative was RDS + native `tsvector`/`ts_rank` as a BM25 substitute. Rejected on the same ground as `AGENT-16`: it would deploy a *different retriever* than the one every published number describes. Arm 2 (+BM25/RRF) is +0.177 recall@10 over Arm 1 and the whole Arm 3 headline rests on it, so swapping the lexical leg is a re-measurement, not a re-host — and the deployed system would then not be the benchmarked system, which is the one thing `spec.md` 2.2 forbids. Cost of the choice is honest and small: managed backups and failover become ours. Same drift class as `AGENT-18` — the stack table was written before the code chose its extensions, and neither was logged until Day 12 forced the question |
| DEPLOY-3 | **Bedrock is dropped from the Day 12 scope; generation stays on Gemini. Closes `AGENT-18` on its own recommendation (a)** | `AGENT-18` established the drift (zero `bedrock`/`boto3` anywhere; every model call is Gemini) and left the Day 12 fork open. Taking (a): the pinned-model requirement `spec.md:296` leaned on Bedrock for is **already satisfied** — model ids are written into every results row's `config` block — so Bedrock buys nothing the project lacks. Porting instead would invalidate every dollar figure in this log, since `COST-24`'s thinking-token billing is model-specific, and cost `COST-13`-scale paid re-runs to restore them. Consequence to state plainly in the writeup rather than hide: the published numbers come from outside the AWS account. `spec.md:398`'s ~10h budget was partly *for* Bedrock, so this reclaims hours toward `DEPLOY-2`'s self-hosting |
| DEPLOY-4 | **Day 13-14 ships a live URL on a single EC2 host running both containers via docker-compose, and it stays up through Day 14 — but the whole plan is CONDITIONAL on the free-tier credits being verified present in the account before anything is provisioned.** Fallback if they are not: deploy, screenshot, measure the container p95, tear down same-day. AWS spend is credit-funded and does **not** draw on the $40 project budget, which stays reserved for Gemini calls (`EVAL-1`, `COST-39`) | Checked 2026-09-05 against AWS's own July-2025 announcement: $100 at signup plus up to $100 earned in $20 increments, Free account plan, 6 months or credit exhaustion. **Two things deliberately not trusted.** (1) The `.edu`/AWS Educate route — third-party sites claim a no-card "Starter Account" with credits, but AWS's own AWS Educate page describes only courses and sandboxed labs, and Starter Accounts have historically been region/service-restricted in ways that would not run `DEPLOY-2`. Unverifiable from a primary source, so not in the plan. (2) The secondary-source claim that the Free plan *shuts down* rather than billing the card at credit exhaustion — **AWS's announcement does not say this**, so a Budgets alarm goes up before any workload, not after. Single EC2 over ECS Fargate on credit burn: two Fargate tasks sized for two transformer models plus a 2.1 GB database run an estimated $100+/month and would consume the whole credit grant inside two months. Also arm64-native — the build host is Apple silicon, so Graviton needs no emulation. Instance size is deliberately left open: it depends on the serving container's measured memory footprint, which does not exist until the image is first built (run-list #7). `spec.md:446` pre-blessed this exact degrade ("ship on a single container host and say so honestly in the writeup") |
| AGENT-19 | **Day 9's headline, at n=200: the loop WINS on answer accuracy and LOSES on retrieval, and those are not in tension — they are the finding. 69.2% vs the static arm's 61.6%, discordant 14-1, McNemar exact two-sided p=0.00098** | The n=60 early signal in `SESSION.md` pointed at a negative result (73.1% vs 69.2%, n=52, not significant) and `AGENT-15` had already decided to report one. At n=200 it is significant and it is the loop's. **What makes it interpretable is that retrieval moved the other way.** Arm 6's iteration-1 recall@10 is **0.703** against the static arm's **0.736** on the same 200 questions — the loop's own first retrieval is *worse*, because iteration 1 searches with a query the `plan` node rewrote, while the static arm searches with the raw question that `RETR-31`'s filter+strip was tuned for. So the gain cannot be attributed to finding more gold. The mechanism left is the answer stage: the loop hands `answer` the deduped union of every iteration's chunks (28-30 rather than 10, `agent.py:37`), and it refuses less often than static (19/53 failures vs 29/66). **Quote the accuracy pair and the p-value; do NOT quote a retrieval gain.** Cost of the win: 1.77x per question and a mean 1.24 iterations |
| AGENT-20 | **The stage-latency drift is a memory-saturation curve inside ONE process, not contention between processes — which retires the explanation `AGENT-17` and `SESSION.md` housekeeping both carried.** Measured alone on an idle machine: two regimes, clean and saturated, with the transition complete by question ~10 | `scripts/eval/latency_measure.py` (retrieval only, no LLM, same split/seed as `agent_run.py`) was stopped at 31 of 100 questions because the curve had already resolved. **Clean regime, questions 0-2:** embed p50 **0.116s**, search **0.356s**, rerank **24.87s**, total **25.4s**, swap 2.3 GB. **Saturated regime, questions 10-29:** embed p50 **8.73s** / p95 13.72s, search **2.87s** / 5.57s, rerank **46.86s** / 62.57s, total **57.99s** / 71.56s, swap oscillating 7.4-9.8 GB. **2.3x on total, 75x on embed.** The Day 9 run's own p50s (embed 12.98s, search 2.98s, rerank 59.59s) sit in the saturated band — search p50 matches to 0.11s — so that run was in saturation essentially throughout, and `spec.md:121` must be published from the clean regime with the regime named. **Three things kill the contention story.** Nothing else heavy ran: no second pass, no agent, Docker idle. `ps` RSS reported the python process at **0.1 GB** while swap grew 7 GB, because MPS allocations live in unified memory the process does not account for — the exact blind spot `SESSION.md` warns about. And swap fell **9.8 GB -> 2.8 GB the instant the process exited**, so the memory was that process's, not the Docker VM's. **It saturates rather than growing without bound**, so 'leak' is the wrong word: the working set simply exceeds 16 GiB once both models and the candidate texts are resident. Consequence for Day 13: a container p95 measured on this laptop is a measurement of this laptop's RAM (`DEPLOY-1`) |
| AGENT-21 | **Four questions are scored wrong in BOTH arms while being semantically right, because `answer_eval` requires a number on the ANSWER line and their gold is yes/no. Both arms are understated by ~2 points; the gap between them is not** | Found while verifying the worst-failures draft rather than by reading the scorer. Dev has 5 yes/no-gold questions in this 200: `finqa_dev_551`, `_649`, `_613`, `_247` and `convfinqa_2119`. **The first four are scoreable and are being scored** — their `program_answer` is a numeric 1.0/0.0, so `gold_is_scoreable` returns True on a single parseable field — and the model answers 'Yes'/'No'/'No'/'Yes', all correct, all parsed as `no_number` and counted wrong. (`convfinqa_2119` has `program_answer='yes'`, no numeric field at all, and is correctly excluded.) Replaying with `yes->1.0`/`no->0.0` moves arm6 **69.2% -> 71.5%** and static **61.6% -> 63.4%**: both rise, the 7.6-point gap moves 0.5, so `AGENT-19`'s headline is unaffected and **69.2% is a floor, not an estimate**. **Deliberately NOT fixed:** redefining a metric after seeing the results it produces is a decision for the human, not a cleanup, and the replay is free whenever it is taken (`CLAUDE.md` rule 3). Same family as `RETR-24`/`AGENT-16` — a scorer's assumption about the shape of an artifact its producer never guaranteed |
| AGENT-22 | **`agent_analyze.py`'s 'all iters (union)' row cannot be compared to the static row, and its nDCG@10 cannot differ from iteration 1's by construction. Quote neither** | `agent_analyze.py:142` scores union recall at `k=len(got)` — every chunk the loop saw across up to 4 iterations, so up to 40 — while the static row is recall@10. The printed pair **0.756 vs 0.736** therefore compares a 40-slot budget against a 10-slot one and reads as a loop win; the like-for-like pair is iteration-1 **0.703** vs static **0.736**, which is a loop *loss* (`AGENT-19`). Separately, `:134` builds the union in iteration order, so its first ten elements are always iteration 1's top-10 and union nDCG@10 is *identically* iter-1 nDCG@10 — the 0.593/0.593 match flagged as suspicious is arithmetic, not coincidence, and the metric carries no information. **The union-recall number is not wrong, it answers a different question** ('did looping ever surface gold, at any depth') and is worth keeping under that name. This is the fourth-and-fifth instance of the project's recurring class in one file: a number correct in the context that produced it, misleading in the one that quotes it |
| AGENT-23 | **At least one Day 9 'reasoning failure' is a wrong gold label, verified against the filing text — so the `reasoning` bucket (41 of arm6's 53 failures) is an upper bound on answer-stage blame, not a measurement of it** | `finqa_dev_147` asks Ball Corporation's total return over the five years **ending 2010-12-31** on $100 invested 2005-12-31. The gold chunk's own table reads `Ball Corporation | $100.00 | $110.86 | $115.36 | $107.58 | $134.96 | $178.93` across 2005-2010, so the answer is $178.93, a **78.93%** return. The model said 78.93%. **Gold says 34.96**, which is the **2009** column — an off-by-one-year read of the same row. The model is right and the label is wrong. **This is one verified instance, not a rate.** A machine-written draft (`data/day9_failure_diagnosis_ANALYSIS.md`) proposes four more of the same family and two scorer artifacts; only this one, `finqa_dev_247` (yes/no, `AGENT-21`) and `finqa_dev_672` (whose gold program divides 1.4 by an 8.4 the question never mentions, so the model's refusal is correct) have been checked by hand. **Why it matters beyond one question:** `worst_failures.py` labels a failure `reasoning` when the gold chunk WAS in the top-k, which is a statement about retrieval, not about whether the target was right. Every mislabeled gold lands in that bucket by construction. So `reasoning 41/53` bounds how much the answer stage could be at fault and should be quoted that way. **What is NOT established:** whether these golds are wrong in the FinQA source or only against our chunking — the original tables were never opened — and therefore no correction to any published accuracy figure follows from this row. It argues for a floor (`AGENT-21`), not for a restatement |
| AGENT-24 | **One line — `torch.mps.empty_cache()` per retrieval — removes the memory saturation `AGENT-20` measured, and it is what makes `spec.md:121`'s p50/p95 publishable at all. Laptop steady state is now embed p50 0.075s, search 0.241s, rerank 32.60s, total 32.97s (p95 36.53s), with swap flat** | `AGENT-20` established the two regimes; this establishes the cause and the fix. **Two arms, 25 questions each, same split/seed, one process each** (`scripts/checks/mps_leak_probe.py`). Without it, questions 10-24 average embed **7.70s** and rerank **48.50s** with swap at **8,376 MB**. With it, the same band averages embed **0.072s** and rerank **34.07s**, and swap *falls* to **1,984 MB**. **The mechanism is not a leak, and the distinction is the interesting part:** `torch.mps.current_allocated_memory()` sits pinned at **4,542 MB** through all 25 questions in both arms — the two models, nothing retained — so no tensor is being held. MPS's caching allocator simply keeps freed blocks rather than returning them, which is free on a discrete GPU and ruinous on 16 GiB of *unified* memory the OS also needs. That is why `ps` RSS showed 0.1 GB against 7 GB of swap growth, and why every previous diagnosis (`AGENT-17`, battery, then thermal, then contention with the Docker VM) looked for a second process and found nothing. **Verified in the shipped path, not just the probe:** 15 questions through `retrieve()` after the change give the figures in the claim, swap 1832 -> 1824 MB. **Accuracy-neutral by construction** — it releases blocks already freed, so no score, ranking or candidate can move; it is a memory call, not a compute path. Timed into its own `mps_empty_cache_s` and **counted in `total_s`**, unlike `model_init_s`, because a caller pays it on every question. **Caveats:** n=14 steady-state, so the laptop p95 rests on one sample — acceptable only because `DEPLOY-1` requires the *gated* p95 to come from the serving container, where the device is CPU and this code path does not execute. And rerank's stable 32.6s still sits ~1.3x above the coldest observed 24.9s, so the fix bounds the growth rather than abolishing it |
| DEPLOY-5 | **`DEPLOY-1`'s image had never been built, and building it found five defects — three that made the build impossible and two that would have shipped a system different from the benchmarked one. It now builds, starts offline as non-root, and answers.** Image **9.06GB**: `/models` 4.3GB baked weights, `/opt/venv` 1.2GB | Every one was invisible to review and obvious on first build, which is `CLAUDE.md`'s recurring class pointed at a Dockerfile. **Build-blocking.** (1) `COPY --from=ghcr.io/astral-sh/uv:${UV_VERSION}` — `--from` does not expand variables, so the file could never build; fixed with a named `FROM ... AS uvbin` stage, the only instruction that expands a global ARG. (2) **The CPU-only intent was defeated by the lockfile.** `uv.lock` carries 43 `nvidia-*` entries plus `triton` marked `sys_platform == 'linux'`, so `uv export` emits them as explicit requirements — at which point `UV_TORCH_BACKEND=cpu` is inert, because it governs *resolution* and this step installs an already-resolved list. ~3GB of CUDA runtime went into an image whose own header says CPU only, and **the build ran the disk down to 5GiB and was killed during layer export**. Fixed by stripping the packages from the export and keeping `--torch-backend=cpu` for torch itself; verified in the image as `2.13.0+cpu`, `cuda.is_available() False`, zero `nvidia-`/`triton` packages. (3) `cp -r /models /models-baked` duplicated 4.3GB of weights inside one layer for no reason; `COPY --from=weights /models` does the same job. **Correctness, and these are the ones that matter.** (4) The lifespan warmup died calling `load_matched_questions()` — `retrieve()` -> `resolve_companies()` -> `build_lexicon()`, which imports the HF dataset loader *and* reads `data/chunks/`, neither of which is in a serving image. `company.py` already intends the fix (`load_lexicon` reads a cached JSON and only builds when it is absent, and the docstring says building is kept out of the retrieval path) — the 10KB `data/company_lexicon.json` simply was never copied in, because `.dockerignore` excludes `data/`. Now baked, so image and eval resolve identical aliases. (5) **`stage_latency` was always `{}`.** `api.py` filtered `last_call_stats()` for keys ending `_s`, but the `_s` keys live inside `["timings"]` and the top level holds `timings`/`rerank_query`/`tickers`/`candidates`/`reranked`. Day 13 gates a p95 read from that field, so **the gate would have gated an empty dict** — `RETR-24`'s class again, a consumer assuming a shape the producer never promised. **Open, and it is an accuracy risk rather than a bug:** `python:slim` ships no `/usr/share/dict/words`, so `RETR-11`'s English-word guard on risky aliases is **OFF in the container** and 'visa applications' can filter to Visa Inc. The deployed resolver is therefore not the benchmarked one, which is precisely what `DEPLOY-2` refuses to allow. Fix before AWS: install `wamerican` or bake a wordlist and set `COMPANY_WORDLIST` |
| DEPLOY-6 | **The CPU-only serving container answers correctly and is far too slow to ship as an interactive API: one question took 212.4s end-to-end, of which the cross-encoder was 207.8s. `spec.md`'s p95 gate cannot be met by this design on free-tier hardware, and that has to be settled before AWS, not on it** | First real end-to-end measurement, and it only exists because `DEPLOY-5` fixed `stage_latency` from the `{}` it had always returned. Same question, same corpus, same k=10 over `CANDIDATE_K` candidates: **rerank_s 207.76s on CPU against 32.60s on MPS** (`AGENT-24`), a **6.4x** penalty — and `INFRA-6`'s 2.1x, which the Dockerfile header cites to justify CPU, was measured on a different workload and does not describe this one. **The extrapolation is the problem.** This ran at **755% CPU**, i.e. ~8 cores, and `DEPLOY-4` plans a free-tier arm64 instance with **2 vCPUs**: naively ~4x again, so **~800s/question**. Resident memory 2.17-2.75GiB, which also rules out the 1GiB and 2GiB instance classes. Retrieval is correct throughout — top citation `BLL_2010_9389` at 0.996, `value` 178.93, matching the filing's own table (`AGENT-23`) — so this is purely a latency and sizing result, not an accuracy one. **Four routes, none yet taken, and the choice is a real trade rather than a tuning knob:** cut `CANDIDATE_K` for the served path only (cheapest, but it deviates from the benchmarked arm, which is what `DEPLOY-2` refuses); export the cross-encoder to ONNX/quantised int8 (keeps the arm, needs a re-measurement to prove ranking parity); serve on a GPU or larger CPU instance (breaks the credit budget `DEPLOY-4` is built on); or accept a batch/async API and drop the interactive p95 from `spec.md` scope, saying so. **Do not size the EC2 host until this is decided** — instance choice follows from the route, not the other way round |
| CI-2 | **The retrieval gate is green on the real 400-question fixture and demonstrably fires on a regression, so `CI-1` is no longer synthetic-only.** Fixture 400 questions / 0.53 MB; measured baseline recall@10 **0.7854**, recall@50 **0.8196**, nDCG@10 **0.6593** | Built and run 2026-09-05 once the Day 9 run freed the machine. **The baseline sits ABOVE the published dev figures** (0.760 / 0.802 / 0.627) and that is the design working, not a discrepancy: `SEED=17` samples 400 of 1235, and `CI-1` measures the baseline on the fixture precisely so the gate cannot fire on the sampling difference. The gap is ~+0.025 at recall@10, about one standard error, so nothing here contradicts `RETR-39`. **The regression test is what was actually missing.** Degrading the fixture by shuffling the ranked list of a random 25% of questions — a real regression's shape, since a broken reranker or a lost sort keeps the candidates and loses the order, which is exactly `AGENT-16` — drops recall@10 to 0.6454 and nDCG@10 to 0.5303 and exits **1**; the untouched fixture exits **0**. **`recall_50` is unchanged at 0.8196 in the degraded run, and that is the sharpest evidence the gate measures what it claims:** reordering within a 50-candidate list cannot change set membership at 50, so a gate that had also 'failed' recall@50 would have been reporting noise. Degradation done against a scratch copy — no broken fixture was ever committed |
| EVAL-2 | **Refusal correctness is 47/48 = 97.9% as scored, and the single 'hallucination' is a defect in OUR label, not in the model — so the honest reading is 47/47 = 100% on the correctly-labelled set, with an unanswerable set that is one question smaller than it claims** | Run 2026-09-05, 48 questions, ~$0.60, `data/unanswerable_results.jsonl`. Perfect refusal in four of five categories: `out_of_corpus_company` 12/12, `out_of_corpus_year` 10/10, `cross_company_premise` 8/8, `underspecified` 6/6. `out_of_scope_metric` scored 11/12. **The miss was checked rather than reported.** `unans_034` asks the age of Philip Morris International's CFO in 2017 and the model answered 53, citing Item 10 — and chunk 104 of `PM_2017_1413329` contains the literal row `Martin G. King | Chief Financial Officer | 53`. A 10-K's Item 10 lists executive officers *with their ages*, so the question is answerable and the model read it correctly out of the filing. **This is precisely the risk `EVAL-1` flagged and marked against itself:** the 18 questions in `out_of_scope_metric` and `underspecified` carry `verifiable: false` because they rest on what a 10-K contains rather than on a machine-checkable corpus fact, and exactly one of them was wrong. The three machine-verified categories are 30/30. **The inverse error is UNCHECKED and matters more.** Here the mislabel was visible because the model answered; a question that is secretly answerable and that the model refuses scores as *correct* and inflates the rate silently — `EVAL-1`'s own 'the number is a lie' case. Nothing in this run rules that out for the other 17 unverifiable questions. **So quote 97.9% only with the defect attached, or quote 30/30 on the verifiable categories.** Fix is to drop or rewrite `unans_034`, which also means the ~$0.012 to re-ask it, not a re-run. **What the run also settles, and it is the cheap win:** 18 of 48 questions resolved to no company at all and all 18 refused — a retrieval-side guard could reject those before any LLM call, at zero model cost |
| OBS-13 | **The dashboard's counts are only correct inside the run's own time window, and NO default window is that window: 'last 1 day' reads 185 questions, 'last 7 days' reads 225, and the truth is 200. Use a custom range of 2026-09-04 20:10 -> 2026-09-05 02:00, and it then matches the results file EXACTLY** | Found while capturing the `OBS-11` screenshots. Neither default is wrong about what it measures; both are wrong as a description of the run. **Under-count:** the run started ~2026-09-04 20:15 and ended 01:55:08 (339.6 min), so a 24-hour window truncates its first ~15 questions. **Over-count:** a 7-day window sweeps in the aborted pre-`AGENT-16` attempt (18 questions) and the pilots, giving 225 questions and 278 verdicts against a true 200 and 247 — and those extra rows are *the buggy build*, which is the same disambiguation trap `SESSION.md` already flags for individual traces (`OBS-8`'s deterministic seeding merges a re-run into its existing trace), now at dashboard scale. **Confirmed 2026-09-05 on the corrected window, and this is the row's real value:** the dashboard reads questions-reaching-iteration 200 / 26 / 13 / 8 and verdicts finish 193 / loop 54, against a results file holding 200 / 26 / 13 / 8 and 193 / 54. **Six counts, exact.** A prediction of 198 made from the log's `200 sampled (seed 42), 2 already done, 198 to run` was wrong — those two questions were re-run earlier the same evening and sit inside the window. **So this is a cross-validation, not merely a filtering fix:** the OTel span path and the JSONL writer are separate code producing separate artifacts, and they agree exactly; `rerank-candidates` p50 59.59s also equals `agent_analyze.py`'s `rerank_s` p50 to the centisecond. The instrumentation is verified rather than assumed. **Widen `OBS-11`'s rule:** it said `agent_analyze.py` is authoritative for `$/question` because per-trace cost is not expressible. It is authoritative for **trajectory and verdict counts too**, for a different reason — not that the dashboard cannot express them, but that it cannot scope them to one run. **Separately, and it is the one that would have shipped a wrong gate:** the stage-latency tile is captioned 'Day 13's budget is gated on the p95 column' and currently shows `embed-query` 12.97s p50 / 18.03s p95 and `rerank-candidates` 58.86s / 1.39m — all saturated-regime, pre-`AGENT-24`. True figures are embed 0.075s and rerank 32.60s, so gating off this tile sets an embed threshold ~200x too loose. **Regenerate it from a post-fix run before Day 13 reads it.** Screenshots taken meanwhile are honest as operational shape (cache split, error level, loop-vs-static contrast) and must not be read for absolute stage numbers |
| AGENT-25 | **The company filter silently switches OFF for most of the loop's extra work: 28 of 47 later-iteration queries (59.6%) resolve to no company, because `retrieve()` resolves from the query it is handed and after iteration 1 that is the planner's rewrite, which routinely drops the company's name. This is the mechanism behind `AGENT-19`'s retrieval loss** | Found by opening one trace (`finqa_dev_168`, loop trace `1314c4e76d88b89a7f5e01655f5f4c2a`) rather than by reading code — the fifth-and-sixth instance of this project's recurring class, and the first where the bad assumption is the *model's* rather than ours. **The worked case.** Iteration 1 searches `Global Payments 2017 Form 10-K net tangible assets acquired` and returns three GPN chunks. Iterations 2-4 search `filing_stem:GPN_2017_1123360 "NOTE 2" OR ...` — a field:value syntax **the retriever does not parse** — and iterations 2 and 3 return Host Hotels, Hologic, Philip Morris, LKQ and E-Trade while looking for Global Payments. Verified directly: `resolve()` returns `['GPN']` on the iteration-1 text and `[]` on all three rewrites. With no ticker, `retrieve()` takes the unfiltered branch across all 799 filings. **Rates over the run:** 7/47 later queries (14.9%) use the invented `filing_stem:` syntax, 12/47 (25.5%) return a top-5 sharing no company with iteration 1's, and **28/47 (59.6%) resolve to no company at all** — so the syntax is the visible symptom and name-dropping is the larger cause. **Why it survived every prior check:** nothing errors. An unfiltered search returns a full, plausible, well-formed candidate list, and `RETR-31`'s +0.040 filter simply stops applying. It is invisible in aggregate metrics and obvious in one trace. **It also sharpens `AGENT-19` rather than undermining it:** the loop still beats the static arm 69.2% to 61.6% at p=0.00098 *while* running most of its additional retrieval unfiltered, because `answer` sees the union and iteration 1's filtered chunks are in it. So the measured win is a floor on a working loop. **The instrumentation was already reporting it and nobody had read it**, which is the observability lesson: the `search-candidates` span carries a literal `"filtered": false` on iterations 2-4 and `true` on iteration 1, and `resolve-company` shows `{"tickers": ["GPN"]}` then `{"tickers": []}` three times. No inference was needed — the trace states it. (Iteration 4 recovers GPN hits anyway, but by accident: BM25 matched the literal string `GPN_2017_1123360` sitting in the query text, not the filter.) **FIXED 2026-09-05.** `retrieve()` takes a `resolve_from: str | None = None` argument — the text the company filter resolves against, defaulting to `query` so every non-agent caller is byte-identical to before — and `agent.py`'s `retrieve_node` passes `state["question"]`, the original. The rewrite still drives the search, which is the loop's purpose; it no longer decides which company we are in. **Counterfactual over the run's own 47 later-iteration queries: unresolved falls 59.6% -> 14.9%**, and the residual 7 are questions whose original text names no resolvable company, which is a different and pre-existing limitation. Guards green, agent smoke test passes. **Provenance: every Day 9 number was produced BEFORE this fix**, so `AGENT-19`'s 69.2% is a pre-fix measurement and a re-run would not reproduce it — the loop should do better, not worse. Preferred the interface over prompt-constraining — the planner should not be able to disable a retrieval feature by choosing words. Constraining the plan prompt is the weaker half of the fix; the interface is the real defect |
| DEPLOY-7 | **`DEPLOY-5`'s open wordlist defect is closed by baking the benchmark's own list into the image, NOT by installing a package — because the obvious package ships a measurably different resolver.** `data/wordlist_web2.txt`, 2.5MB on a 9.06GB image, with `COMPANY_WORDLIST` pointing at it | The tempting fix is `apt-get install wamerican`, and it is wrong. Debian's `wamerican` (2020.12.07-2) is **102,485 words** against macOS `web2`'s **235,976**, and over the 250 real aliases and tickers in `data/company_lexicon.json` the two disagree on **27**. The direction matters: `wamerican` carries proper nouns, so lowercased it marks `intel`, `merck`, `nike`, `walmart`, `goldman`, `citigroup`, `lockheed`, `mastercard` as ordinary English -- which makes them *risky* under `RETR-11`, which makes them stop matching in lowercase questions. That is ~11% of aliases behaving differently from the benchmarked resolver, which is exactly what `DEPLOY-2` refuses to allow, and it would also drift silently with the apt mirror. The baked copy is sha256-verified byte-identical to the list the benchmark reads. **The guard is doubled because the failure is silent by construction:** `_english_words()` emits a `RuntimeWarning` and returns an empty frozenset when no list is found, and warnings are not errors anywhere in the serving path, so `is_risky` quietly degrades to "len <= 3" and nothing fails. `scripts/checks/container_wordlist.py` therefore runs at **build** (fails the image) *and* at **startup** (fails the container), since build-time cannot cover `docker run -e COMPANY_WORDLIST=...` repointing it. Verified on a throwaway build: default passes with 234,456 words; guard-off exits 1; a wamerican-substituted list exits 1 on the sentinel assertions. **Not verified: the full 9.06GB rebuild**, which is the user's to run |
| AGENT-26 | **Reviewing the four files this session touched found the `AGENT-24` drain skipped on every error path and `stage_latency` shipping three keys that double-count — both invisible, both aimed straight at numbers this project publishes** | Neither is a bug in what was written; both are the *shape* of what was written meeting a path nobody walked. **(1) The drain was straight-line code after the rerank block**, so any raise from `chunk_texts`, `cross_encoder.predict` or the DB skipped it -- and `agent_run.py:213` catches per-question exceptions and keeps the pass running, so a single transient failure at question 40 of 300 silently restores `AGENT-20`'s saturation curve for questions 41+, inflating every stage p50 that pass then publishes, with no error anywhere. The drain is skipped precisely on the run most likely to be memory-stressed. Now a `_drain_on_error` context manager on the failure path, with the success path still timing it into `mps_empty_cache_s`; no timing is recorded on failure because a failed call has no latency worth publishing. **(2) `api.py`'s `{k: v for ... if k.endswith("_s")}` matched nine keys, not four.** `embed_lock_wait_s` and `rerank_lock_wait_s` are already *inside* `embed_s`/`rerank_s` and `model_init_s` is one-off warm-up, so a Day 13 gate summing that dict counts rerank twice and fails a system that passes -- the same class as the `{}` bug `DEPLOY-5` just fixed, one level up, and still with no consumer written, which is why it was catchable now. Now an explicit whitelist. **(3) `device == "mps"` is string equality against an unvalidated env var:** `pick_device()` returns `RAG_SEC_DEVICE` verbatim, so a valid `mps:0` turns the drain off silently; now `startswith`. **(4) The `resolve-company` span traced `query` while resolving from `resolve_from`**, so post-fix traces would pair a rewrite against populated tickers -- an incoherent pair -- and pre/post-`AGENT-25` traces were indistinguishable on the one attribute that changed. Span input now follows the resolution, and `resolve_from` is recorded on the root span. **(5) `agent.py`'s `retrieve_tool` body still called `_retrieve(query)` with no `resolve_from`.** Confirmed dead in the current graph, but swapping in a prebuilt `ToolNode` would silently revert `AGENT-25`; it now raises. **What the review also confirmed, and it is the load-bearing part:** `AGENT-25` cannot reach any published number -- the `RETR-39` static path never calls `retrieve()` at all, it replays `data/retr7_rr_dev_scores.jsonl` -- and all four other call sites take the default, so they are byte-identical to before |
| AGENT-27 | **The yes/no scorer question (`AGENT-21`) is decided: report BOTH, with 69.2%/61.6% as the headline and 71.5%/63.4% as a labelled sensitivity beside it. The mapping lives in `agent_analyze.py`, deliberately not in `answer_eval.py`** | The correction is defensible on the merits -- the model answered four yes/no questions correctly and a numeric-only scorer marked them wrong in both arms -- but adopting it as *the* number would be redefining a metric after seeing the results it produced, and the whole value of the strictness (`AGENT-9`: no free-text fallback, because the fallback turned `ANSWER: Insufficient information` into `2007.0`) is that it was fixed before the run. So the shipped scorer stays exactly as it was when the run was measured and the sensitivity sits next to it, printed unconditionally rather than behind a flag nobody would pass. Reproduces `AGENT-21`'s replay to the digit: arm6 69.2 -> 71.5%, static 61.6 -> 63.4%, gap 7.6 -> 8.1. `answer_eval` gained one public `answer_line()` so the sensitivity could read the ANSWER body without re-implementing the regex -- the duplication `AGENT-9` already paid for once with `FORMAT_LINE`. **`AGENT-21`'s own arithmetic needed one correction, found by verification:** it recovers **4 failures in arm6 but only 3 in static**, because static's `finqa_dev_247` was a genuine `refused`, not a `no_number` -- a model reason, not a scorer one. So "100% of yes/no fail for a scorer reason" holds for arm6 only |
| EVAL-3 | **`unans_034` is retired, and the unanswerable set is 47 questions scoring 47/47 = 100%. The row was NOT deleted from the results file** | `EVAL-2` established the question is answerable -- Item 10 lists executive officers with their ages, and `PM_2017_1413329` chunk 104 carries `Martin G. King | Chief Financial Officer | 53` -- so the row tested nothing and its own `note` ("appears in the proxy, incorporated by reference") was simply wrong about what a 10-K contains. **The interesting half is where the fix went.** `--score-only` read the *results* file directly, so dropping the question alone would have left it scored forever. Rather than delete a measured row, the questions file is now the single source of set membership and the scorer filters results to it, printing what it excluded. The measurement stays on disk, the score reflects the current set, and a retired question cannot go on counting in either direction -- which matters because the dangerous error here is the inverse one `EVAL-2` flagged and left unchecked: a secretly-answerable question that the model *refuses* scores as correct and inflates the rate silently. Retired row archived to `data/archive/unans_034_retired.jsonl`. Validator green: 30/30 machine-verified, 17 asserted. **Quote 47/47 with the set size, not 100% bare** |
| AGENT-28 | **The machine-written failure diagnosis was verified claim by claim against the chunks and the results file: 38 claims, 34 confirmed, 1 refuted, 3 needing correction, 0 cut. And the verification extended `AGENT-25` rather than merely confirming it** | Verified by opening `data/chunks/` and re-deriving `worst_failures.py`'s ranking, not by re-reading the draft. Several claims reproduce exactly to the last digit: `finqa_dev_610`'s gold is bit-identical to 8,905/18,038 = 0.493680008870163 (the 2013 column, for a question asking 2014), and `finqa_dev_2666`'s 4,840,000 was confirmed *exhaustively* -- all 50 numerals in the chunk, every pairwise product, exactly one pair hits it. **Three corrections before anything is quoted:** the yes/no recovery is 4 in arm6 and **3** in static (`AGENT-27`); `finqa_dev_367` must come OUT of "where looping helped" -- static had the gold at rank 2 and refused anyway, and the loop had it at iteration 1 rank 1, so no rewrite rescued anything; and `finqa_dev_610` is rank 20, not "just outside" the ten, with ranks 12-19 unexamined -- rank 12 (`finqa_dev_323`, $167.48 ending value vs 67.48% return, identical in both arms) is a fifth artifact class the draft never names. **`finqa_dev_367` is stronger than drafted:** the maturities table's own header reads "excluding lease obligations", so the gold divides an excluded item by a total that explicitly excludes it -- the denominator is wrong by definition, not merely arguable. **The extension to `AGENT-25`, which is the real find:** that row says the filter was on for iteration 1 and off for 2-4. Across the run that is **false** -- `plan` rewrites the query before the *first* retrieval too, so **20 of 200 iteration-1 queries also resolve to no company** (196's iteration-1 text says "Adobe" where the question says "Adobe Inc."). Under the fix 9 of those 20 become filtered; the other 11 name no resolvable company in the original either. The row's own 28/47 later-iteration figure re-derived exact. **Consequence for the draft:** its "off-target dilution" finding and its "the loop rescued 490" claim both describe the `AGENT-25` bug, not loop policy -- 672 ran iterations 2/3/4 unfiltered and 490 ran 2/3 unfiltered, so 490's rescue happened while the filter was off. No diagnosis flips, because gold was at rerank rank 1 in all ten regardless |
| DEPLOY-6-RESOLVED | **The reranker route is decided: export the cross-encoder to ONNX and quantise to int8, keeping `CANDIDATE_K` at the benchmarked value. Not yet implemented** | Chosen over the three alternatives on one criterion -- what it costs in *numbers you then have to re-earn*. Cutting `CANDIDATE_K` for the served path is free and the biggest single lever, but it makes the deployed system different from the benchmarked one, which is precisely `DEPLOY-2`'s and `DEPLOY-5`'s standing objection, and it owes a fresh dev-split recall measurement before any claim about the live URL is quotable. A bigger instance spends the credits `DEPLOY-4` needs to keep the URL up, and CPU scaling is roughly linear from 755% CPU, so interactive speed needs a lot of cores. Async sidesteps the p95 gate rather than passing it, and Day 13 explicitly wants a p95 measured in the container. ONNX/int8 keeps `CANDIDATE_K`, so the deployed ranking is the one the numbers describe and nothing is owed -- **conditional on proving ranking parity**, which is the work this route buys instead: quantisation is lossy, so it must be shown not to reorder the top-10 before it can claim `RETR-39`'s figures. If parity fails, `CANDIDATE_K` becomes the fallback *with* its re-measurement, not instead of it. **Do not size the EC2 host until parity is measured** |
| DEPLOY-8 | **The ONNX/int8 route is built and exportable but NOT yet validated, and the one timing observation is discouraging: 174s for a question's 50 pairs, against the container's fp32 207.8s. Treat the speedup as UNMEASURED, not as 1.2x** | Run 2026-09-05, stopped early on memory pressure before parity completed. **What is established.** The export works and the artifact is genuinely quantised -- 146 `MatMulInteger` and 98 `DynamicQuantizeLinear` nodes, fp32 2.27GB -> int8 **569MB** (4.0x smaller), 35s to export and 13s to quantise. **48 `MatMul` nodes stay fp32**, so the conversion is partial by design. **What is NOT established, and must not be quoted:** any speedup. The 174s is **n=1**, native on this Mac, against a 207.8s figure measured inside Docker at 755% CPU on a different code path -- two different machines' conditions, so the ratio is not a like-for-like comparison and the fp32-CPU-native leg was never reached. **Three findings that survive regardless.** (1) **`optimum` is uninstallable here**: every published `optimum-onnx` caps transformers at <4.58 against this project's pinned 5.8.1, so the documented `export_dynamic_quantized_onnx_model()` route is closed, and downgrading transformers under the benchmarked models to satisfy an export tool is not a trade worth making. `torch.onnx.export` + `onnxruntime.quantization` do the job with no transformers dependency. (2) **Three export defects are now encoded in the script** rather than rediscovered: transformers 5.x's mask builder raises `IndexError` under the legacy tracer (needs `dynamo=True`), the dynamo exporter writes `value_info` that onnx's own shape inference re-derives as `(1024) vs (1)` and aborts the quantiser (needs the annotations stripped), and a >2GB model writes external data so `st_size` alone reports 0 MB. (3) **The memory cost is the method's, not the machine's:** a 2.27GB fp32 graph, a 569MB int8 session and the torch CrossEncoder in ONE process, on top of the Docker VM, drove swap to 8.4GB on 16 GiB. **Two hypotheses for the poor timing, both untested:** the shipped CrossEncoder's `max_seq_length` is **8192**, so long chunks make very large sequences, and `DynamicQuantizeLinear` recomputes activation ranges per batch, which is exactly where that sequence length is expensive. **Next attempt must be one backend per process, `max_length` capped, and detached.** `models/onnx/` was deleted to reclaim 4.8GB; `onnx_rerank_export.py` recreates it in ~50s. **If parity or the speedup fails, the fallback is cutting `CANDIDATE_K` WITH a fresh dev measurement** (`DEPLOY-6-RESOLVED`), and that possibility is now materially more likely than when the route was chosen |
| DEPLOY-9 | **`DEPLOY-8`'s two remediations split on measurement: one-backend-per-process is adopted, and capping `max_length` is RETRACTED as both useless and unsafe. The parity script now runs in phases and needs no torch model at all for the parity leg** | `DEPLOY-8` proposed both fixes untested, and one of them does not survive contact with the data. **The cap is retracted.** Its premise was that the shipped CrossEncoder's `max_seq_length` of 8192 makes very large sequences. Measured over 3,000 live `variant=A` chunks: p50 **854** tokens, p95 1452, p99 1504, **max 1592** — so 8192 never binds, and because `padding=True` sizes each batch to its own longest member rather than to the ceiling, a cap cannot remove compute that was never being spent. It is not a harmless no-op either: `retrieve.py:262` calls `predict(pairs, batch_size=32)` with no override, so the published fp32 scores in `retr7_rr_dev_scores.jsonl` were produced at 8192, and capping below ~1600 would truncate real chunk text — turning a script whose entire claim is "only the reranker's arithmetic differs" into one that also measures truncation. `--max-length` survives as a worst-case bound only, defaulted to 2048 above the observed maximum, and the run now **counts truncated pairs and prints a warning if any is non-zero**, so the isolation claim is checked rather than asserted. **The process split is adopted, and it is cheaper than expected:** the parity leg needs no torch model whatsoever, because the fp32 side is the published file, so it constructs `AutoTokenizer` + the ORT session and nothing else — the original script pulled in the whole `CrossEncoder` purely to reach `.tokenizer`, which is a large part of the footprint `DEPLOY-8` blames. Phases are `parity` / `latency-fp32` / `latency-int8` / `report`, each a separate process re-exec'd by `--phase all`, so the OS reclaims a backend at exit; partials cross between them as JSON under `data/onnx_parity_partial/`. The two latency legs are cross-checked on question ids **and** pair counts before combining, which catches a stale partial from a different `-n` or seed being reported as a speedup. Helpers verified: sampling is deterministic in `n`+`SEED` alone across processes, 50 candidates per question, Kendall tau ±1 on synthetic input. **Still unmeasured: parity itself and the speedup** — this row is about the harness, not the result |
| DEPLOY-10 | **The Docker VM holds a fixed 7.65 GiB of a 16 GiB machine regardless of load, and Postgres inside it uses 188 MiB. Cut the allocation before any measurement pass** | Observed after the previous session's job was killed: `com.apple.VirtualMachineService` still showed ~7.9 GB in Activity Monitor with nothing running. It is not a leak and it is not the killed process. `docker info` reports `MemTotal` **8215117824 bytes = 7.651 GiB**, a preallocated guest-RAM region owned by a VM process independent of anything on the host, so killing a host Python job cannot shrink it; `docker stats` shows the only container at **188MiB / 7.651GiB (2.4%)**, i.e. ~7.5 GiB reserved and idle. **This is the same instrument trap the housekeeping section already flags:** `ps` RSS reports **0.70 GB** for that process against Activity Monitor's ~7.9, because RSS cannot see the guest's mapped memory — exactly the reason `AGENT-24` went undiagnosed for so long. **Consequence:** `AGENT-20`/`AGENT-24`'s saturation and `DEPLOY-8`'s 8.4 GB swap were both measured with roughly half the machine's RAM held by a VM serving a 188 MiB database. Lowering Docker Desktop's memory limit to ~3 GiB frees ~4.5 GiB for the ONNX and latency passes at no cost to Postgres. **Not yet applied — it is a Docker Desktop setting, so it is the user's to change**, and any latency published afterwards must record the new allocation next to it, per the standing rule that a latency without machine state is not a measurement |
| DEPLOY-11 | **ONNX/int8 is ABANDONED: it is 3.1x SLOWER than fp32 on CPU, not faster, so `DEPLOY-6-RESOLVED` is reversed and the fallback — cutting `CANDIDATE_K` with a fresh dev measurement — becomes the live route.** Parity was never measured and does not need to be | The route's whole purpose was CPU speed at unchanged `CANDIDATE_K`. Measured 2026-09-06 on `finqa_dev_353`, 50 pairs, same question back-to-back in one process: **int8 194.7s vs fp32 63.0s**. **The decisive detail is that int8 does not scale with threads at all** — 194.7s at `intra_op_num_threads=4` and 195.2s at 8, against fp32's 63.0 -> 60.7 — so this is not thread starvation and no session tuning recovers it. It fits `DEPLOY-8`'s own export note that **48 `MatMul` nodes stayed fp32**: each is a quantise/dequantise boundary, and `MatMulInteger` has no optimised ARM64 kernel in this `onnxruntime` build, so the conversion is both partial and slow on the parts it did convert. **The failure is on the leg nobody thought to condition on.** `DEPLOY-6-RESOLVED` made the route conditional on *ranking parity* and treated the speedup as given; parity is now moot, because a path that is lossy AND slower has nothing to recommend it. **What the 20-minute smoke run bought:** it killed a route that a full `-n 50` parity pass would have spent ~2.9h characterising. Running the cheap leg first is the transferable lesson. **Two misreads corrected in-session, both mine:** the smoke test's `0.64x` compared *different questions* (the phases slice `_load_rows` differently), so the real ratio is 3.1x and worse, not better; and the parity phase's 208s/question mean is a saturated-regime number, not int8's latency. **Not established and deliberately not chased:** parity itself, and whether int8 would win on Graviton, which has different int8 kernels from Apple Silicon — the fallback is cheaper than testing that. `models/onnx/` deleted again (4.8GB); `onnx_rerank_export.py` recreates it in ~50s if ever needed |
| DEPLOY-12 | **Two numbers that gate EC2 sizing are now suspect: the container is 3.5x slower than native on the SAME operation, and CPU rerank latency on this laptop drifts ~1.45x within six questions** | Both found while measuring something else, and both sit under `DEPLOY-6`'s sizing extrapolation. **(1) Container vs native.** `DEPLOY-6` measured 207.8s of cross-encoder for one question in the container; native fp32 CPU on the same 50-pair workload is **63.0s**, first question both times, so this is not drift. 3.5x on identical work. Leading suspicion is the linux/arm64 torch build's BLAS against native Mac Accelerate, **untested**. `DEPLOY-6` reasoned "755% CPU, so a 2-vCPU host implies ~800s/question" — that scales a number which **does not scale**: fp32 went 63.0s -> 60.7s from 4 to 8 threads, i.e. the reranker is memory-bandwidth bound, not core bound. So the sizing extrapolation is unsafe in both its base and its scaling law, and must be re-derived. **(2) Drift, with a same-question control.** Six questions in one process: 60.1 / 62.0 / 79.3 / 78.3 / 75.6 / **87.1s**, last/first **1.45x**. Not question variance: `finqa_dev_353` ran 63.0s and 60.7s at positions 1-2 of a fresh process and **79.3s** at position 3 here — same question, same 50 pairs, +26% from position alone. **Mechanism is NOT `AGENT-24`'s:** swap was 1.0GB during and after, so nothing is paging. The machine is `Mac15,12`, a **fanless MacBook Air M3**, and the curve (flat for two, then a persistent step down) fits thermal throttling under sustained multi-core load; `pmset -g therm` reports nothing, but housekeeping already records that instrument as having never fired here, so it is uninformative rather than exculpatory. **Cause unconfirmed — the discriminating test (idle gaps between questions) was not run.** **Consequence to carry forward:** `AGENT-24` certified stage latency "flat over 25+ questions" on the MPS path; this is a CPU path and a different mechanism, so that certification is not contradicted — but the `OBS-13` tile's 100-question pass runs on this same fanless machine, and if throttling is live it would publish a saturated p95 into the very gate `OBS-13` exists to protect. Record machine state and per-question times, not just a p50/p95, when that tile is regenerated |
| DEPLOY-11-CORRECTED | **`DEPLOY-11`'s mechanism is WRONG and its headline reverses: int8 is not slower than fp32, it is ~1.5x FASTER within its own runtime. The 3.1x was a BACKEND gap, not a precision one. The abandonment decision stands for macOS; its reason does not, and it does not transfer to Graviton** | `DEPLOY-11` compared `latency-fp32` (torch CrossEncoder) against `latency-int8` (ONNX Runtime session) and read the difference as precision. Those are different backends. Verified on this machine: `torch.__config__.show()` reports **`BLAS_INFO=accelerate`**, so torch's matmuls reach Apple's undocumented **AMX** coprocessor; ORT's MLAS never uses Accelerate and gets plain NEON. `ort_fp32_latency.py` measured the missing cell -- three legs, one backend per process, same 3 questions, same 50 pairs: **torch-fp32 58.5s, ort-fp32 415.7s, ort-int8 276.3s** (p50). **ort-fp32/torch-fp32 = 4.5/7.1/7.6x per question** -- same precision, so it is purely backend. **int8/fp32 within ORT = 0.38/0.66/0.83** -- int8 wins all three. **Why the fast kernel is missing:** ORT gates its i8mm QGEMM kernels behind `#if defined(__linux__)` (`mlas/lib/platform.cpp`); this M3 reports `FEAT_I8MM: 1` and ORT detects it, then declines the kernel because the OS is not Linux. The SVE fallback does not help either -- no M-series has SVE (`FEAT_SME: 0`). **Consequence for `DEPLOY-12`(1):** its "leading suspicion, untested" is now established -- 63.0s native vs 207.8s container is the AMX-vs-NEON gap (Linux cannot reach AMX), not container overhead, and 207.8s (torch, no AMX) ~ 194.7s (ORT int8, no AMX) is the tell. **Consequence for Graviton:** Linux + i8mm means the gated kernel compiles IN, and AWS documents ~30% for BERT/RoBERTa on ORT >=1.17 -- while torch there loses Accelerate. **So the container's 207.8s is the WEAK configuration and ORT-int8-on-Graviton is untested.** **Caveats that must ship with these numbers:** n=3; the ORT legs drift upward within the run (fp32 1.5x, int8 3.3x) while torch is flat at 1.11x, so the directions are robust and the magnitudes are not -- do not quote 7.11x, quote the 4.5-7.6x range and the machine state. torch's 58.5s replicates `DEPLOY-11`'s 63.0s within 7%; the int8 leg did not replicate (276.3s vs 194.7s, different questions) | Kept: ONNX stays dead on macOS -- 276s against torch's 58s is not close, so the *decision* was right for the wrong reason. Reversed: the claim that quantization is the problem. Reopened: int8 on Graviton, deliberately scoped as a ~20-minute measurement on the EC2 host we are building anyway, not as its own workstream |
| DEPLOY-13 | **The price of cutting `CANDIDATE_K` is replayable exactly from disk -- no reranker run, no GPU, no spend** | `retr7_rr_{dev,test}_scores.jsonl` stores all 50 candidates per question in first-stage RRF order with the rerank score attached (the property `static_ranking_order.py` locks). Serving at `CANDIDATE_K=K'` hands the reranker the first K' of that list and takes the top 10 by score -- both halves already on disk, so slicing at K' and re-sorting reproduces the served ranking bit for bit. `scripts/eval/candidate_k_curve.py` prints recall@10, nDCG@10 and the pool ceiling per K over both splits. **Prices the reranker's input only:** the first stage is unchanged, which is the right scope, since search is 0.241s against rerank's 32.60s | Written rather than assumed, because the K-cut became the live route by elimination (`DEPLOY-11`) and never on a measured cost |
| DEPLOY-14 | **Cutting `CANDIDATE_K` CANNOT reach an interactive p95 at any acceptable recall, and it is therefore not a latency solution -- it is at best a 20-40% trim. The p95 target has to come from the backend or be restated** | Measured on both splits via `candidate_k_curve.py` (`DEPLOY-13`), n=1235 dev / 1545 test. **test recall@10 by K: 50 -> 0.747, 40 -> 0.736, 30 -> 0.711, 25 -> 0.669, 20 -> 0.627, 10 -> 0.523.** Dev tracks it within 0.01-0.03 at every K, so this is not a dev artifact. **The decisive arithmetic:** rerank cost is linear in K, so reaching an interactive number from the container's 207.8s needs a 10-20x cut, and **K=10 costs 0.224 recall on test** -- more than the entire filter+strip headline gain of +0.140. The lever is linear in latency and near-linear in recall, and the required cut is far past where the curve collapses. **Why it collapses:** measured recall tracks the pool ceiling almost exactly at every K (test K=30: 0.711 against a 0.730 ceiling; K=20: 0.627 against 0.637), so cutting K destroys the CANDIDATE POOL, not the reranker's ordering -- the gold is genuinely spread through the first stage's tail, not front-loaded. The reranker is already recovering 95-98% of whatever pool it is handed, which is `RETR-33`'s finding restated: first-stage candidate generation is the constraint. **What IS free:** K=40 costs 0.012 test recall for 20% less rerank work; K=30 costs 0.037 for 40%. Both are real and neither is a p95 fix | Adopted: **K=30 as a modest trim, not as the latency answer** -- it keeps recall at 0.711 test, above Arm 3's 0.629 pre-filter level, for 40% of the reranker's work. Rejected: any K below 25, at any latency benefit. **Consequence:** `DEPLOY-11`'s "fallback becomes the live route" is now closed too -- BOTH routes it named are exhausted, so the p95 gate rests on the backend (ORT int8 on Graviton, per `DEPLOY-11-CORRECTED`, or a purpose-built server such as HF TEI) or on restating the target as non-interactive. Do not size EC2 on the assumption that K-cutting will rescue the number |
| DEPLOY-15 | **Docker's default 64MB `/dev/shm` fails the HNSW index build, and the error names the wrong resource: it says "No space left on device" while the host has 3.9G free** | `pg_restore` of the 670MB dump into the containerised Postgres exited 1 with `errors ignored on restore: 2`. Every row loaded (A=99654 / B=4708 / C=1665) and `chunks_bm25_idx` built; only `chunks_embedding_hnsw` was missing. The failing statement was `CREATE INDEX ... USING hnsw`, and the error was `could not resize shared memory segment to 1070632832 bytes: No space left on device`. **Not disk.** `df -h /dev/shm` read 3.9G on the host and **64M inside the container** — Docker's fixed default, independent of instance RAM, so resizing the box does nothing. Postgres allocates parallel index-build workspace there, and `maintenance_work_mem=1GB` asks for more than 64M | `shm_size: 2gb` on the Postgres service in the deploy compose file. Index then built in **65s**. **Two habits this reinforces:** an exit code is not a restore verification — `errors ignored on restore: 2` is a warning line, and `pg_indexes` is what caught the missing index; and diagnose shared memory *inside* the container, never from the host |
| DEPLOY-16 | **The `variant` AST guard crashes the SERVICE at startup on any non-UTF-8 file matching `*.py` — it caught `SyntaxError` but not `UnicodeDecodeError`, and `get_conn()` runs it on the serving path** | The API container failed lifespan startup with `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xa3 in position 45` from `preflight.py:50`'s `ast.parse(path.read_text())`. Trigger was 20 AppleDouble `._*.py` sidecars that macOS `tar` wrote into the build context and `rglob("*.py")` duly matched. **The sidecars were an own goal; the uncaught exception is a real defect** — the guard runs inside `get_conn()`, so any undecodable `.py` under the scan dirs takes the API down rather than failing a lint run, and `restarts=5` with `exit=0` made it look like a slow start | Catch `(SyntaxError, UnicodeDecodeError)` and skip: Python source is UTF-8 by definition, so an undecodable file is not a module the guard can miss. Added `._*` to `.dockerignore`, and build the context with `COPYFILE_DISABLE=1 tar`. **Same class as `RETR-24`/`AGENT-16`:** code assuming a shape — here, that everything named `*.py` is decodable Python |
| DEPLOY-17 | **`t4g` burstable instances make latency unmeasurable, and `t4g.small` cannot run this service at all. Deployment host is `c7g.2xlarge` (8 vCPU, 16 GiB, Graviton3, non-burstable)** | Two separate findings. **(a) Memory:** the API container is **3.95 GiB resident** under load, against `t4g.small`'s 2 GiB total — so the free tier was never a candidate, and `DEPLOY-6`'s free-tier sizing question is closed by RAM before latency enters it. **(b) Throttling:** on `t4g.large` in `Standard` credit mode a single `/ask` ran 1029s+ without finishing, while `CPUCreditBalance` fell 34.4 -> 0.04 over 35 minutes; CPU dropped from 199.9% to ~165% and heading to baseline. Latency became a function of instance uptime, not of the workload. **This is the third instance of the same trap** — laptop rerank drift was blamed on battery, then thermals, and was MPS memory (`AGENT-24`); here it is CPU credits | `c7g.2xlarge`: non-burstable, so no credit cliff; Graviton3 rather than t4g's Graviton2, which also makes it the better target for the open ORT/i8mm question (`DEPLOY-11-CORRECTED`). ~$0.29/hr, stopped when idle. **Standing rule, now three-for-three: no latency number ships without the machine state recorded beside it** — and for burstable instances that state includes the credit balance |
| DEPLOY-18 | **First end-to-end measurement on the real deployment target: 157.7s/question, of which rerank is 156.9s — 99.5%. The service is correct and it is not interactive, and both halves are now measured rather than extrapolated** | `c7g.2xlarge`, nothing else running, warm process. `POST /ask` on `finqa_dev_753`: wall clock **160s**, `total_s` **157.66**, `rerank_s` **156.91**, `search_s` 0.589, `embed_s` 0.154, `resolve_s` 0.0005. Top citation `BLK_2016_1364742` chunk 81 at **0.9955** — right company, right year — and the answer computed 746/4,915 = 15.18% with its inputs shown. Warmup (model load + one full `retrieve`) is **75s** on 8 cores against ~14 min on the throttled 2-core box. Beats the Mac Docker VM's 207.8s rerank, so `DEPLOY-12`'s flagged sizing basis can be re-derived from this instead | **Publish 157.7s as the deployed per-question latency and state the host beside it.** Embed+search together are 0.74s, so no first-stage work can move the number — `DEPLOY-14`'s conclusion holds with a real figure behind it: reaching interactive from 157.7s needs ~50-75x, and K=10 buys 5x for 0.224 recall. **Restate the p95 target as non-interactive in the writeup**, with the three measured routes behind it. **Also observed, for the failure analysis:** the answer offered two figure sets (746/4,915 and 750/4,938) — the model reading both the 2016 and 2015 comparative columns, which is `RETR-3`'s self-reprinting problem surfacing in GENERATION rather than retrieval |
